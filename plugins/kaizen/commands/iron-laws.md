---
name: iron-laws
description: CLI over the iron-laws single-source-of-truth registry — the non-negotiable rules for kaizen-plugin-original development. list every law, show one law's full record, check code against the auto-enforced laws (staged diff or whole-plugin audit), or render the read-only reference. Subcommands - list | show <id> | check [--staged|--all|--law <id>] | render
argument-hint: [list|show <id>|check [--all|--law <id>]|render]
---

# /kaizen:iron-laws

!`python3 ${CLAUDE_PLUGIN_ROOT}/skills/workflow/scripts/iron_laws.py $ARGUMENTS`
