---
name: review
description: "Fast diff-time code review (HEAD vs base). Runs analyzers, returns severity-tagged findings. Pair with /kaizen:audit for periodic full-repo."
argument-hint: [--scope <dir>|--base <ref>|--json|--agent]
---

# kaizen review

!`bash ${CLAUDE_PLUGIN_ROOT}/skills/workflow/scripts/review.sh ${ARGUMENTS}`
