---
name: browser
description: "Manage Playwright-backed MCP browser server (navigate/click/type/screenshot/extract). After install + /reload-plugins, mcp__kaizen-browser__* tools become available."
argument-hint: [install|check|status|path|fg]
---

# kaizen browser

!`bash ${CLAUDE_PLUGIN_ROOT}/bin/kaizen-browser ${ARGUMENTS:-check}`
