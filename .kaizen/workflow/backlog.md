# Backlog

> Generated from `backlog.json`. **Do not edit this file** — use
> `kaizen backlog {add|start|tick|done|park|render}`.
> Schema: `kaizen.backlog` v1.

## In flight

_(none)_

## Next up

- [ ] **BK-020** kaizen:karpathy-check — automation upgrades — probe: `/kaizen:karpathy-check fires automatically on commit-msg / pre-commit boundaries (opt-in); CI workflow runs it on PRs; threshold + scope configurable via .kaizen.toml` — verify: `Wire as opt-in pre-commit hook (KAIZEN_KARPATHY_AUTOCHECK=1) firing on staged diff; add gha workflow step; surface findings in gatekeeper-style envelope; integration test: commit with deliberate over-nesting fails the autocheck when knob is set, passes when off. Also: structured JSON output mode for CI gating, severity thresholds (warn-only vs block), and per-axis disable knobs`  `[karpathy automation gate ci]`
- [ ] **BK-022** kaizen:backlog → Claude Code TaskList auto-sync (no roundtrip) — probe: `kaizen-backlog add/start/tick/park automatically reflects into the active CC TaskList (subject/description/status), so the agent doesn't need separate TaskCreate calls` — verify: `Hook layer that bridges backlog state changes to the CC task surface. Two directions: (a) on 'kaizen-backlog add', emit a TaskCreate equivalent via a SessionState-aware mechanism (file watcher or daemon tick) so the next agent prompt picks it up; (b) on 'kaizen-backlog tick/done', emit TaskUpdate completed. Constraints: CC TaskList lives in-harness, not on disk — so the bridge needs either a CC SDK hook OR a marker file the agent loads on next prompt. Likely shape: write to .kaizen/inbox/backlog-task-events.jsonl, then UserPromptSubmit hook surfaces unprocessed events as 'pending task suggestions' for the agent to ack with TaskCreate calls. Integration test asserts add-then-prompt surfaces the suggestion.`  `[backlog task-list automation bridge]`
- [ ] **BK-024** kaizen — project vs global metadata scope discipline (audit + lint) — probe: `Every kaizen feature that persists state declares its scope: project (per-repo, lives in .kaizen/ or <repo>-slug memory) vs global (user-wide, lives in ~/.claude/.kaizen/ or ~/.claude/projects/default/memory). Lint catches features that write to the wrong scope (e.g. project-scoped data leaking to global path).` — verify: `(a) Survey existing persisted-state features (backlog, handoff store, brain, dxm, trace, gold, intent, inbox) — document scope for each in a domain/state-scopes.yaml lens manifest. (b) Add iron-law: every script that writes state must resolve its path via _paths.py and the path must be tagged project|global in the manifest. (c) BK-023 ledger declares scope=project (per-repo memory dir, NOT default/memory). (d) Integration test: feature writes go to the declared scope only — no cross-scope bleed.`  `[scope metadata lint iron-law]`
- [ ] **BK-025** SessionStart auto-discovery → QA the user (in-flight items, stale state, drift) — probe: `On SessionStart, a discovery hook surveys local state (in_flight backlog items, uncommitted changes, stale handoff per BK-021, pending TaskList tasks, recent gold drops) and surfaces a tight QA prompt (AskUserQuestion or one-line summary) asking 'resume X?' or 'pivot?' — no manual /handoff resume needed` — verify: `(a) Hook script gathers: in_flight backlog count + IDs, last handoff age + verdict, uncommitted file count, pending tasks; (b) emits a single envelope with 'discoveries' and 'recommended_action'; (c) surfaces via SessionStart additionalContext (one short block, not 30KB); (d) when nothing pending, hook is a silent no-op (KISS — don't talk if nothing to say); (e) integration test: prime fixtures, fire hook, assert envelope shape + content; (f) wired via plugin.json hooks section`  `[session-start hook discovery qa handoff]`
- [ ] **BK-026** plugin-development — full audit of the dev-workflow surface (gaps + stale refs) — probe: `Audit produces a single report: every feature in plugins/kaizen/scripts/ vs feature-shape.yaml slots; gap-classified (missing slot / present-but-out-of-spec / stale path reference / undocumented surface). Output to .kaizen/workflow/audits/plugin-development-<ts>.md + .jsonl` — verify: `(a) Walk plugins/kaizen/scripts/<feature>/ + skills/<feature>/ + commands/<feature>.md + bin/kaizen-<feature> + tests/test_<feature>*.py for each known feature ID. (b) Predicate-evaluate against feature-shape.yaml::predicates. (c) Cross-check feature-shape.yaml's path_patterns against ACTUAL layout (skills/workflow/scripts/<feature>.py in yaml vs scripts/<feature>/<feature>.py in reality — stale conventions). (d) Scan SKILL.md bodies for stale path refs to ~/.claude/skills/workflow/, .kaizen/superpowers/, etc. (e) JSONL-indexed deliverable per [[Notes/pref-jsonl-indexed-deliverables]].`  `[plugin-development audit gaps stale]`
- [ ] **BK-027** subagent dispatch — optimizations + analyze pass + content-delivery + gaps — probe: `Audit + improve the subagent dispatch surface: kaizen-implementer / general-purpose / Explore / Plan / wf-*/ kaizen-* subagents. Coverage gaps, prompt-template quality, return-shape consistency, dispatch-rubric accuracy` — verify: `(a) Survey every available subagent_type in plugins/kaizen/agents/ and ~/.claude/agents/; tabulate tool-inventory, model preference, isolation defaults. (b) Identify gaps — work-types that have no good subagent fit (per dispatch-rubric.yaml). (c) Audit prompt-template quality: do prompts hand over self-contained context (file paths, line numbers, what to change)? Flag 'based on your findings' anti-patterns. (d) Standardize return-shape across subagents (envelope vs prose vs hybrid); content-delivery contract for 'brainstorm', 'audit', 'implement', 'review'. (e) Refine the dispatch rubric with real-session evidence (which dispatches were chosen, were they right). Ship as a single audit deliverable + targeted fixes to dispatch-rubric.yaml + agent definitions.`  `[subagent dispatch audit optimization]`
- [ ] **BK-028** Semantic score matrix for decision-making / uncertainty (start: brainstorm scoring) — probe: `kaizen-score matrix --input <ideas.jsonl> --axes feasibility,leverage,uncertainty,fit returns a typed envelope ranking each input on each axis with confidence. v1 targets brainstorm/idea-pile jobs; v2 fans out across the plugin (handoff outcome rubric extension, backlog probe scoring, gold→brain promotion candidates, agent dispatch fit).` — verify: `(a) Define scoring axes catalog in domain/score-axes.yaml: feasibility / leverage / uncertainty / fit / novelty / cost / urgency — each with definition + signal sources. (b) Bridge to embeddings or LLM scoring (Ollama-backed, free local first per kaizen:models pattern). (c) Output: structured matrix CSV / jsonl + markdown summary. (d) Integration: 'kaizen-score brainstorm <file.jsonl>' adds {axis_scores: {feasibility: 0.x, leverage: 0.y, ...}, uncertainty: 0.z} to each entry. (e) Fan-out targets after v1: kaizen-handoff (extend outcome rubric); kaizen-backlog (probe-quality score); kaizen-gold (promote-readiness); kaizen-agent dispatch (fit score for the work-type). (f) TDD: golden-output fixtures for known idea piles.`  `[scoring decision uncertainty brainstorm plugin-wide]`
- [ ] **BK-029** backlog: atomic write for backlog.{json,md} (tempfile+rename) — probe: `Ctrl-C during save_store or auto-render leaves half-written backlog file → corrupt JSON or partial .md` — verify: `save_store + cmd_render write via tempfile.NamedTemporaryFile then Path.replace(); existing tests confirm content; add canary test that interrupts mid-write`  `[backlog atomic robustness]`
- [ ] **BK-030** backlog: cmd_tick idempotent on already-done items — probe: `kaizen-backlog tick BK-N where N is already done silently re-stamps committed_at — should no-op or warn` — verify: `If item.section already done, exit 0 with informational note (no mutation); --force re-stamps. TDD covers both paths`  `[backlog idempotency ux]`
- [ ] **BK-031** backlog: list --pending alias for in_flight+next_up — probe: `No single command shows 'active queue' (in_flight + next_up combined); have to run two list calls` — verify: `kaizen-backlog list pending returns both sections in one envelope; --json includes section field per item`  `[backlog ux]`
- [ ] **BK-032** commands/memory-ledger.md description trim below 250 chars (karpathy) — probe: `wc -c on description field exceeds the soft karpathy threshold` — verify: `Trim to <=250 chars while preserving the trigger phrase set; gate test that asserts every commands/*.md description <=250`  `[commands karpathy boy-scout]`
- [ ] **BK-033** commands/backlog.md mentions done alias (BK-019 follow-up) — probe: `done alias was added to backlog CLI but commands/backlog.md help table + description still list only tick` — verify: `verb table row added; description trigger list includes 'done'; test grep ensures parity with argparse choices`  `[backlog docs boy-scout]`
- [ ] **BK-034** backlog.md generated header lists verify verb — probe: `render_md header line currently mentions {add|start|tick|done|park|render} — verify is omitted despite being a real verb` — verify: `Update render_md header line; test_backlog asserts 'verify' appears in the regenerated header`  `[backlog docs boy-scout]`
- [ ] **BK-035** CLAUDE.md adds memory-ledger to Discovery surface block — probe: `memory-ledger is the new umbrella over 16 surfaces but CLAUDE.md doesn't reference kaizen-memory-ledger catalog as a discovery handle` — verify: `Add 1-2 lines under 'Discovery surface' pointing at kaizen-memory-ledger catalog and the manifest path`  `[claude-md docs boy-scout]`
- [ ] **BK-036** memory-ledger status: --missing-only filter flag — probe: `kaizen-memory-ledger status prints 16 lines; when scanning a fresh repo most are present and the noise hides the real findings` — verify: `Add --missing-only flag suppressing present entries in both human + json modes; counts/summary still show full numbers`  `[memory-ledger ux]`
- [ ] **BK-037** memory-ledger flow --mermaid emits sequenceDiagram (first cut of ML-004) — probe: `Reading continuity_flow as raw json/text is high-friction; visual sequenceDiagram makes phase transitions scannable` — verify: `flow --mermaid emits a valid mermaid sequenceDiagram block (Actor=phase, Note=surfaces). Round-trip test parses + asserts node count == phases count`  `[memory-ledger visualization]`
- [ ] **BK-038** kaizen-backlog show missing-ID returns structured error envelope — probe: `kaizen-backlog show BK-999 prints 'id not found' + exit 2 — no --json envelope, agents can't parse` — verify: `Wrap not-found in {error: 'id-not-found', id: 'BK-999'} envelope when --json set; exit 2 preserved; test covers both modes`  `[backlog envelope api]`
- [ ] **BK-039** kaizen-help-gen visibility — auto-regenerated commands/help.md needs an entry-point — probe: `commands/help.md regenerated silently when I added memory-ledger; no documented verb for the help-gen step + no test that asserts every new command lands in help.md` — verify: `Either document the existing kaizen-help-gen invocation OR add a 'kaizen help regen' verb. Add a test that grep-asserts each commands/*.md appears in the generated help.md.`  `[help-gen discoverability docs]`
- [ ] **BK-040** workflow_runner: legacy state.json cleanup nudge fires on every commit — probe: `Every commit prints 'legacy state.json at .kaizen/workflow/state.json (missing current_stage/schema; written by an older runner)' — noisy, not actionable inline` — verify: `Two options: (a) auto-migrate the legacy file on first run; (b) silence the warn after a one-time acknowledgment knob. Pick (a) — TDD test runs old-format state.json through the migration + asserts new shape`  `[workflow migration noise-reduction]`
- [ ] **BK-041** kaizen-backlog tick --committed resolves 'HEAD' to git rev-parse sha — probe: `Used 'kaizen-backlog done BK-N --committed HEAD' multiple times this session — literal string 'HEAD' stored. Should resolve to short SHA so it pins to a commit not a moving ref` — verify: `If --committed value is 'HEAD' or starts with 'HEAD~', resolve via git rev-parse --short and store the SHA. Test covers HEAD / HEAD~1 / explicit-sha / arbitrary-string passthrough`  `[backlog git ux]`
- [ ] **BK-042** Lint: large markdown deliverables without sibling .jsonl (pref-jsonl-indexed-deliverables) — probe: `Brain note pref-jsonl-indexed-deliverables says >=30-entry deliverables ship paired .md + .jsonl, but no automation catches violations` — verify: `Add a deliverable-shape rule scanning .kaizen/{plans,audits,brainstorms}/*.md — when entry count >=30 (counted via headings or list items), require sibling .jsonl. Fires as a gatekeeper soft finding`  `[lint gatekeeper deliverables]`
- [ ] **BK-043** handoff: _parse_handoff_yaml ImportError path also surfaces _parse_error — probe: `Current code sets _parse_error on YAMLError but the ImportError branch (pyyaml missing) also silently returns empty body — same silent-FAILED class as the bug we just fixed` — verify: `ImportError branch also sets _parse_error='pyyaml unavailable: <msg>'. Existing tests still green; new test asserts ImportError → _parse_error present`  `[handoff robustness parser]`
- [ ] **BK-044** _paths.sh: mirror KAIZEN_BACKLOG_LEDGER_* env knobs for shell consumers — probe: `BK-023 introduced KAIZEN_BACKLOG_LEDGER_DIR + KAIZEN_BACKLOG_LEDGER_DISABLE but only _paths.py was updated (if at all); shell scripts/hooks can't source them via _paths.sh` — verify: `Add the two env knobs to _paths.sh with default expansion + comments. Test: 'source _paths.sh && env | grep KAIZEN_BACKLOG_LEDGER' returns both`  `[paths shell parity]`
- [ ] **BK-045** kaizen:intent — deep dig + analyze the intent system — probe: `Audit covers: every intent rule in skills/intent/domain/intents.yaml — trigger quality (false-positive rate), action shape (suggest vs subcommand), confidence calibration. Surface dead rules + stale subcommand references (e.g. /kaizen:code-tour after cat-2 retirement). Cross-check intent suggestions against the live commands/* surface` — verify: `(a) Survey all rules in intents.yaml + count by action.subcommand. (b) Run each trigger against trace.jsonl sample data to measure firing rate. (c) Flag any suggest: string mentioning a retired slash. (d) Confidence-calibration: rules with high confidence that rarely fire (or never fire) — candidates for retirement/refinement. (e) Cross-check that every action.subcommand resolves to a live commands/<name>.md OR to a skill. Output deliverable to .kaizen/audits/intent-audit-<ts>.{md,jsonl}.`  `[intent audit analyze quality]`
- [ ] **BK-046** trace + analyze code-to-test coverage; auto-generate test stubs (TDD seed pass) — probe: `kaizen-coverage gaps surfaces 1:1 script-to-test deficiencies but doesn't act. Generate failing test scaffolds for each gap so TDD can fill them in. Trace + correlate which scripts have ZERO calls in the trace log — those are dead surfaces that DON'T need tests` — verify: `(a) Walk scripts/<cluster>/*.py (excluding _<x>.py privates) + match to tests/test_<x>*.py. (b) Cross-reference with trace.jsonl call counts per bin/script over a representative time window. (c) For each gap, classify: TDD-seed (real surface needs test), DEAD-CODE (zero trace calls → park for retirement), VENDORED (no test, intentional). (d) For TDD-seed gaps, scaffold tests/test_<name>.py with RED-failing skeleton (one test per public function, asserting return shape). (e) Output coverage-trace audit at .kaizen/audits/coverage-trace-<ts>.{md,jsonl}`  `[coverage trace tdd audit]`

## Done (this week)

- [x] **BK-012** Brainstorming skill — add per-idea confidence score + deferral threshold — probe: `/kaizen:brainstorming today produces a flat list of ideas; no quantitative basis for deferral. Should expose a 0.0-1.0 confidence per idea + a configurable threshold for KEEP vs Phase 2 vs YAGNI cutoffs.` — verify: `brainstorming output emits {description, confidence, deferral_bucket} per idea; threshold configurable via skill arg; bucket auto-suggested + user-confirmable`
- [x] **BK-018** kaizen:debug — code-checker + linter for Python + JSON Schema + YAML + JSONL — probe: `kaizen-debug check --all on a clean tree returns green` — verify: `Integration test injects breakage per axis (bad py syntax / schema-invalid yaml / malformed jsonl / unescaped-quote yaml — the case that silently broke handoff assess on 2026-05-19) and confirms each axis fails independently`  `[debug lint schema yaml jsonl]`
- [x] **BK-019** kaizen:backlog — debug + fix + improve + optimize pass — probe: `kaizen-backlog list/add/start/tick/park/done all return canonical envelopes; JSON↔MD generation is byte-stable across N round-trips; no orphaned BK-N IDs after rebases; verbs interactive when args omitted (AskUserQuestion)` — verify: `Audit existing surface for: (a) shapes/regressions — orphan IDs, status transitions, edge cases in JSON parse; (b) UX — argparse error messages, missing-arg hints, default sections; (c) performance — N=500 backlog items list time, regen time; (d) optimization — schema validation, atomic write, hook integration. Ship targeted fixes + benchmarks. Integration test exercises every verb end-to-end against a sandboxed .kaizen/` — committed: `d3140e7`  `[backlog quality audit]`
- [x] **BK-021** kaizen:handoff — verify gains freshness signal (age + commit-delta thresholds) — probe: `kaizen-handoff verify on a stale handoff returns verdict=drift with freshness reason; on a current handoff returns clean; thresholds configurable via .kaizen.toml` — verify: `Add age signal (days since handoff date) and commit-delta signal (count from handoff HEAD to current HEAD). Roll into verify-rules.yaml::verdict_rollup. Thresholds: stale=>30 days OR >100 commits = drift; >90 days OR >500 commits = regression. Integration test: synthesize handoff dated 60 days ago + 200 commits later, assert verdict='drift' with freshness in qualitative_residue or new freshness:{age_days, commit_delta, verdict} envelope field. Wire schema extension. SKILL.md note about the threshold knobs` — committed: `HEAD`  `[handoff verify freshness staleness]`
- [x] **BK-023** kaizen:backlog → auto-loaded ledger in CC memory dir (token-efficient, no roundtrip) — probe: `Compact backlog summary auto-written to ~/.claude/projects/<slug>/memory/backlog_ledger.md on every save_store; agent reads from auto-loaded context instead of calling 'kaizen backlog list'` — verify: `TDD: (a) save_store side-effect writes backlog_ledger.md with counts header + active items + recent done; (b) format is byte-stable across redundant saves; (c) write is atomic (tempfile+rename); (d) ledger updates within one save call. Path resolves via project_memory_dir helper. Existing 'kaizen backlog list' surface preserved unchanged.` — committed: `HEAD`  `[backlog memory token-efficient auto-load]`

## Parked / deferred

- [ ] **BK-001** trace gap 1+2+3+6: emit internal events for lens-driven handlers — probe: `kaizen trace search 'evt=handoff.verify' should surface hits; today returns 0 because outer Bash trace doesn't carry sub-command granularity` — verify: `after change: kaizen-handoff verify + assess + scaffold + create + auto-finalize each fire a start/end trace event with sid + verdict/bucket payload. ~40 LOC across handlers + ~8 LOC lens-validation + ~8 LOC BucketWalker + ~12 LOC dispatchers = ~70 LOC total. Tests cover each emission`  `[trace observability lens]`
- [ ] **BK-002** trace gap 4: per-tool trace events from MCP servers (21 of 22 untraced internally) — probe: `kaizen trace search 'src=mcp' returns events from state_mcp only; the other 21 mcp__kaizen__* invocations trace only via outer PreToolUse — internal phase/error/cache events are dark` — verify: `shared trace_tool() helper in _mcp_trace.py wraps @mcp.tool; applied incrementally to each *_mcp.py — start+end events per tool call; tested via per-server import smoke`  `[trace observability mcp]`
- [ ] **BK-003** trace gap 5: nested subprocess.run calls invisible to trace (bridge, migrate, flow, scratch) — probe: `handoff bridge --apply shells out to brain.py capture; the brain call doesn't appear in trace because it's subprocess.run not a Bash tool call. Same in migrate.py dispatcher (path_migrate.py / migrate_paths.sh), flow_cli.py (4 children), scratch.py (inner bash -c)` — verify: `shared _traced_run(argv, evt, sid) helper in skills/workflow/scripts/_subproc.py wraps subprocess.run with before+after trace fires; applied at ~5 nested-exec sites; tests cover each`  `[trace observability subprocess]`
- [ ] **BK-004** trace gap 7: long-running ops emit no progress events (onboard/knowledge/brain index, scrape) — probe: `kaizen trace search 'evt=progress' returns nothing; indexers run dark between PreToolUse and PostToolUse — minutes-long ops invisible mid-flight. Statusline can't surface 'indexing chunk 3000/12000'` — verify: `new 'progress.tick' event schema; tick emission inside each indexer's inner loop (every N items or M seconds); statusline reads latest tick when available. Needs schema design first`  `[trace observability progress schema]`
- [ ] **BK-005** trace gap 8+10: tool error details + etu rejection events not captured — probe: `kaizen trace search 'evt=PostToolUse-Bash' shows result=err flag only; the actual error message isn't recorded. Same for etu_decision rejections (eval/find /, etc.) — surfaced to user but not traced` — verify: `posttooluse-trace.sh data payload gains error_msg (truncated 200 chars, redacted for tokens/paths); _bash_gate.py::etu_decision fires evt=etu.reject before returning ask. Privacy review on redaction`  `[trace observability errors privacy]`
- [ ] **BK-006** dxm: hooks-not-firing for install-session — replay history + document reload rule — probe: `fresh install of dxm shows events-<current-sid>.jsonl is empty; CC loads hooks.json at session start only` — verify: `SKILL.md callout + 'kaizen-dxm replay --session SID' subcommand that walks ~/.claude/projects/<slug>/<sid>.jsonl, synthesizes Pre/PostToolUse events into events-<sid>.jsonl. After replay, kaizen-dxm now matches the JSONL's tool call count`  `[dxm observability onboarding]`
- [ ] **BK-007** dxm: extract toolUseID + durationMs + exitCode + command from CC event attachments — probe: `CC events carry attachment.{toolUseID,durationMs,exitCode,command,stderr} but dxm-event.sh extracts only tool_name + session_id` — verify: `Hook script adds 4 grep -oE extractors + extended JSON line template; kaizen-dxm tail shows per-event tool_use_id + duration_ms + exit_code; pair Pre↔Post via tool_use_id is computable`  `[dxm observability capture]`
- [ ] **BK-008** dxm: wire remaining 7 CC lifecycle hooks (SessionStart/End, UserPromptSubmit, Stop, SubagentStop, PreCompact, Notification) — probe: `hooks.json wires dxm-event.sh only into PreToolUse + PostToolUse; the other 7 lifecycle slots fire kaizen hooks but dxm doesn't capture them` — verify: `hooks.json gains 7 entries each passing the event name as arg; kaizen-dxm tail filtered by evt_type shows SessionStart, SessionEnd, UserPromptSubmit, Stop, SubagentStop, PreCompact, Notification events`  `[dxm hooks observability]`
- [ ] **BK-009** dxm: retention/rotation via kaizen-dxm clean + daemon hygiene loop — probe: `events-<sid>.jsonl grows append-only forever; no cap or GC` — verify: `kaizen-dxm clean --older-than 7d removes stale per-session files; daemon.py hygiene loop calls it; max-file-size enforces in-session cap`  `[dxm retention hygiene]`
- [ ] **BK-010** dxm: close handoff integration loops (scaffold→dxm freshness, verify→dxm churn, auto-finalize→dxm link) — probe: `scaffold knows jsonl_lag_seconds but doesn't query dxm when stale; verify ignores dxm tool churn; auto-finalize doesn't record session lineage` — verify: `scaffold mining gains dxm fallback when lag>30s; verify reports last 60s tool churn from dxm; auto-finalize calls 'dxm link --parent --child' as part of indexing`  `[dxm handoff integration]`
- [ ] **BK-011** dxm: TDD the dxm-event.sh shell hot path — probe: `dxm-event.sh has zero test coverage; regression in grep -oE / sed / printf template would silently break capture` — verify: `tests/test_dxm_hook_sh.py feeds canned event JSON to the hook, asserts the resulting JSONL line shape; covers BSD date %N fallback + KAIZEN_DXM_DISABLE=1 early-exit + missing-session_id no-op`  `[dxm tdd hooks]`
- [ ] **BK-013** positional-tokens Phase 2: per-slot embeddings (trigger: onboard.db unification) *(design/2026-05-18-positional-token-schema-design.md:38-41,271)* — probe: `grep 'embedding' plugins/kaizen/skills/workflow/scripts/_token_db.py` — verify: `test_token_db tests for embedding column + retrieval green; semantic search returns slot-granular hits`  `[tokens phase-2 yagni-deferred trigger-gated]`
- [ ] **BK-014** positional-tokens Phase 2: tree-sitter-typescript + tree-sitter-markdown grammars (trigger: first TS/MD user) *(design/2026-05-18-positional-token-schema-design.md:40,268-270)* — probe: `grep -E 'typescript|markdown' plugins/kaizen/skills/workflow/scripts/_token_extractor.py` — verify: `test_token_extractor: TS file slots, MD heading slots extracted; _LANG_BY_EXT covers .ts/.tsx/.md`  `[tokens phase-2 yagni-deferred trigger-gated]`
- [ ] **BK-015** Brainstorm signal: novelty_score (Jaccard idea-overlap) *(docs/superpowers/specs/2026-05-18-brainstorming-confidence-score-design.md)* — probe: `Phase 2 deferral from BK-012 spec §row 3 — implement Jaccard-overlap signal so the rubric can flag near-dup ideas during the score pass` — verify: `Trigger: first duplicate-idea complaint OR ≥2 rubric-tuning sessions where bucket distribution warrants overlap discount. Verify: novelty_score in signals dict + new rubric rule + paired test for the dup-collapse case.`  `[brainstorm phase-2 signals]`
- [ ] **BK-016** kaizen-brainstorm stats subcommand (bucket distribution drift) *(docs/superpowers/specs/2026-05-18-brainstorming-confidence-score-design.md)* — probe: `Phase 2 deferral from BK-012 spec §row 16 — add stats subcommand that reports bucket distribution + drift across N JSONLs` — verify: `Trigger: bucket distribution shifts ≥20% across 3 brainstorms. Verify: stats subcommand returns per-bucket counts + delta vs baseline + paired tests for distribution-rollup math.`  `[brainstorm phase-2 cli]`
- [ ] **BK-017** Pre-commit gate nudge: *-brainstorm.md without sibling .jsonl *(docs/superpowers/specs/2026-05-18-brainstorming-confidence-score-design.md)* — probe: `Phase 2 deferral from BK-012 spec §row 18 — gate nudges when a brainstorm.md lands without its companion .jsonl` — verify: `Trigger: first real 'I forgot to score it' incident. Verify: new gate check in skills/iron-laws or commit-msg hook that warns when *-brainstorm.md staged without sibling .jsonl.`  `[brainstorm phase-2 gate]`
