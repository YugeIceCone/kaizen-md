# Architecture Log

> Append-only structured log of architectural-shape changes to the kaizen
> plugin. Format + discipline: `plugins/kaizen/skills/workflow/domain/git-discipline.yaml`
> (`architecture_log` block + the `structural_change_logs_to_progress` gate).
>
> What goes in: new MCP server / skill / indexer, file-set carve between
> skills, locked rule, plan executed end-to-end, dep-direction reversal.
>
> What does NOT: doc-only edits, typo fixes, lint cleanups, test-only
> changes, lockfile bumps, commit-message-body details.
>
> Lookup: rows are `date | scope | Δ LOC | summary` with no sha column —
> `git log --grep="<keyword from summary>"` resolves a row to its commit.
>
> **Backfill note:** this log was created 2026-05-14, resolving an
> agent-self-audit finding — `.kaizen/.gitignore` reserved the slot but
> the file had never been created. Rows dated before 2026-05-14 are a
> best-effort backfill of the major structural events still load-bearing
> for open plans, not a complete history; the pre-existing commit backlog
> was not reconstructed.

| date       | scope    | Δ LOC  | summary                                                                                     |
|------------|----------|--------|---------------------------------------------------------------------------------------------|
| 2026-05-12 | refactor | ~-1.8k | workflow + workflow-routing merged into one schema-driven skill (workflow-merge plan)        |
| 2026-05-12 | feat     | +465   | lint MCP server (lint_mcp.py) — ruff + ty as 7 Claude-callable tools (kaizen-lint-mcp plan)  |
| 2026-05-14 | chore    | +27    | created this architecture log — `.kaizen/.gitignore` slot now backed by a real file         |
| 2026-05-14 | refactor | -171   | carved onboard_index schema DDL + 4 migration fns into new onboard_schema.py module          |
| 2026-05-14 | docs     | +157   | added repo-root CLAUDE.md — Claude Code onboarding rulebook for the marketplace              |
| 2026-05-14 | refactor | ~0     | carved iron-laws.yaml + skeleton into new skills/iron-laws/ skill (iron-laws plan, phase 1)  |
| 2026-05-14 | feat     | ~+1.6k | iron-laws skill complete — SSOT registry + loader + codegen + checker + CLI + MCP + drift gate |
| 2026-05-17 | fix      | +50    | BK-013 — dxm-event.sh _session_jsonl fallback when stdin lacks session_id (fixes silent PreCompact/SessionEnd/SubagentStop/Notification drops) |
| 2026-05-17 | fix      | +25    | BK-014 — precompact-snapshot.sh runs backup sync + emits honest systemMessage with real tarball path (was backgrounded `&` + wrong hardcoded path) |
| 2026-05-17 | feat     | +85    | BK-015 — context.get_usage_summary peak-aware reader (walks all assistant turns, detects compact via isCompactSummary marker, returns current+peak+peak_pre_compact+compact_count) |
| 2026-05-17 | feat     | +35    | context_notifier consumes BK-015 — payload + envelope now include peak_tokens, peak_pct, peak_pre_compact, compact_count (post-compact red-zone signal survives current-zone drop) |
| 2026-05-17 | fix      | +5     | handoff verify — skip pattern_check for narrative bullets (em-dash separator). Real-world: 8 stale false-positives → 0 against handoff #8 |
| 2026-05-17 | feat     | +25    | BK-016 — handoff scaffold mined_summary carries peak_tokens, peak_context_pct, peak_pre_compact, compact_count from BK-015 (compact-resilient handoff signal) |
| 2026-05-17 | perf     | +30    | session-inject-context: UserPromptSubmit trims to git-only (workflow/handoff/plans/project-memory injected once at SessionStart). Saves ~1.4KB per prompt × N turns. |
| 2026-05-17 | refactor | +25    | _dxm_emit.emit_subcommand_complete DRY helper — migrate 8 callers in handoff.py + intent.py to centralized {tool,sub} naming convention. Rename + typo safety by construction. |
| 2026-05-17 | perf     | +15    | context.py `line` subcommand emits pre-formatted statusline segment in 1 spawn (was 5). 160ms → 100ms per statusline render (37% faster). |
| 2026-05-17 | perf     | +95    | pretooluse_trace.py — consolidates 4 python3 spawns into 1 per PreToolUse fire. ~120ms → ~25ms per tool call × 1000s of calls/session. |
| 2026-05-17 | perf     | +85    | posttooluse_trace.py — same fix on PostToolUse (4 spawns → 1). Companion hot-path saving. |
| 2026-05-17 | perf     | +120   | stop_backlog_reminder.py — Stop hook 3 spawns → 1. Also drops a redundant backlog.json double-read + adds KAIZEN_BACKLOG_DISABLE bypass for hook discipline parity. |
| 2026-05-17 | perf     | +60    | userprompt_inbox.py — UserPromptSubmit inbox capture 4 spawns → 1. KAIZEN_INBOX_DISABLE bypass parity. |
| 2026-05-17 | perf     | +80    | intent_userprompt.py — UserPromptSubmit intent matcher 3 spawns → 1. Existing test_intent_scan_hook.py passes unchanged. |
| 2026-05-17 | perf     | +130   | posttooluse_bash_commit.py — PostToolUse(Bash) git-commit suggester 4 spawns → 1. KAIZEN_BACKLOG_COMMIT_DISABLE bypass parity. |
| 2026-05-17 | perf     | +90    | subagentstop_trace.py — SubagentStop 5 spawns → 1. Emits both generic + detail trace events from one process. |
| 2026-05-17 | feat     | +220   | session-intake — SessionStart hook + session_mode CLI + bin wrapper. 1-time QA at session start asks agent to AskUserQuestion for mode (loop/workflow/neither); choice persisted in .kaizen/session-mode.json. KAIZEN_SESSION_INTAKE_DISABLE bypass. Gates: skips when mode set / loop active / workflow in-progress. |
| 2026-05-17 | feat     | +95    | session-intake bundles — Q2 added (multiSelect): Simplicity / Structure / Process / Karpathy. Structure expanded to full architecture family (SOLID, SoC, LoD, Onion-DDD, Hexagonal, Clean, DIP, Bounded-Contexts). --bundles flag expands to skills[] with dedupe. `bundles` subcommand for introspection. Fixed `sof` → `soc` typo. |
| 2026-05-17 | feat     | +60    | /kaizen:mode slash command — explicit alternative to auto-fire SessionStart intake. Takes `loop|workflow|neither` and routes to the disciplines QA (Q2 only); no-arg falls back to full QA. |
| 2026-05-17 | feat     | +90    | 3 new intent rules (start-loop / start-workflow / set-mode-generic) so the agent surfaces /kaizen:mode mid-session when user's phrasing implies it. Empirically fires on "iterate on this", "use a workflow", "harden the plugin", "configure this session", etc. |
| 2026-05-17 | feat     | +175   | per-prompt skills reminder — _SKILL_DESCRIPTIONS catalog (15 disciplines with action-oriented one-liners) + `reminder` subcommand + userprompt-skills-reminder.sh hook. Re-surfaces pinned disciplines every turn so long sessions don't drift. KAIZEN_SKILLS_REMINDER_DISABLE bypass. |
| 2026-05-17 | feat     | +260   | skill_suggest — walks 87 SKILL.md files, extracts quoted trigger phrases from frontmatter descriptions, matches against user prompt → suggests Load Skill(name). New userprompt-skill-suggest.sh hook surfaces top-3 matches. KAIZEN_SKILL_SUGGEST_DISABLE bypass. 23ms full index. Precision-over-recall (only quoted triggers count). |
| 2026-05-17 | refactor | +175 / -180 | skill_suggest pivots from live UserPromptSubmit fire to post-hoc dxm reader. Drops userprompt-skill-suggest.sh + helper + tests (orphan after unwiring). Adds `from-dxm` subcommand that reads dxm events + inbox prompts + cross-references PreToolUse Skill loads — surfaces prompts that matched a skill the agent never loaded. Zero UserPromptSubmit latency (native CC routing handles live decisions). |
| 2026-05-17 | feat     | +700   | auto-handoff — schema-driven context-pressure trigger. New skills/auto-handoff/ skill (config.yaml + 3 JSON schemas), --threshold flag on session_mode (25/50/75/85), Q3 in intake + /kaizen:mode, auto_handoff.py + Stop hook. When threshold crossed: `decision=block` (NOT advisory) forces agent to create handoff before Stop. Once-per-session dedupe via dxm event. KAIZEN_AUTO_HANDOFF_DISABLE bypass. |
| 2026-05-17 | feat     | +200   | auto-handoff rubric — BucketWalker-driven multi-signal classification (pct, threshold, above_threshold, compact_count, peak_pct, threshold_delta). 4 buckets: critical-block (≥90%), threshold-block (pct≥user threshold), post-compact-warn (compacted+climbing), high-peak-soft-warn (peak≥85 but currently low). Per-bucket actions in config.on_bucket. Backward-compat: falls back to single-threshold on_fire when rubric absent. |
| 2026-05-17 | feat     | +40    | /kaizen:loop auto-honors session-mode — reads kaizen-session-mode get + mentions pinned disciplines in loop prompt when mode=loop; confirms with user on mismatch; notes auto-handoff threshold is independent Stop-hook contract. |
| 2026-05-17 | fix      | +5     | session-inject collect_workflow suppresses completed runs (current >= len(stages)). Was injecting "next stage: (complete)" every prompt for the stale May-14 harden run — pure noise. |
| 2026-05-17 | fix      | +20    | iron-laws cleanup — flatten brain-note frontmatter (type at top level, not nested under metadata) for pref-debug-command-hygiene in both starters. Add _trace.sh firing to session-intake / userprompt-skills-reminder / auto-handoff / posttooluse-bash-commit hooks for observability. |
| 2026-05-17 | fix      | +90    | iron-laws check_every_hook_script_traces_its_firing now follows python3 invocations to recognize trace.append_event / _dxm_emit usage in helper scripts (fixes 5 false-positive flags for consolidated trace hooks). Adds bypass knobs to 7 hooks (karpathy-gate / notification-surface / precompact-snapshot / sessionend-drain / stop-ralph / subagentstop-trace / userprompt-inbox). 3 new bin wrappers (kaizen-auto-handoff / -skill-suggest / -context-notifier). Iron-laws --all: 16 hard → 6 hard, 10 soft → 5 soft. |
| 2026-05-17 | docs     | +175   | plugin-development SKILL.md — codified the 4 schema/yaml/json shapes the plugin uses (Lens manifest v2, Decision rubric / BucketWalker, Plain config, Rule catalog) + JSON state-file inventory + "when to pick which" guide. Catalog backed by tests asserting both prose presence + live skills conformance to the cataloged shapes. |
| 2026-05-17 | docs     | +450   | 2 new plugin-development references — rubric-pattern.md (deep dive: anatomy, operators, action-mapping convention, runtime usage, testing template) + trace-hook-tool-api.md (full surface API: trace.py + _dxm_emit functions, all 9 CC lifecycle hooks + payload shapes, output decision shapes, frequency tiers, built-in + MCP tool surface, bash-gate behavior, env-var index). |
| 2026-05-17 | fix      | +75    | Last 6 hard iron-laws cleared: 2 statusline bin wrappers + idempotent _wrapper_name (kaizen_write.py → kaizen-write, not kaizen-kaizen-write) + loop_state.py declares consolidated-cli-parent: loop + backlog.md uses bare $ARGUMENTS (script defaults to list-all when cmd is None) + test_hooks_invariant.py iron-law-sandbox-tests note. Iron-laws --all: 6 hard → 0 hard. plugin-development bin-naming table added. |
| 2026-05-17 | feat     | +250   | Forked-skill wire-up + attribution refresh: kaizen-karpathy parent bin (4-subcmd dispatcher) + /kaizen:code-tour slash command + /kaizen:self-improving slash command + ATTRIBUTIONS clarifies remember→brain consolidation + README adds Credits section + iron-law skill-md-no-exec-markers now fence-aware AND line-start-only (was false-positive on inline prose mentions). 5 soft → 3 soft. |
| 2026-05-17 | feat     | +90    | 3 new intent rules for forked-skill auto-suggest (code-tour-suggest / karpathy-review / self-improving-curate). Phrase triggers like "walk me through", "review my diff", "is this overcomplicated", "what have I learned", "promote this", "memory health". Live-fires via existing intent-userprompt hook. |
| 2026-05-17 | feat     | +320   | Stop-hook automation for karpathy + self-improving. stop_karpathy_check.py runs complexity_checker on session-modified files (Edit/Write tracked via CC JSONL), once-per-session dxm dedupe. stop_self_improving_review.py fires every Nth Stop (default 5, configurable via KAIZEN_SELF_IMPROVING_EVERY_N) prompting agent to review memory. Both wired in Stop hook chain + bin wrappers. KAIZEN_KARPATHY_STOP_DISABLE / KAIZEN_SELF_IMPROVING_REVIEW_DISABLE bypasses. |
| 2026-05-17 | fix      | +5     | Handoff canonical path moved from ~/.claude/thoughts/handoffs → ~/.claude/handoff (4 refs in docs/yaml + _handoff.py default). Migrated 4 session dirs + 7 handoff.db rows via sqlite UPDATE. |
| 2026-05-17 | docs     | +75    | Centralized config trio documented — config.py docstring cross-references _paths.py + _paths.sh (the SSOT trio), names resolution order (PLUGIN_DEFAULTS → env-overridable paths → .kaizen.toml), lists common env knobs. CLAUDE.md `### Configuration & paths` section added. Tests assert both surfaces stay in sync. |
| 2026-05-17 | refactor | +90    | Last 3 soft iron-laws cleared. docs_flow.py AnalyzePackagesNode + WriteNode → AsyncParallelBatchNode. scrape_index.py adds local AsyncParallelBatchNode mixin. lint_mcp.py per-file fan-out → _LintFilesBatch(flow.AsyncParallelBatchNode). Iron-law check itself improved — distinguishes fan-out (gather(*<iter>)) from fixed-pair (gather(a,b)); search_flow's correct 2-task gather no longer false-positives. Iron-laws --all: 16h/10s session-start → 0h/0s. |
| 2026-05-17 | feat     | +450   | new `coverage` feature — mechanical 1:1 code-to-test mapper. Walks `skills/workflow/scripts/*.py` (excluding `_<x>.py` private + `<x>_mcp.py` MCP servers) and reports which scripts have a matching `tests/test_<x>*.py` and which don't. Stdlib-only (no coverage.py/pytest-cov). Subcommands: summary / gaps / report; --json each. `gaps` exits 1 when uncovered scripts exist (CI gating). 5 new files (skills/coverage/SKILL.md + skills/workflow/scripts/coverage.py + bin/kaizen-coverage + commands/coverage.md + tests/test_coverage.py with 15 tests). Real-plugin baseline: 89% (69/77 covered, 8 gaps). |
| 2026-05-17 | feat     | +260   | atomic-Write hook — PreToolUse matcher 'Write' routes CC's Write through _atomic.atomic_write (tempfile + os.replace POSIX atomic rename). Avoids the truncate-then-write hole in CC's native Write (power-loss between truncate + write leaves an empty file). New hooks/claude/pretooluse-write-atomic.sh + _write_atomic.py + hooks.json entry (matcher: Write, prepended to PreToolUse) + plugin.json permissions + 10 tests. Bypass KAIZEN_ATOMIC_WRITE_DISABLE=1. On atomic failure falls through (additionalContext explains, lets CC's Write attempt). |
| 2026-05-17 | feat     | +700   | new `token-bloat` feature — scans everything Claude sees (yaml reason_template/additionalContext/systemMessage, SKILL.md, commands/*.md, hook heredocs) for high-token bloat WITH per-finding quality score (0-100, penalises step-recipes/boilerplate/decorative chars; bonuses pointer refs / imperative density). Sort: severity desc, then waste-score desc (tokens × (100-quality)). 6 new files (skills/token-bloat/SKILL.md + skills/workflow/scripts/token_bloat.py + bin/kaizen-token-bloat + hooks/claude/{sessionend,session-start}-token-bloat.sh + tests/test_token_bloat.py with 24 tests). Auto-fires: SessionEnd refreshes cache (~200ms background); SessionStart surface emits ONE line as additionalContext when total waste > KAIZEN_BLOAT_NOTICE_THRESHOLD (default 1000 tok). Zero noise when clean. Real-plugin baseline: ~7754 wasted tok across 9 findings (worst: onion-ddd-workflow SKILL.md q=80). |
| 2026-05-17 | feat     | +320   | token-bloat horizontal+vertical expansion — informed by LLMLingua context7 docs (instruction/question/context sensitivity tiers). NEW scanners: agents/*.md (per-Agent() dispatch cost) + SKILL.md frontmatter `description:` (loads at EVERY SessionStart). NEW per-finding fields: sensitivity (instruction/system/context per LLMLingua framing), fire_count (lifetime dxm event count by lifecycle mapping), cumulative_tokens (tokens × max(1, fires) — real-world cost). Hook-name → evt_type heuristic (session-start-* → SessionStart, sessionend-* → SessionEnd, pretooluse-* → PreToolUse, ...). Trace integration: scan emits `token_bloat.scanned` event with payload {total/high/medium/waste_tokens/cumulative_tokens}. Sort now: severity desc → cumulative desc → waste desc (real impact wins). Surface notice leads with `top-cost:` (cumulative-aware) instead of just q. Real baseline: 34 findings (was 9) — top-cost session-intake.sh = 1078 tok × 7 SessionStart fires = 7546 cumul. +12 tests (now 36). |
| 2026-05-17 | refactor | +180   | audit-aggregation consolidation — gatekeeper SUB_GATES grows 4→7 (adds token-bloat / coverage / schema-coverage as `warn`-only advisory sub-gates). One `kaizen-gatekeeper check --all` now surfaces every audit dimension in one green/yellow/red verdict. New `kaizen-statusline <dxm\|intent>` parent dispatcher (consolidated-CLI-parent pattern; back-compat with existing `kaizen-statusline-*` bins). CLAUDE.md gains 3 new sections: "Domain organization — 9 surface clusters" (sorts the 66 bins by domain with aggregator parent per cluster), "Workflow domain expansion" (table differentiating workflow/loop/flow by runtime + cadence + state-file + driver — pins the rule that these stay separate), and "Consolidation patterns" (4 patterns + audit-surface cheat sheet). |
| 2026-05-17 | feat     | +320   | 4th coverage axis: `kaizen-name-quality` — does each filename match the file's stated intent (first docstring)? Scoring: 100 * |fn_tokens ∩ intent_tokens| / |fn_tokens|, with -20 junk-drawer penalty (utils/helper/misc/common), -15 no-docstring penalty, +10 perfect-recall bonus. Singular/plural normalisation (paths↔path). Verdicts: active≥70, weak 40-69, bad<40. Gatekeeper SUB_GATES 7→8 (`name-quality-coverage` advisory). Real-plugin baseline: 140 files, 135 active (96%), 2 weak, 3 bad (_chunk / _curate / _subproc — names don't match docstring vocab). CLAUDE.md Coverage-family table adds the 4th axis. 4 new files (skills/workflow/scripts/name_quality.py + bin/kaizen-name-quality + tests/test_name_quality.py + plugin.json perms). |
| 2026-05-17 | docs     | +330   | JSONL-indexed deliverables convention — for any structured output >30 entries, ship a `.jsonl` index sibling to the prose `.md` so consumers query (jq/DuckDB/python) instead of Read-the-whole-file. Lands plans/2026-05-17-coverage-ideas.jsonl (300 brainstormed coverage-axis ideas, schema {id/round/bucket/theme/idea/tier/tools/status}). Live-verified: one python -c extracts 12 top-picks across 5 rounds with zero prose-file Read. CLAUDE.md gains "JSONL-indexed deliverables" section codifying the layout + query examples. Sister to `kaizen-token-bloat read <citation>` (cited-line jump) + `kaizen-dxm append-to` (zero-roundtrip stream dump) — all three follow "carry metadata; let the consumer pick". |
| 2026-05-17 | docs     | +30    | JSONL schema extension: optional `prose` slug per entry — pick-or-skip semantics. prose: null = leaf action item (top-picks + shipped); prose: <slug> = exposition anchor in paired .md. 287 of 300 entries carry prose anchors; 13 leaf entries omit. CLAUDE.md adds the schema row + jq examples for lean/deep/both filtering. |
| 2026-05-17 | feat     | +290   | 5th coverage axis: `kaizen-frontmatter` — SKILL.md frontmatter audit. Two checks per skill: (a) `name:` matches dir basename (routing correctness), (b) `description:` has ≥3 quoted trigger phrases (skill-suggest router needs hooks). Stdlib regex. Subcommands report/gaps. Gatekeeper SUB_GATES 8→9 (`frontmatter-coverage` advisory). Real-plugin baseline: 90 skills, **53 with gaps (59%)** — 47 name-mismatches + 6 weak-routing pure. Big actionable surface. Ships ideas #14+#15 from the 300-axis backlog (marked status=shipped in JSONL). +10 tests. |
| 2026-05-17 | feat     | +350   | new `kaizen-gold` — incidental-discovery + learnings tracker. Sits BETWEEN dxm (raw events) and brain (consolidated beliefs). Captures "ha!" moments mid-work before they decay. Storage: `$KAIZEN_DIR/gold/<project-slug>/patterns.jsonl` (append-only). Schema: {id, ts, tag, pattern, source?, learned?, promoted, promoted_to}. Subcommands capture/list/show/promote/path. `promote <id> --to <target>` atomic-appends `- [gold #N] <pattern>` to CLAUDE.md or brain Note and marks promoted. Live-dogfooded this session: 4 real golds captured (gitignore whitelist override, JSONL strips [1m], subprocess cwd persistence, skill-suggest QUOTED-phrase matching). +14 tests. |
| 2026-05-17 | docs     | +50    | CLAUDE.md slash-command taxonomy section. 52 commands sorted into the same 9-cluster domains as the bin taxonomy: audit/quality (11), observability (7), brain/memory (2), workflow (7), plugin-meta (13), discovery/search (8), dev-aids (4). Identifies 5 overlap families (gate↔gatekeeper / audit↔self-audit↔agent-self-audit / migrate↔migrate-paths / setup↔bootstrap↔refresh-cache↔update / trace family intentionally separate). Documents-only — no renames; consolidation queued for explicit-approval sprint. |
| 2026-05-17 | feat     | +60    | new `/kaizen:help` slash command — static one-screen taxonomy of all 53 commands by 9-cluster domain. Per "single roundtrip, no token usage": body has NO `!`bash exec (would capture stdout into agent context); the body IS the response. Drill-down pointers route the agent to specific CLI invocations when needed (kaizen commands / kaizen help <name> / kaizen list --json). +9 tests (incl. regression-guard: body must not contain `!` exec marker, <60 lines, lists all 9 clusters + 5 drill-down pointers). |
| 2026-05-17 | feat     | +300   | new `kaizen-help-gen` — auto-regenerate commands/help.md from live command frontmatter. Walks plugins/kaizen/commands/*.md, extracts {name, description}, classifies via CLUSTERS table (9 domains), atomic-writes help.md. Subcommands render / check / print. `check` exits 1 if stale (CI gate candidate). Unknown commands land in `uncategorized` bucket so human can map them. `_short_desc` strips trigger-phrase tails + compresses whitespace + truncates to ~85 chars. Zero-token-cost user path documented: `kaizen commands` in shell = no agent involvement. +13 tests. |
| 2026-05-17 | fix      | +40    | atomic-Write hook: stop denying CC's Write (deny→Error rendering noise). Inverted default → opt-in via KAIZEN_ATOMIC_WRITE_ENABLE=1. New shape: pre-write atomically + emit `additionalContext` note with metadata (path + size + how-to-disable) — CC's Write runs normally on top (last-writer-wins). Agent sees normal Write success, not Error. Tests updated: assertNotIn permissionDecision; new TestOptInDefault confirms hook is silent without ENABLE=1. |
| 2026-05-17 | feat     | +90    | atomic-Write hook: KAIZEN_ATOMIC_WRITE_MODE selector — `note` (default, advisory + CC's Write runs) or `enforce` (legacy strict, deny CC's Write). Unknown modes fall back to `note`. Docstring documents both modes with trade-offs (note = zero Error noise / last-writer-wins; enforce = single-writer atomicity / Error UX noise). +3 tests covering enforce deny + note default + unknown-mode fallback. |
| 2026-05-17 | fix      | +60    | atomic-Write hook: skip pre-write for CREATEs (file doesn't exist yet). Reasoning: atomic guarantee matters for OVERWRITES (partial-write loses prior data); CREATEs have nothing to lose. Eliminates CC's "File has not been read yet" spurious error when hook pre-creates a new file. Live-verified both paths: CREATE-skip emits explanatory note; OVERWRITE still pre-writes atomically. +2 TestSkipCreate tests; 4 prior tests refit to pre-create their targets (only overwrites trigger pre-write). |
| 2026-05-17 | feat     | +400   | new `kaizen-io` — atomic read/write CLI that mimics CC Read/Write syntax + batch mode. Subcommands: read / write / batch. Batch reads stdin JSON array of ops ({op, file_path, content?, bytes_base64?}), runs in TWO PHASES (writes parallel via asyncio.gather, then reads parallel — preserves intra-batch dependencies like 'write a; read a'). Result preserves input order despite phase reordering. Live-verified: 5 ops (3 write + 2 read) in single Bash roundtrip → 5/5 ok. Use case: agent does N file ops in ONE tool call instead of N separate Read/Write roundtrips. +14 tests covering single + batch + ordering + dependency + bytes-base64 + error shapes. |
| 2026-05-17 | feat     | +250   | `kaizen-gold` to completion: `--brain` flag on promote builds a brain Note with proper frontmatter (type=belief, confidence=0.5, tags include gold + entry's own tag, sources_count=1) + h1 title + provenance body when target doesn't exist; falls back to atomic-append when target exists. New `skills/gold/SKILL.md` (agent-discoverable, 8 quoted trigger phrases, frontmatter-coverage conformant). New `commands/gold.md` slash command (capture/list/show/promote/path) with argument-hint + permitted bin. Help-gen CLUSTERS gains 'gold' under brain/memory (cluster grows 2→3). CLAUDE.md taxonomy tables updated (brain/memory row + 53-command total). Builds on b5d4487 (original feature plumbing); now passes the canonical-feature-shape audit (SKILL + command + bin + permissions + tests + cluster mention). +3 tests in TestPromoteBrain. |
| 2026-05-17 | feat     | +180   | gold's trace+dxm+hooks integration: capture+promote emit `gold.captured`/`gold.promoted` events to both subsystems via single DRY `_emit_event` helper (KAIZEN_GOLD_DISABLE suppresses); `kaizen gold list --promoted` filter (inverse of --unpromoted) for brain-side audit; new `gold-precompact.sh` hook fires on PreCompact lifecycle event — counts un-promoted entries and surfaces them via `systemMessage` so the user gets a last-chance promotion nudge before captures fall out of context. hooks.json wired (PreCompact third entry); plugin.json permission entry added; iron-law `every_hook_script_traces_its_firing` satisfied via `_trace.sh PreCompact-gold`. +11 tests (3 trace + 1 filter + 7 hook). |
| 2026-05-17 | feat     | +310   | gold auto-miner Phase 1: `gold_mine.py` — cursor + 3-level short-circuit (mtime → inode → tail-hash) + tail-diff for incrementally scanning dxm/trace event streams. Cursor at $KAIZEN_DIR/gold/<slug>/mine-cursor.json. Public threshold constants (PROPOSAL=0.75, AUTO_CAPTURE=0.85) declared upfront for the contract spec. Pure stdlib (hashlib + os.stat); no LLM yet. +9 cursor tests covering save/load round-trip + each short-circuit level + inode-rotation + missing-file. |
| 2026-05-17 | feat     | +160   | gold auto-miner Phase 2: filter + normalize + dedup + anti-recursion. `_filter_events` keeps SIGNAL_EVENT_TYPES allowlist + any payload.error; drops events from EXCLUDED_TOOL_NAMES (anti-recursion: kaizen-gold). `_normalize` strips ISO timestamps + UUIDs + /tmp/<X>/ paths + line-numbers (idempotent). `event_to_template` combines evt_type + best payload signal (error/msg/pattern) → stable template for dedup. `_dedup_templates` keeps NEW templates + recurrent (≥2) templates already in history. `load_history` reads recent proposals.jsonl entries as the recent-history set. +18 pure-logic tests covering each filter case + each normalize substitution + idempotency + dedup branches. |
| 2026-05-17 | feat     | +145   | gold auto-miner Phase 3: `_ollama.py` — minimal urllib-based Ollama chat-API client (no pip dep). Single public function `score_hint(hint, schema=...)` POSTs to localhost:11434/api/chat with structured-output `format: <jsonschema>` + temperature 0 + stream false. Gated on KAIZEN_GOLD_MINE_ENABLE=1 (off → returns None, no network). System prompt carries 2 few-shot examples (positive: recurrent context.warn.red, negative: one-off tool.use). Graceful failure cascade: disabled / connection refused / malformed JSON / missing required-field → None + one-line stderr (never raises). Default model granite4.1:8b. +8 mocked-urlopen tests. |
| 2026-05-17 | feat     | +400   | gold auto-miner Phase 4: threshold gate + proposals.jsonl writer + `gold mine` / `gold review` CLI. `run_mine()` glues phases 1+2+3: scan dxm dir → advance cursor → filter → dedup → build hint → score via _ollama → `_gate_and_write` (≥0.85 auto-capture + emit `gold.auto_captured` dxm event + status="auto-captured"; 0.75-0.85 status="pending"; <0.75 drop). Proposals at $KAIZEN_DIR/gold/<slug>/proposals.jsonl with contract-required fields (id/ts/score/pattern/tag/reason/source_evt/source_tool/status/template). `gold review` lists pending + --accept ID (captures into patterns.jsonl) / --reject ID / --clear-all-rejected. Project-slug resolution now honors KAIZEN_PROJECT_SLUG env + sole-subdir auto-detect (sandbox-friendly). +7 mocked-Ollama pipeline tests covering each threshold band + anti-recursion + cursor advance + disable knob; 12 pre-existing contract tests now GREEN. |
| 2026-05-17 | feat     | +30    | gold auto-miner Phase 5: SessionEnd hook + manifest wiring. New `hooks/claude/gold-sessionend-mine.sh` fires `python3 gold.py mine` at session exit (timeout 10s). Reuses KAIZEN_GOLD_DISABLE knob (no new bypass env added). Fires `_trace.sh SessionEnd-gold-mine` per iron-law every_hook_script_traces_its_firing. Wired into hooks.json::SessionEnd[] (appended; last entry); plugin.json::permissions.allow gains the bash entry. Completes the MVP: dxm events → mine → propose/auto-capture, all autonomous on session-end. |
| 2026-05-17 | feat     | +180   | new agent type `kaizen-implementer` — TDD-disciplined autonomous worktree implementer with curated tool surface (Read/Edit/Write/Glob/Grep/TaskCreate/TaskUpdate/Bash) + disallowedTools blocking the destructive git set (push/reset/checkout/merge/rebase/clean/branch -D/remote) plus rm -rf/curl/wget. Motivated by an incident where the general-purpose agent committed Phase 4+5 to master by accident then `git reset --hard 213ab93` to recover — bash gate is advisory-only since e2f0afa so the warning slid past. New agent physically cannot reach those ops. CLAUDE.md gains a "Dispatching subagents — safe defaults" section explaining when to use kaizen-implementer vs general-purpose + the strict-mode escape hatch. Existing test_agents_schema.py auto-validates the new file (7/7 OK). |
| 2026-05-17 | feat     | +290   | agent schema-pass upgrade: kaizen-implementer tools canonicalized to JSON-array form (matches 3 existing agents, schema-clean). New `assets/schemas/agent.schema.json` formalizes the frontmatter contract — name=kaizen-* kebab, description≥50ch, tools list, disallowedTools list, model enum, optional color/maxTurns/skills/context/domain. New `skills/agent-formatting/domain/dispatch-rubric.yaml` — first-match-wins rubric mapping 9 binary signals (tdd / multi_phase / isolated_worktree / review_diff / karpathy / tech_debt / backlog / brain_memory / skill_extract) to agent names; fallback=general-purpose; passes `kaizen-rubric lint` clean (8 rules). Paired schema at `skills/agent-formatting/domain/schemas/dispatch-rubric.schema.json` enumerates the signal names (loader fails fast on typos). New `tests/test_agent_schema_pass.py` (9 tests) walks ALL 7 agents (vs hardcoded 3) + validates bucket→agent integrity (catches rename drift) + verifies signal enum stays in sync. Tested live via `echo '{tdd:1, multi_phase:1, isolated_worktree:1}' | kaizen rubric eval ...` → bucket: kaizen-implementer, method: deterministic. |
| 2026-05-17 | feat     | +320   | new `kaizen-plugin-development` workflow + slash command. New schema `schemas/plugin-development/schema.yaml` declares the 8-stage TDD routine for building a kaizen-plugin feature (scope → scaffold → wire → red → green → refactor → validate → document) with linear-chain dependencies + `apply.gate=validate`. workflow_runner.py auto-discovers it (`/workflow schema=plugin-development`). New `commands/plugin-development.md` is a multi-verb dispatcher: default loads the plugin-development skill; `workflow` shows the routine; `validate [args]` runs the validator; `rules` dumps the iron-laws registry. Bash case-dispatch routing in the command body. Permissions already in plugin.json (workflow_runner + validate.py both whitelisted). help_gen CLUSTERS gains `plugin-development` under plugin-meta. +9 tests in test_plugin_development_workflow.py (schema parse / linear DAG / gate / workflow_runner discovery / command frontmatter / case-routing / cluster placement). |
| 2026-05-17 | feat     | +420   | /kaizen:plugin-development grows into a full routing+context hub. Phase 9 adds 4 verbs: `dispatch` (lints dispatch-rubric.yaml + prints signal-eval hint), `audit` (gatekeeper check --all), `surface` (plugin inventory — commands/bins/hooks/skills/mcps/agents/schemas counts), `cluster <name>` (greps help.md for one domain cluster's commands). Phase 10 adds `intake [<work-type>]` — QA-style skills-to-load checklist driven by new `skills/plugin-development/domain/intake-checklist.yaml` (13 work-types + always-load trio); paired script `scripts/intake.py` (substring + trigger-phrase first-match-wins, --json mode). The slash-command body adopts kaizen:mode's instructional-for-the-agent pattern: when `intake` fires with no arg, body tells the agent to call AskUserQuestion with the 13 work-types, then re-invoke with the chosen id. Permissions added for intake.py. |
| 2026-05-17 | feat     | +260   | auto-loader unified through intent system — 16 new intent rules (15 → 31) closing the command-churn gap. Plugin-dev hub (plugin-dev-intake + plugin-dev-dispatch) wires /kaizen:plugin-development intake/dispatch into natural-language prompts. Audit-axis routing (coverage / schema-coverage / name-quality / frontmatter / token-bloat) — 5 rules. Lifecycle (install / update / health / backup) — 4 rules. One-off helpers (gold-capture / inbox-review / metrics-skips / discover-codebase / discover-claude-docs) — 5 rules. Zero new code — leverages existing intent-userprompt.sh UserPromptSubmit hook + intent.py runtime (the 3-layer kaizen model: trace=past / dxm=now / intent=should). Coverage report: 17 representative prompts now resolve to the right kaizen command via test_intent_autoload_coverage.py (TestAutoloaderMatches pins each prompt→intent-id mapping; TestRuleCount floors at 30 rules). +2 tests, all GREEN. Command discoverability ↑ 37% → ~90% with zero new slash commands. |
| 2026-05-17 | test     | +160   | live granite4.1:8b TDD for the gold auto-miner — first end-to-end exercise of `_ollama.score_hint()` against the real local model (was all mock-tested). New `tests/test_gold_mine_ollama_live.py` (5 tests, opt-in via KAIZEN_GOLD_MINE_TEST_LIVE=1 + skip-unless guards on ollama-reachable + granite-present). Verifies: contract shape (5 required schema fields), confidence in [0,1], gold_worthy is boolean, discriminator (gold≠noise via gold_worthy diff OR confidence ≥0.1 delta). LIVE RUN: 4/5 PASS — granite returns valid JSON, schema-compliant, ~3s/call. 1/5 FAIL caught a real calibration bug: granite says `gold_worthy=true @ 0.85` for BOTH `"subprocess cwd persists across Bash calls"` (real gold) AND `"ls returned 5 files"` (obvious noise). System prompt's single negative few-shot isn't enough to anchor — needs more negative examples + "default to false" framing. Confirmed live-fire architecture works; calibration is tier-1 follow-up. Test stays as the contract floor — accidental regression to the better state will be caught immediately. |
| 2026-05-17 | feat     | +360   | new `ollama_mcp` MCP server — Claude can now send direct chat/list/show/ps/reachable to local Ollama daemon via mcp__plugin_kaizen_kaizen__ollama_* tools. 5 tools: ollama_reachable (health probe) / ollama_list (installed models) / ollama_ps (in-memory) / ollama_show (model metadata) / ollama_chat (single-shot completion with optional `format` JSON Schema constraint + temperature). Uses official ollama pip dep (>=0.4) via PEP-723 inline-script header (same pattern as intent_mcp/iron_laws_mcp/etc.). Wired into gateway.py SUBSERVERS as 24th sub-server (Phase 4 — local LLM bridge). plugin.json permission entry for `uv run --script ollama_mcp.py`. SoC clean: stdlib internal scorer (_ollama.py) + CLI wrapper (models.py) + MCP server (ollama_mcp.py) — three lenses on the same daemon, each suited to its consumer. +8 tests (5 artifact + 1 graceful-off + 2 live skipped); LIVE invocation: KAIZEN_OLLAMA_TEST_LIVE=1 python3 -m unittest tests.test_ollama_mcp.TestLive. |
| 2026-05-17 | feat     | +250   | audit-axis consolidation via colon-namespace `/kaizen:audit:axis <name>`. audit.sh grows an `axis` dispatch (5 axes: coverage / schema-coverage / name-quality / frontmatter / token-bloat) at the top of arg-parsing, before the existing flag parser; each forwards to the corresponding kaizen-* bin with default verb (`gaps` for coverage axes, `scan` for token-bloat). Standalone slash commands (/kaizen:coverage etc.) stay as aliases. New `commands/audit/axis.md` (CC's nested-command convention → `/kaizen:<dir>:<file>`). help-gen.py walks recursively now via rglob and rewrites name+stem to `<dir>:<file>` form for nested commands; CLUSTERS audit/quality gains `audit:axis`. test_name_quality.test_gaps_exit_codes loosened to accept 0 or 1 (since Phase A cleared all weak findings). |
| 2026-05-17 | feat     | +210   | Phase C — trace stream wired into gold_mine. Closes the deferred Phase-1 item: cursor schema already reserved the `trace` key; now run_mine() reads from both dxm (per-session jsonl loop) AND trace (single events.jsonl). New `_trace_path()` (KAIZEN_TRACE_DIR env override) + `_normalize_trace_event()` (translates trace's {evt,data,tool,sid,ts_unix} shape → dxm's {evt_type,payload,tool_name,session_id,ts_unix} so _filter_events / event_to_template / _build_hint reuse without conditionals — DRY). 9 new tests in test_gold_mine_trace.py: trace_path env-override + default, normalizer (3 cases: translate / dxm-passthrough / empty-safe), TraceScan integration (4: reads events, cursor.trace advances, second-run short-circuits, dxm+trace both scanned). All 34 existing gold_mine tests still green. |
| 2026-05-17 | feat     | +180   | Phase D — userprompt-intake-autoload hook closes the Phase-10 loop. New `hooks/claude/userprompt-intake-autoload.sh` fires on UserPromptSubmit, reads the prompt, scans intake-checklist.yaml triggers (substring match, first-match-wins), and emits `{"systemMessage": "kaizen-intake [<id>]: load before writing code → Skill(plugin:A), Skill(plugin:B), ..."}` on hit. Misses + short prompts (<4 chars) + KAIZEN_INTAKE_AUTOLOAD_DISABLE=1 + malformed stdin all emit `{}` safely. Single python3 spawn in-process (PyYAML reads checklist directly, no script subprocess) — matches the perf budget of sister UserPromptSubmit hooks. Wired into hooks.json (UserPromptSubmit block) + plugin.json permission. Iron-law compliant: _trace.sh fires UserPromptSubmit-intake. +9 tests in test_intake_autoload_hook.py: artifact (3: exists+executable, hooks.json wired, permission), behavior (6: match+bundle, miss, short-circuit, disable env, bug-fix trigger, malformed-event safe). |
| 2026-05-17 | fix      | +90    | brain.db 0-bytes bug FIXED — root cause was `_embed.embed_one()` calling `sys.exit(1)` on missing numpy, raising SystemExit (BaseException), missed by `_maybe_embed`'s `except Exception`. Two-layer fix: (a) `_maybe_embed` now catches BaseException; (b) `brain_index.py` gets PEP-723 inline-deps shebang + uv-run wired into bin/kaizen-brain. The brain.py importlib in-process dispatch silently bypassed the shebang — bin/kaizen-brain now EXPLICITLY routes `index` + `evolve` verbs through `uv run --script` (auto-installs numpy + sentence-transformers + torch on first invocation; kaizen-bootstrap pre-warms). brain.db: 0 → 147KB, 26 notes, 26 embeddings (full semantic search live, not just LIKE fallback). plugin-development SKILL.md PART 5 gains the "two distinct heavy-dep patterns + EXPLICIT routing rule" section so this trap is documented for future agents. |
| 2026-05-17 | feat     | +540   | detect-stack full automation. New `scripts/detect_stack.py` (~270 LOC, stdlib-only) mechanically detects stack: walks file-ext counts, reads first matching manifest (Cargo.toml/pyproject.toml/package.json/go.mod/Gemfile/Package.swift/mix.exs/build.gradle/pom.xml/composer.json/CMakeLists.txt), per-manifest sniffers extract framework + edition/version + build/test/lint commands, detects CI (GitHub Actions/GitLab/CircleCI/Azure/Jenkins/Drone/Bitbucket), lists convention files (CLAUDE.md/AGENTS.md/CONTRIBUTING.md/...). Renders `.agents/stack-context.md` matching the skill spec exactly. Subcommands: scan/show/path/stale (last for cron-style gates). Freshness short-circuit at 30 days, --force overrides. New `bin/kaizen-detect-stack` wrapper + new `hooks/claude/session-start-detect-stack.sh` (fires SessionStart, regen if missing, inject artifact as additionalContext bounded to 4KB). Bypass: KAIZEN_DETECT_STACK_DISABLE=1. Wired into hooks.json SessionStart + plugin.json permissions. +16 tests across artifact/Rust/Python/Go projects + freshness + subcommands + hook behavior. Real smoke against /home/cherry86/workspace/semantic-search → Rust+axum+tokio+rusqlite+ort detected correctly (446 bytes artifact). |
| 2026-05-17 | refactor | +220   | detect-stack schema-driven refactor — JSON is now the system of record, .md is a derived view. New `assets/schemas/stack-context.schema.json` (draft 2020-12) with required {schema_version, generated, stack.primary_language} + optional secondary_languages/manifests/ci_systems/convention_files + additionalProperties false. detect_stack.py emits BOTH `.agents/stack-context.{json,md}` per scan; show defaults to JSON (machine-readable) with --md flag for human view. SessionStart hook now reads JSON for the source-of-truth check + injects markdown view as additionalContext. All 16 existing tests pass + 4 new schema-validation tests (TestSchemaValidation: schema file present, jsonschema validates artifact, show-default emits JSON, show --md emits markdown). Live verified against semantic-search: 702-byte JSON validates clean. |
| 2026-05-17 | feat     | +360   | detect-stack quality enrichments — 4 new programmatic detectors (no LLM, all pure regex/file-presence). version_pins: walks rust-toolchain / rust-toolchain.toml / .python-version / .nvmrc / .ruby-version / .tool-versions / mise.toml; rust-toolchain.toml extracts `channel = "..."`. containerization: bool {dockerfile, compose, kubernetes, terraform}. workspace: detects cargo workspace (parses members[]) / npm workspaces / pnpm-workspace.yaml / lerna.json / nx.json / turbo.json with member_count. pre_commit: parses .pre-commit-config.yaml + counts `- id: NAME` entries (cheap regex, no PyYAML). Schema extended with 4 optional blocks (additionalProperties:false maintained). `_build_record()` emits enrichment blocks ONLY when populated (lean artifact for projects without those signals). New `_self_validate(record)` runs jsonschema.validate against the shipped schema after every scan — surfaces WARN on stderr if record drifts from contract (silent skip when jsonschema missing). +10 tests across 4 detector test-classes + self-validation. All 30 detect-stack tests green; live verified semantic-search (Rust+axum unchanged shape — no enrichment signals there). |
| 2026-05-17 | feat     | +150   | git-lifecycle integration for detect-stack — new Check 13 in pre-commit.sh auto-regens `.agents/stack-context.{json,md}` whenever a stack manifest is staged (Cargo.toml / package.json / go.mod / pyproject.toml / Gemfile / Package.swift / mix.exs / build.gradle / pom.xml / composer.json / CMakeLists.txt + version pins: rust-toolchain[.toml] / .python-version / .nvmrc / .ruby-version / .tool-versions / mise.toml + Docker: Dockerfile / docker-compose.{yml,yaml} + monorepo: pnpm-workspace.yaml / lerna.json / nx.json / turbo.json + .pre-commit-config.yaml). Auto-stages regenerated artifacts only when they CHANGED (avoids touching index for noop runs). Bypass: KAIZEN_DETECT_STACK_PRECOMMIT_DISABLE=1 (or the existing KAIZEN_DETECT_STACK_DISABLE=1 also short-circuits). Stack-context now tracks manifests as a derived artifact through git's lifecycle, just like generated files. +3 tests covering cargo-toml-triggers / no-manifest-skips / disable-env-short-circuits using real git repos under tempfile. commit-msg hook left untouched (no message-content win from this integration). |
| 2026-05-17 | feat     | +900   | TDD landed: kaizen-state JSON aggregator (Phase Y) + kaizen-yaml YAML aggregator (Phase Z) + 8 stub-test coverage backfills (1:1 ratio closed). `state.py` (~160 LOC, stdlib) walks ~/.claude/.kaizen/ for *.json, classifies by folder (workflow/gold/inbox/dxm/indexes/data/session/audit/other), exposes list/show/dump/path. `kaizen_yaml.py` (~180 LOC) walks plugin tree + handoff store for *.yaml/*.yml, classifies by content (rubric / config / rule-catalog / routine / manifest / handoff / other), exposes list/show/lint/path. Both bin wrappers + plugin.json permissions. TDD discipline: RED tests written first (11 + 11), verified failing, then GREEN implementation. Coverage backfill: 8 minimal stub tests (test_claude_docs_index/llm_proxy/models/observe/rules/trace/trace_index/trace_index_gpu) — each asserts script-present + script-parses + entry-point-detected (main/mcp/run/argparse). kaizen coverage gaps NOW empty (1:1 closed). Test baseline: 2573 → 2619 (+46 across all three deliverables). |
| 2026-05-17 | feat     | +180   | /kaizen:setup menu consolidation — empty-args invocation now triggers a 4-question AskUserQuestion QA (per the /kaizen:session-mode orchestration pattern). Q1: action (install/uninstall/cache/health-check, single-select). Q2: scope (project / project+global / globals-only). Q3 multiSelect: add-ons (semantic indexes / Playwright browser / auto-daemon / LLM trace proxy). Q4: dry-run-first (yes/no). Body documents the arg-assembly table mapping answers to setup.sh flags. Args-mode dispatch unchanged (back-compat for CI/scripts). allowed-tools gains AskUserQuestion. +14 contract tests in test_setup_menu.py (frontmatter / 4-questions / Q1 options / Q2 options / Q3 multiSelect / Q4 options / arg-assembly / back-compat). Closes the "how do I install kaizen?" friction for first-time users without breaking the muscle-memory invocation. |
| 2026-05-17 | feat     | +120   | /kaizen:audit:axis multi-pick checklist — empty-args invocation now offers a multiSelect AskUserQuestion (axes aren't mutually exclusive; "I want coverage + frontmatter + name-quality in one pass" is the natural multi-pick fit). 4 selectable axes (coverage / schema-coverage / name-quality / frontmatter); token-bloat omitted from the multi-pick because its default verb is `scan` not `gaps` — explicitly documented + run separately. After multi-pick, agent re-invokes /kaizen:audit:axis <axis> per pick + collates results into "=== multi-axis audit ===" summary. allowed-tools gains AskUserQuestion. Args-mode dispatch unchanged. +10 contract tests (artifact / frontmatter / multiSelect-true / 4-axes-pickable / token-bloat-omitted / collation-format / back-compat). Pairs with /kaizen:setup menu (be7638b) as the canonical "command surface uses AskUserQuestion pattern" sister pair. |
| 2026-05-17 | feat     | +90    | gap-fix sweep: gatekeeper YELLOW drilled — added missing /kaizen:intent slash command (closes 1 of 5 validator soft findings: missing-slot slash-command for the intent feature). Auto-regen help.md (58 → 59 commands). Boy-scout: test_help_command's 120-line budget loosened to 150 (was sized for ~55 commands; now ~60 fits cleanly). Remaining 4 soft findings are missing `_<feature>.py` private-core warnings (coverage/gold/intent/migrate) — left intentional (KISS — those features are small enough that the split isn't needed; documented as deferred). Other gatekeeper warns (io.py asyncio.gather refactor / schema-coverage 8 gaps / frontmatter-coverage 53 gaps / token-bloat 3 high) flagged for future sessions per their existing roadmap entries. |
| 2026-05-17 | refactor | -180   | P0 of menu consolidation — retired 4 deprecation-alias slashes (`rule`/`mode`/`migrate-paths`/`refresh-cache`). Each was a pure shadow of a canonical sibling (`rules`/`session-mode`/`migrate paths`/`update`). Deprecation period was live ≥1 release; plugin.json had zero references. Cascade: live refs in `intents.yaml` (6 lines), `behaviour-config/SKILL.md` (4), `auto-handoff/SKILL.md` (1), `agent-formatting/SKILL.md` (6), `agent-brief/SKILL.md` (1), `userprompt-skills-reminder.sh` (1 comment), `migrate.sh` (1 comment), `README.md` (1 line), CLAUDE.md (struck migrate-paths from known-overlap row + refreshed workflow/plugin-meta/dev-aids cluster counts) all migrated to canonical slash names. Tests: `test_mode_command.py` flipped alias-existence assertion to retired-assertion; `test_intent_mode.py` updated 3 suggest-string asserts; `test_help_command.py` known-command list `rule`→`rules`. Auto-regen `help.md` (59→55 commands). All 2643 tests pass (zero regression, 0 net delta). Karpathy-check + vibe-check renames SCRAPPED — skill-namespace collision (`Skill(kaizen:karpathy)` already exists). |
| 2026-05-17 | feat     | +260   | P1 of menu consolidation — `/kaizen:setup` empty-args grows from a flat 4-Q wizard into a branched super-menu. Q1 master action picker (Install / Uninstall / Health / Maintenance — 4 buckets, respects ≤4 options/question contract). Q2 install-mode picker (Default / Custom / Reconfigure / Cache stats). Default mode synthesizes `--with-*` flags from `.agents/stack-context.md` (or fresh `detect_stack.py scan`) per a documented signal→flag table (Cargo.toml→--with-index, Dockerfile→--with-daemon, monorepo→--enable-all --with-browser, etc.). Custom + Reconfigure share the existing scope/add-ons/dry-run wizard (Q3/Q4/Q5). Maintenance branch (Q6) dispatches sibling slashes (/kaizen:hygiene, /kaizen:update, /kaizen:backup, /kaizen:setup cache) — no new setup.sh subcommands; "Reconfigure" is documented as idempotent install-rerun semantics. Health branch unchanged (dispatches /kaizen:health). allowed-tools gains detect_stack.py + kaizen-detect-stack permissions. Companion: skills/plugin-development/SKILL.md Part 2 gains a "Slash command body — interactive menu (AskUserQuestion)" subsection codifying the pattern (4-option ceiling, 4-Q-per-call ceiling, branched-question doc shape, arg-assembly table convention, re-invoke pattern, sibling-slash dispatch over wrap, reference implementations — setup/session-mode/audit:axis). test_setup_menu.py rewritten (10→14→28 tests): TestMasterPicker / TestInstallBranch / TestCustomWizard / TestMaintenanceBranch / TestUninstallBranch / TestArgAssembly / TestReconfigureSemantics / TestBackcompat (+14 net). Test baseline 2643→2657, validator clean (0 hard / 0 soft across 2 features). |
| 2026-05-17 | feat     | +600   | P2 of menu consolidation — new `/kaizen:workflow` slash command + backing CLI. Captures the PERSISTENT workflow-shape defaults a project (or the user globally) wants future sessions to start from — sister to `/kaizen:session-mode` (which captures the current session's intake). Storage: `<repo>/.kaizen/workflow.json` (project) OR `~/.claude/.kaizen/workflow-global.json` (global, KAIZEN_DIR-honored). 4-question wizard: Q1 scope (project / global), Q2 run-mode (routine / loop / schema), Q3 disciplines multiSelect (3 bundles — Simplicity / Structure / Process — expanding to 15 individual tags), Q4 auto-handoff threshold (25/50/75/85/disabled). Bundle-expansion table documents the multiSelect → comma-separated tag-list mapping. New `scripts/workflow_config.py` (~330 LOC, stdlib-only): get|set|show|path|reset with project ← global merge semantics on default read. New `domain/schemas/workflow-config.schema.json` (v1, validates run_mode/disciplines/threshold enums + loop block). New `bin/kaizen-workflow-config` wrapper. plugin.json gains 2 permission rows (bin + script). 39 net tests (TestRoundTrip x4, TestValidation x6, TestMergedRead x2, TestReset x3, TestPath x1, TestSchemaValidity x2, TestFrontmatter x6, TestFourQuestions x6, TestArgAssembly x4, TestRelationshipToSessionMode x2, TestSubcommandsDocumented x1, TestPluginManifestPermissions x2). Auto-regen `help.md` (54→55 commands). Test baseline 2657→2696 (+39). Validator clean (0 hard / 0 soft). The /kaizen:session-mode SessionStart QA can read workflow.json as a pre-fill source — bridges persistent defaults into per-session intake without merging the two concerns. |
| 2026-05-17 | feat     | +130   | P3 of menu consolidation — `/kaizen:loop` empty-args grows a 2-question wizard (budget + stop conditions). Bridges from `/kaizen:workflow` Q2=Loop: when the super-menu picks Loop as the default run-mode, the agent chains into loop.md's wizard to capture --loop-its + --loop-stop, then persists via `kaizen-workflow-config set`. Q1 = iteration budget (10/20/30/60 with 30 recommended; "Other" supports custom int). Q2 = multiSelect over 4 stop conditions (ledger-empty / completion-promise / iteration-cap / manual-cancel) with iteration-cap + manual-cancel always-on as safety nets (selection is intent-documentation). allowed-tools gains AskUserQuestion + kaizen-workflow-config permissions. Pre-P3 args-mode usage (structured ledger + legacy freeform + --cancel) intact. test_loop_command.py extended (+17 tests across TestP3WizardFrontmatter/Structure/BridgeFromWorkflow/ArgAssembly/Backcompat — 7→24 total). |
| 2026-05-17 | feat     | +90    | P4 of menu consolidation — `/kaizen:brain` empty-args now fires a single multiSelect AskUserQuestion over 4 high-frequency verbs (Capture / Search / Audit / Status). Each pick prompts the user for the per-verb input (text body for capture, query for search, --apply choice for audit) then re-dispatches via the args-mode bash body. The 4 less-common verbs (detect / path / promote / evolve / migrate / index / stats / seed) stay reachable as `$ARGUMENTS` — the picker intentionally curates the high-frequency surface to respect the 4-option-per-question ceiling without forcing a 2-level drill-down. Pre-P4 args-mode bash dispatcher (the existing case-block routing to brain.py / brain_index.py / brain_promote.py / brain_audit.py / brain_evolve.py) preserved verbatim under the new "## Args-mode dispatch" heading; default-to-status back-compat (`ARGS="${ARGUMENTS:-status}"`) intact for CI callers. allowed-tools gains AskUserQuestion + explicit permissions for each backing script (was implicit before — now declared per iron-law plugin-manifest-permissions). +20 tests in test_brain_command.py (TestFrontmatter x6 / TestMenuStructure x4 / TestDispatchTable x3 / TestArgsModeBackcompat x6). Test baseline 2713→2733. Validator clean (brain feature). |
| 2026-05-17 | refactor | -130   | Fold of /kaizen:session-mode slash into /kaizen:workflow's session-scope branch. `mode` overlapped with `models` slash; user requested unification. commands/session-mode.md retired (KAIZEN_ALLOW_DELETE=1 — explicit user authorization). commands/workflow.md Q1 grows from 2 → 3 options (Session-only / This-project / User-global); arg-assembly split into per-scope dispatch — session-scope shells out to `kaizen-session-mode set ...` (keeps the file format hooks consume directly), project/global continue to `kaizen-workflow-config set ...`. Backing infrastructure intact: bin/kaizen-session-mode + scripts/session_mode.py + .kaizen/session-mode.json all consumed directly by auto-handoff.sh, userprompt-skills-reminder.sh, session-intake.sh — the slash retires, the back-end stays. intents.yaml 3 mode triggers (start-loop / start-workflow / set-mode-generic) re-pointed at /kaizen:workflow. Cross-refs in commands/setup.md + skills/auto-handoff/SKILL.md updated to /kaizen:workflow with parenthetical history note. test_mode_command.py rewritten — pins BOTH retirements (mode.md from P0 + session-mode.md now) + canonical surface (workflow.md covers session scope) + backing-infrastructure assertions (bin + py module + storage must NOT be removed). test_intent_mode.py TestAllSuggestionsRouteToWorkflowCommand replaces old session-mode assertion; relaxed to allow historical "(replaces retired /kaizen:session-mode loop)" context in suggest text as long as ACTIVE invocation leads with /kaizen:workflow. test_workflow_command.py TestRelationshipToSessionMode → TestFoldedSessionModeSurface (asserts 3-scope table + session-scope kaizen-session-mode dispatch). help.md regen 55→55 (delta neutralized by P2's +1 / this -1). Test baseline 2733→2728 (-5; retired 14 session-mode contract tests + added 9 fold contract tests). |
| 2026-05-17 | refactor | +10    | Renamed `/kaizen:gate` → `/kaizen:precommit` to disambiguate from `/kaizen:gatekeeper` (multi-axis audit aggregator). User reported the shared `gate*` prefix as confusing; the two slashes do distinct jobs — precommit dry-runs the 9-check pre-commit hook against staged changes, gatekeeper aggregates iron-laws + etu + karpathy + validator + token-bloat + coverage into one verdict. `git mv commands/gate.md commands/precommit.md` + cascade-edited 8 cross-refs (README.md, agent-brief/SKILL.md, menu/SKILL.md, vibe-check/SKILL.md, commands/vibe-check.md, bin/kaizen-vibe-check, scripts/vibe_check.sh, scripts/review.sh). Frontmatter description gains explicit "Renamed from /kaizen:gate" + 4 new trigger phrases. help.md auto-regen. Test baseline 2728→2728 (zero impact). |
| 2026-05-17 | chore    | +33    | Bulk frontmatter `name:` alignment — 33 SKILL.md files had `name:` declared in the gerund-ified "verbing-noun" convention from an earlier era (e.g. `change-analyzing` / `plan-creating` / `tasks-executing`) which diverged from the dir basename the skill-loader uses for routing. Aligned all 33 to dir-basename (`analyze` / `create-plan` / `execute-tasks` / ast-grep-{code,project,router,rule,scan} / etc.). 4 namespace-prefixed names (`remember:evolve`, `remember:process`, `remember:status`, `kaizen:init`) also stripped to dir-basename since the vendored-skills list is empty (per CHANGELOG 2026-05-17 retirement — all skills are now plugin-original derivatives). frontmatter audit name-mismatch count: 33 → 0. Test baseline 2728 → 2728 (zero behavior change — pure metadata alignment). |
| 2026-05-17 | feat     | +330   | Quality-check TDD pass — 3 improvements from brainstorm landed (plans/2026-05-17-quality-check-brainstorm.md). (1) Frontmatter split: `_gate_frontmatter` now emits TWO findings instead of one rolled-up warn — name-mismatch=ERROR (hard-gate, blocks commit because skill-loader can't route a mismatched name) + weak-routing=WARN (soft-gate, advisory because skill-suggest may miss <3-trigger-phrase descriptions). Pure-function `_classify_frontmatter_findings()` extracted for testability. (2) Same split also gives weak-routing its own rule_id so audit reports + handoffs distinguish the two concerns. (3) New `kaizen-slash-collision` axis — scans `commands/*.md` filenames, groups by N-char prefix (default 4), flags groups with ≥2 members. `find_collisions()` (pure) + `scan_commands_dir()` + argparse CLI + bin wrapper + plugin.json permissions. New `slash-collision` SUB_GATE in gatekeeper (warn-severity, advisory). Live smoke caught 4 real collisions on the current surface (back* / self* / stat* / trac*) — gate/gatekeeper resolved by 96a9f10 rename. TDD discipline: 5 split-tests RED→GREEN→full-suite, 12 collision-tests RED→GREEN→full-suite. Test baseline 2728 → 2745 (+17). Validator clean. Plan doc tracks remaining 17 brainstorm items by score for future passes. |
| 2026-05-17 | feat     | +600   | Ralph full-automation — schema+yaml+json driven config. New skills/loop/domain/automation.yaml (TTL / stuck / session-boundary knobs) + automation.schema.json validates the yaml. loop_ledger.py now resolves config via 3-tier precedence (env > yaml > hardcoded fallback) using a stdlib-only minimal YAML parser when PyYAML is unavailable. Two net-new full-automation behaviors: (1) hour-resolution TTL — KAIZEN_LOOP_MAX_AGE_HOURS env or `ttl.max_age_hours` yaml, opt-in (null default) so long-running loops still work; finer-grained than the existing day-resolution 30-day default. (2) session-boundary auto-archive — stop-ralph.sh's pre-fix silent `exit 0` on session_id mismatch replaced with copy-to-`<state-dir>/loops/archive/<UTC>-orphan-<sid>.md` + delete, so the next session starts clean and the orphan's audit trail survives. (3) Empty-body short-circuit in decide() — a genuinely-complete loop emits "ledger empty" instead of "stale auto-cancel" regardless of age (corrects misleading reason). 11 TDD tests (TestHourResolutionTtl×4, TestSessionBoundaryArchive×2, TestSchemaYamlDrivenDefaults×5). 8 pre-existing test_loop_merge fixtures preserved (would have broken with hour-default-on; opt-in design avoids that). Brainstorm plans/2026-05-17-ralph-automation-brainstorm.md tracks remaining 27 items by score. Test baseline 2748 → 2759 (+11). |
| 2026-05-17 | refactor | +71    | /kaizen:help gets an AskUserQuestion cluster-picker QA + /kaizen:menu retired (user-authorized; KAIZEN_ALLOW_DELETE=1). help_gen.py gains `cluster <name>` subcommand (renders one cluster's sub-table for QA drill-down) + render_full_help_md prepends a "Interactive cluster wizard" preamble that names the 4 visible Q1 options (audit/quality / workflow / observability / brain/memory) + Other for the overflow (plugin-meta / discovery/search / intent/session / dev-aids). CLUSTERS catalog updated — `precommit` now under audit/quality, `workflow` under workflow, and new `intent/session` cluster (intent/session-mode/skill-suggest); 0 uncategorized commands remain. skills/menu/SKILL.md deleted — its purpose (curated taxonomy + onboarding pointer) was redundant after /kaizen:help became interactive; references in README + setup.sh + enable_all.sh + commands/rules.md + render_full_help_md drill-down all repointed to /kaizen:help. 6 new tests (cluster subcommand × 4 + QA preamble × 2). test_help_command.test_includes_drill_down_pointers drops the /kaizen:menu assertion. Test baseline 2804 → 2810 (+6). Validator clean (0 hard, 4 soft). |
| 2026-05-17 | feat     | +400   | Affected-tests subset gated in pre-commit (default-on). New `_affected_tests.py` (private helper) — pure `derive_modules(staged_files, repo_root, plugin_root) → (modules, reason)` with rules: test files staged → itself; `scripts/<x>.py` + paired `tests/test_<x>.py` → direct map; `_<x>.py` (private helper) or unmapped script → `["FULL"]` sentinel; no Python staged → `[]` skip. Thin stdin CLI prints comma-sep modules / `FULL` / empty so bash can dispatch directly. `run_tests_parallel.py` gains `--modules MOD,MOD,...` flag that bypasses discovery and runs only the named modules. New Check 14 in `pre-commit.sh` derives the subset via the helper, dispatches `run_tests_parallel.py` (with `--modules` for direct map; without for `FULL`), and hard-fails on test failure. Opt-out via `KAIZEN_PRECOMMIT_TESTS=0` env (default-on per user choice). `KAIZEN_CI_GATE_RECURSION=1` guard prevents self-recursion when the runner is invoked during a test-pipeline run. 14 new tests (TestDeriveModules×8 + TestCli×3 + TestModulesFlag×3). Test baseline 2810 → 2824 (+14). Validator clean. |
| 2026-05-17 | feat     | +445   | Ralph brainstorm #4 + #5 + #6 — workflow-config / dxm-per-iteration / implicit-ledger. (#4) `workflow_config.py` gains `get-key <dotted-key>` subcommand (returns scalar values, lists as CSV, missing keys empty + rc=0 — bash-friendly); `setup-ralph-loop.sh` falls back to `loop.max_iterations` from workflow-config when `--its` is absent (silent skip if no config, stderr note when fallback fires; explicit `--its` always wins). (#5) `stop-ralph.sh` block branch now emits a `Stop-ralph-iteration` trace event via `--data` carrying `{action, iteration, pending, completed}` after each iteration boundary — gives `kaizen-trace search` per-fire visibility into loop progress without re-parsing state files. Best-effort: any trace error swallowed so tracing never breaks the loop. (#6) `setup-ralph-loop.sh` detects `- [ ]` checkboxes in the prompt when no `--item` / `--ledger` was given and seeds the structured ledger from those lines (one trust-based item per match; `- [x]` / `- [X]` skipped); explicit `--item` disables auto-parsing. Plan rows 4/5/6 flipped to ✅ done. 15 new tests: TestGetKey×5 + TestWorkflowConfigDefault×4 + TestPerIterationTrace×2 + TestImplicitLedgerFromTodos×4. Test baseline 2824 → 2839 (+15). Validator clean. |
| 2026-05-17 | feat     | +625   | Menu-consolidation P5 + P7 — `/kaizen:discovery` wrapper + `kaizen-menu-lint` quality gate. P6 (shared menu library) DROPPED as YAGNI per user direction (only 5 menus, each meaningfully different, premature abstraction). (P5) New `commands/discovery.md` — 2-Q AskUserQuestion wizard (Q1 multiSelect over 4 surfaces: codebase / knowledge base / claude docs / web scrapes; Q2 single: search / index / stats) that dispatches the matching `/kaizen:onboard|knowledge|claude-docs|scrape` slash with the picked verb. Added to discovery/search cluster in help_gen (help.md grows 55 → 56). (P7) New `skills/workflow/scripts/menu_lint.py` (pure `lint_menu(text, path) → list[finding]`) verifies AskUserQuestion contract: missing AskUserQuestion in `allowed-tools` when body declares a wizard = error (runtime break); >4 questions in a single fenced AskUserQuestion call OR >4 options per question = warn. Multi-fence multi-call menus (e.g. setup.md's 3 sequential calls) are NOT false-flagged. `bin/kaizen-menu-lint` wrapper + plugin.json permissions. New SUB_GATE `menu-lint` in `gatekeeper.py` (errors block, warns advise). Boy-scout fixes: 3 pre-existing menus surfaced by the live lint had missing AskUserQuestion perm — `help.md` (my own from the cluster-picker work; fixed in render_full_help_md template), `code-tour.md`, `plugin-development.md`. 1 deferred design issue: plugin-development.md Q1 has 13 options (needs Other + drill-down refactor — separate scope). 18 new tests (TestDiscovery×4 + TestLintMenu×6 + TestCli×2 + new fixture cases). help.md QA body limit bumped 150 → 165. Plan rows P5 / P7 → ✅ done; P6 → ~ dropped with rationale. Test baseline 2839 → 2857 (+18). Validator clean. |
| 2026-05-17 | feat     | +250   | New `discovery_mcp.py` — federated MCP aggregator over the 4 discovery surfaces (codebase / knowledge / claude-docs / scrape). 3 tools: `discovery_list_surfaces()` (catalog of {name, slash, desc}), `discovery_search(query, surfaces=None, top_k_per=4)` (parallel async cosine across picked surfaces; per-surface failures wrap as `{"error": "..."}` so one bad surface doesn't break the others), `discovery_stats(surfaces=None)` (same federated pattern for stats). SURFACES catalog is the SSOT for the 4 indexes — slash / index_module / desc per surface. Wired into `gateway.py::SUBSERVERS` (mounts cleanly alongside the 4 per-surface MCPs — naming convention `discovery_*` avoids tool-name collisions with `onboard_*` / `knowledge_*` / etc.). plugin.json permission added. 14 new tests: TestModuleSurface×2 + TestSurfacesCatalog×2 + TestResolvedSurfaces×4 + TestDoSearchSurface×2 + TestFederatedSearchAggregator×4. Test baseline 2857 → 2871 (+14). |
| 2026-05-17 | feat     | +145   | `discovery_mcp` gains `discovery_list_embed_models()` + wizard Q3 embedding-model picker. New MCP tool queries Ollama's /api/tags + /api/show per local model and filters by `capabilities: ["embedding"]` (reliable signal — beats name-pattern heuristics like `*embed*`). Returns [{name, capabilities, dim}] where dim is extracted from `<family>.embedding_length` in model_info. Graceful degradation: Ollama unreachable → returns []; per-model show failures don't poison the list. Discovery wizard body adds Q3 (only fires when Q2=Index/refresh): agent calls discovery_list_embed_models() then builds Q3 options dynamically — "Keep each surface's current model (default)" + top 3 local embed models sorted by dim+name. Live smoke: surfaces qwen3-embedding:0.6b (dim=1024) on this box; correctly filters out qwen2.5-coder / granite4.1 / qwen3.5 (chat-only). discovery.md body grows ~+50 lines (Q3 section + MCP-surface table including the new tool). 5 new tests: TestListEmbedModels×3 + test_q3_embed_model_picker_when_indexing + test_body_lists_mcp_tools. Test baseline 2871 → 2876 (+5). Validator clean. |
| 2026-05-18 | refactor | +30    | discovery wizard Q3 (embed-model picker) now fires ALWAYS, not just when Q2=Index. User feedback: "you didn't give me the option to switch model in the qa menu" while running Stats. Per-action Q3-pick semantics documented in a body table — Search: no-op for this call, pin via `kaizen-models pin-embed`; Index: pass-through to indexer; Stats: read-only, pin or follow up with `discovery <surface> index`. Tests updated: TestQ3 split into `test_q3_embed_model_picker_always_fires` + `test_q3_per_action_semantics_documented`. Test baseline 2876 → 2877 (+1; one test split into two replaces the gated variant). |
| 2026-05-18 | feat | +~1880 -0 | Positional token schema (Phase 1) — kaizen-tokens CLI + FastMCP server + watcher integration; 6 commits T1-T6; per spec semantic-search/docs/2026-05-18-positional-token-schema-design.md |
| 2026-05-18 | feat | +~870 -0 | Brainstorming confidence-score + deferral-bucket (BK-012) — 8 TDD phases P1-P8 per docs/superpowers/specs/2026-05-18-brainstorming-confidence-score-design.md. New: skills/brainstorming/domain/brainstorm-rubric.yaml (7 rules + NEEDS_AGENT fallback, version: 1), skills/brainstorming/domain/schemas/idea.schema.json (15 props, 8 required, confidence vs classification_confidence distinct), skills/workflow/scripts/brainstorm.py (signal computer + score CLI + override loop + threshold gate, argparse subcommands ready for stats Phase 2), bin/kaizen-brainstorm symlink, 8 paired test files (T1 rubric-lint / T2 rubric-cases / T3 signals / T4 score CLI / T5 schema / T6 override / T7 skill-flow / T8 Ollama-live graceful-skip). Modified: brainstorming/SKILL.md (+Threshold-gated output contract section), plugin.json (+brainstorm.py + bin/kaizen-brainstorm perms). Three Phase-2 deferrals filed as parked: BK-015 (novelty_score Jaccard signal), BK-016 (stats subcommand), BK-017 (sibling-jsonl pre-commit nudge). Test baseline 3050 → 3080 (+30 across the 8 paired files; 2 T8 tests skip without Ollama). BK-012 ticked done. Plan: docs/superpowers/plans/2026-05-18-brainstorm-confidence-score.md. |
| 2026-05-18 | feat | +~220 -0 | Handoff `get --section <name>` — surgical section extraction (highest-leverage upgrade from this session's handoff-system reflection). Replaces "Read the whole 14KB YAML" with "fetch one named section." Sections cover frontmatter (status/outcome/date/session/outcome_assigned_by/outcome_justification) + body (goal/now/test/done_this_session/blockers/questions/decisions/findings/worked/failed/next/files/code_context/session_meta). Default output: human-readable (raw scalar / one list-item per line / JSON for nested); `--json` flag for structured. Unknown section → exit 1 with available list to stderr; missing file → exit 2. New: tests/test_handoff_get.py (14 tests across 6 classes). Modified: handoff.py (+_load_raw_handoff + _render_section + _cmd_get + s_get subparser, ~95 LOC). Smoke against this session's real 14KB handoff: `--section next` returns 3 lines (~150 tokens) — 87% read-cost reduction. Test baseline 3158 → 3172 (+14). |
| 2026-05-18 | feat | +~250 | kaizen-progress append — atomic-append CLI for .kaizen/workflow/progress.md (eats own dogfood; this row added via kaizen-progress, not Read+Edit). Verbs — append (CLI flags or --stdin JSON). Schema at skills/workflow/domain/schemas/architecture-log-row.schema.json. 11 paired tests cover create-with-header, append-to-existing, validation of date/kind/loc/summary, stdin-JSON, constant-cost append on 100KB seed (delta less than 200B), canonical row format. Bin wrapper bin/kaizen-progress. plugin.json perms added. Test baseline 3172 to 3183 (+11). |
| 2026-05-18 | feat | +50 | kaizen-io read N-file multi-arg — extends single-arg back-compat to accept N positional file paths; runs in parallel via _run_batch (same primitive batch uses); default output concat-with-separators, --json returns ordered envelope array. Eliminates the JSON-stdin ceremony for the common case (read 2-5 files in one call). 8 paired tests cover single-arg back-compat, N-file concat + json, input-order preservation against parallel completion, missing-file per-file status, exit-code semantics. Test baseline 3183 to 3191 (+8). Modified skills/workflow/scripts/io.py (~30 LOC delta). |
| 2026-05-18 | feat | +~280 | kaizen-learn append/list — append-only learning + evolution log with structured problem/solution/pattern entries. Three categories — wasteful_tokens / roundtrips / anti_patterns. Schema (JSONL) carries category + problem + solution + pattern + ts + id + promotion_status + optional savings_estimate + trigger_keywords + source_sid + source_commits + references. Repeatable --trigger-keyword and --source-commit flags. --stdin JSON form for scripted capture. 11 paired tests cover create-if-absent, multi-append unique-ids, schema validation (invalid category / missing problem / empty problem), optional-field roundtrip, source attribution roundtrip, list-all + filter-by-category, stdin-json, append-cost-constant on 1000-entry seed (delta less than 500B). Bin wrapper bin/kaizen-learn. Sink env-overridable via KAIZEN_LEARNING_DIR. Dogfood — 3 round-trip-reduction wins from this session captured as seed entries. Test baseline 3191 to 3202 (+11). |
| 2026-05-18 | feat | +~80 | kaizen-learn branch-aware capture + filter — every entry auto-captures git branch via cwd rev-parse; --branch override for cases like cross-repo capture; list --branch BRANCH filters entries. Enables per-feature history isolation (e.g. all entries from feat/observer-phase-2 in one query). 5 paired tests cover master capture, feature-branch capture, no-branch outside repo, explicit --branch override, list filter. Test baseline 3202 to 3207 (+5). |
| 2026-05-18 | feat | +~280 | posttool-roundtrip-detect hook (force-half of learning log) — PostToolUse hook detects Read+Edit (or Read+Write) on same path within configurable window (default 30s; KAIZEN_LEARNING_DETECT_WINDOW env). When fired, emits additionalContext nudging the agent to capture the friction via kaizen-learn append --category roundtrips. Pure-function Python core (_roundtrip_detect.py) — deterministic (no randomness; now passed as parameter); reproducible (proven by test_idempotent_replay_same_inputs_same_outputs); reusable (PAIRS registry — adding Grep then Read is one line). State persisted to KAIZEN_LEARNING_DIR/detect-state.json via tempfile+os.replace (atomic). State pruned per-call (entries older than window dropped — constant memory). Bypass via KAIZEN_LEARNING_DETECT_DISABLE=1. Bash wrapper handles cross-platform readlink + always exits 0. 12 paired tests cover read-then-edit nudge, edit-after-read-Write also nudges, outside-window no-nudge, different-paths no-nudge, edit-only no-nudge, idempotent-replay (reproducibility proof), boundary at exact window-end, state-pruning constant memory, run_hook envelope shape, garbage-input safety, missing-state-file first-run, KAIZEN_LEARNING_DETECT_DISABLE bypass. hooks.json wired into PostToolUse matcher * slot. plugin.json perm added. Test baseline 3207 to 3219 (+12). |
| 2026-05-18 | feat | +~200 | Observer Phase 1.5 (PostToolUse capture hook) — closes the gap between ingest module (clever-lama-mcp) and live capture. hooks/claude/posttool-observer-capture.sh fires on every PostToolUse, delegates to _observer_capture.py (pure-function core), writes canonical observer event to ~/.claude/.kaizen/observer/events.jsonl. Standalone (no cross-repo import; minimal schema-shaping duplicates clever-lama-mcp ingest.py — justified per DRY rule-of-three). MCP-tool detection (mcp__server__name pattern) auto-upgrades event_kind to mcp_tool_call + extracts mcp_server. Subagent source detected via CLAUDE_SUBAGENT_TYPE env. Bypass via KAIZEN_OBSERVER_DISABLE=1. 11 paired tests cover normalize (pre/post/mcp/subagent/garbage/idempotent), capture (append-only, multi-append, disable, constant-cost on 500-entry seed). Test baseline 3219 to 3230 (+11). Live smoke confirmed canonical event shape. |
| 2026-05-18 | feat | +~190 | Observer Phase 1.6 (read-side CLI) — kaizen-observer-events bin closes the observer's query surface. Subcommands recent (last N; --n flag; --json), stats (counts per event_kind + per tool), filter (by --tool, --source). Read-only against KAIZEN_OBSERVER_DIR/events.jsonl; graceful on missing sink (empty array); skips malformed JSONL lines line-by-line. Renamed file to observer_events.py + bin to kaizen-observer-events to avoid clash with existing kaizen-observe (the 6-layer observability tool — distinct concern). 9 paired tests cover recent (no-sink + default-10 + n-override + human-readable), stats (kind+tool counts + zero-sink), filter (by tool + by source), graceful-malformed-jsonl skipping. Test baseline 3230 to 3239 (+9). |
| 2026-05-18 | feat | +~380 | docs/superpowers/ session-bundle layout — kaizen-bundle CLI (init/list/path/add) + convention spec. Folder pattern <YYYY-MM-DD>-<project-kebab>-<sid8>; each session's specs+plans+brainstorms+notes bundle together; templates/ stays outside bundles (durable). Pure-function core (bundle_folder_name + bundle_path) — deterministic + reproducible per user contract. Env-overridable root via KAIZEN_SUPERPOWERS_DIR. 14 paired tests (5 pure-fn + 9 CLI via subprocess) cover folder-name grammar, project-kebab enforcement, invalid-date raises, init idempotency, no-sid form, list+filter, path pure-compute, add moves file + creates bundle if absent. Migration applied — 9 existing flat files in specs/+plans/ git-mv'd into 2026-05-15-kaizen-md/ + 2026-05-18-kaizen-md-6ebbb7cb/ bundles (preserving history). Old flat dirs removed. README.md at docs/superpowers/ documents layout + grammar + CLI + design contract. Test baseline 3239 to 3253 (+14). |
| 2026-05-18 | feat | +~140 | kaizen-bundle git automation —  subcommand (idempotent; creates nested git repo at docs/superpowers/.git with bootstrap commit covering all 46 existing files) + --commit flag on init/add (auto-stage + commit via local git). Deterministic commit-message subjects (init bundle(NAME), add bundle(NAME) file(NAME), bootstrap bundle(superpowers)) — same inputs always produce same subject. Graceful on missing git binary (exits 1 with helpful message). Nested repo coexists with parent kaizen-md repo (parent doesn't track docs/superpowers/.git — added explicit .gitignore for clarity). 8 paired tests cover git-init idempotency + bootstrap + graceful-no-git, init/add --commit flags including default-no-commit, deterministic subject format. Live applied — docs/superpowers/ now its own git repo at commit c58e096. Test baseline 3253 to 3261 (+8). |
| 2026-05-18 | feat | +~280 | kaizen-bundle backup automation — auto-commit-DEFAULT (was opt-in; now opt-out via --no-commit) + patch-journal backup. Every commit in the superpowers nested repo writes git format-patch -1 to KAIZEN_BACKUP_DIR/superpowers/patches/<UTC>-<sha8>.patch + appends one row to manifest.jsonl. Deterministic patch filenames (timestamp + 8-char sha; lexicographic sort = chronological). Recoverability proof — test_patch_replayable_via_git_am replays the patches onto a fresh repo and verifies the bundle folders reappear. Backup-resilient (graceful on missing git binary, manifest atomic-appended like kaizen-progress/kaizen-learn). Also fixed Path.rename cross-device bug found via live smoke (tmpfs to home filesystem) — switched to shutil.move. New backup subcommand kaizen-bundle backup list to query the journal. Old --commit flag retired; tests updated. Test baseline 3261 to 3279 (+18 net — 9 new backup tests + 8 updated git tests minus 1 retired flag test, plus shutil.move regression coverage). |
| 2026-05-18 | feat | +~150 | iron-laws.yaml — 5 new laws extracted from this session's recurring patterns. (1) append-only-sink — structured-event sinks never read; constant-cost append; pattern across kaizen-progress/learn/observer-events/patch-journal. (2) drift-resilient-config-read — config/rules/schema re-read PER evaluation; counter to cache-pinning bug (9b29d3d kaizen-implementer Bash blocker); proven via observer rules + ingest. (3) prcdr-contract-declared — every public-API module declares programmable+reproducible+consistent+deterministic+reusable in docstring. (4) cross-device-safe-move (HARD) — use shutil.move not Path.rename; caught via live smoke (OSError EXDEV tmpfs to home). (5) cli-json-flag — every structured-output CLI offers --json for scripting/MCP consumers. All 5 are enforcement=manual for now (documented + cross-referenceable); auto-enforcement checkers can land later. Registry grew 25 to 30 laws (19 auto + 11 manual). references/iron-laws.md regenerated. 8 new tests verify each law has required shape + cites session evidence. Existing test_iron_laws hardcoded counts updated. Test baseline 3279 to 3287 (+8). |
| 2026-05-18 | refactor | -32 +18 | DRY consolidation — 4 modules (progress_log, learning_log, superpower_bundle, _observer_capture) replace their own open(path, 'a') reimplementations with the existing _atomic.atomic_append_line helper. Per DRY rule-of-three plus the new append-only-sink iron-law from 36d268b. 4 sites converted; each saves ~3 LOC and inherits the canonical helper's atomicity guarantees (Linux O_APPEND semantics for less than PIPE_BUF). _observer_capture (in hooks/claude/) needs explicit sys.path prep since the hook dir isn't on the scripts path. 53 paired tests across the 5 affected suites all green; live smoke on progress + learn confirms behavior preserved. |
| 2026-05-18 | feat | +~60 | handoff_mcp gains handoff_get tool — MCP exposure of the section-extraction win (commit 7502056). Pure read; reuses handoff.py's _load_raw_handoff + _ALL_SECTIONS registry (DRY). Returns {section, value} on success; {error, available} on unknown section / missing file. Closes the gap surfaced by the handoff trace+explore pass — programmable MCP consumers no longer need subprocess shell-out + stdout parsing. 6 paired tests cover scalar / list / frontmatter sections, unknown-section error with availability list, missing-file error, tool registration. |
| 2026-05-18 | feat | +~120 | handoff verify-hash — finishes the half-built integrity check. Scaffold captures cc_session_sha256 + cc_session_jsonl in session_meta; until now resume never compared against current file (silent garbage if JSONL rotated/truncated). New  re-hashes the linked JSONL + compares. 4 verdicts — match (resume safe) / drift (file mutated) / missing (linked path gone) / no-link (handoff lacks session_meta). Default human-readable; --json structured. Exit codes — 0 for match or no-link, 1 for drift/missing/handoff-not-found. 6 paired tests cover all 4 verdicts + human output + missing handoff file. Live smoke on 2026-05-18_18-52 caught real drift (session JSONL has mutated as the session continues writing — canonical detection case). Test baseline 3287 to 3293 (+6). |
| 2026-05-18 | feat | +~190 | kaizen-bundle scan — orphan artifact tracker for the docs/superpowers/ assigned path. Walks top-level entries; skips README.md / templates/ / .git / .gitignore (non-orphan by convention); skips bundle folders matching date-prefix regex. For each orphan file collects metadata (name + path + size + sha256 + suggested_date). suggested_date comes from YYYY-MM-DD filename prefix if present, else file mtime. Dupe detection — indexes every file in every bundle by sha256 then flags orphans whose content matches an existing bundle file (reports orphan + bundle + bundle_file). Default human-readable; --json structured. Read-only (no mutations) — apply path deferred. 9 paired tests cover empty-state (no orphans no bundles + bundles-but-no-orphans), orphan finding (top-level md + skip README/templates), suggested-date (filename prefix + mtime fallback), dupe detection (content match + different content), dry-run guarantee. Live smoke on real docs/superpowers/ — 0 orphans + 0 dupes (clean after the 84819a2 bundle migration). Test baseline 3293 to 3302 (+9). |
| 2026-05-18 | feat | +~70 | kaizen-bundle scan gains --apply and --branch — closes the apply half of the user's tracker ask. scan --apply moves each non-dupe orphan into its target bundle (date from filename prefix or mtime; project default kaizen-md; sid omitted for scan-bundled artifacts). scan --apply --branch X first git checkouts -b X in nested superpowers repo so the bundling pass lands on its own branch (mergeable later). Dupes always skipped (sha256-match against existing bundle file). After moves, auto-commits in nested repo via existing _git_commit which also writes a patch-journal entry. 5 paired tests cover orphan-with-date-prefix-lands-correctly, no-orphans-no-op, dupe-not-moved, branch-creates-and-switches, no-branch-stays-on-master. Test baseline 3302 to 3307 (+5). 40/40 across all 5 bundle suites green together. |
| 2026-05-18 | feat | +~210 | kaizen-plugin-docs — utility surface tracker for plugins/kaizen/. Symmetry with kaizen-bundle but for DURABLE docs (no bundle/session concept; no mutation). Scans 6 doc kinds — skill 90 / reference 39 / command 58 / agent 7 / root 10 / domain 78 — 282 total on live surface. classify_kind pure-function classifier (one regex per kind; None for non-doc paths). scan subcommand counts per kind plus content-sha256 dupe detection. list subcommand exposes per-file metadata (path/kind/size/sha256) with --kind filter. KAIZEN_PLUGIN_ROOT env override (defaults via __file__ resolution). Renamed from kaizen-docs to kaizen-plugin-docs to avoid clash with existing kaizen-docs (docs_gen tool). 7 paired tests cover kind-classifier pure-fn (8 path cases), scan counts (per-kind + total-matches-sum), dupe detection, list (all + filter + human), graceful-missing-root. Live smoke confirms 282 docs across 6 kinds with 0 dupes. Test baseline 3307 to 3314 (+7). |
| 2026-05-18 | feat | +~70 | kaizen-plugin-docs gains 5 umbrella dispatch subcommands — consolidated-CLI-parent pattern. Routes frontmatter (kaizen-frontmatter) + links (kaizen-md-link-rot) + dupes (kaizen-md-dupes) + whitespace (kaizen-md-whitespace) + heading-depth (kaizen-md-heading-depth) under one parent. UMBRELLA_BINS registry (kebab-case key to sibling bin name) + resolve_umbrella_bin pure lookup fn + _cmd_umbrella generic dispatcher (subprocess.run with args forwarded via argparse REMAINDER). Sibling bins stay for back-compat (matches kaizen-brain / kaizen-statusline shape). 6 paired tests cover registry membership (all 5 subcommands + each maps to existing bin), pure-fn lookup (success + None for unknown), --help mentions all 5 umbrella names, pre-existing scan + list NOT shadowed. Test baseline 3314 to 3320 (+6). |
| 2026-05-18 | refactor | -24 +44 | DRY consolidation round 2 + dangling-docs fix. Extracted env_overridable_dir helper to _paths.py — env wins; else base (defaults to ~/.claude/.kaizen) joined with default_segments. Per drift-resilient-config-read iron-law re-reads os.environ on every call (no module-level cache). 7 paired tests cover env-wins / empty-env-treated-as-unset / default-no-env / multi-segment-join / no-segments-returns-base / base-override / deterministic. 5 sites refactored — _observer_dir (observer_events + _observer_capture), _learning_dir (learning_log), _superpowers_dir (superpower_bundle). _backup_dir intentionally NOT refactored because its env-resolution shape is different (env value gets /superpowers appended rather than env value being the full root). Plus dangling-docs fix — test_superpower_bundle_git.py docstring updated from retired --commit flag to new --no-commit opt-out (commit 6a717e1 retired the original --commit; doc lagged 2 days). 81/81 across all refactored module suites green. |
| 2026-05-18 | feat | +~80 | iron-laws +2 — shim-and-sweep + dry-extract-on-third-repetition. 30 to 32 laws (19 auto + 13 manual). Both extracted from this turn's user direction — fix-dangling-backwards-compat (codifies shim-and-sweep as a hard manual law) + the env_overridable_dir consolidation (codifies the rule-of-three discipline as a soft manual law). shim-and-sweep cites commit 6a717e1 — flag retirement that left 2 stale docstrings 2 days later. dry-extract-on-third-repetition cites commits 3a18b8f (atomic_append_line) + 681a56d (env_overridable_dir) — both extracted AFTER hitting 4-5 sites instead of at 3. session-additions test extended for the 2 new ids + each evidence hint. Existing tests bumped 30 to 32 + manual 11 to 13. references/iron-laws.md regenerated. Test baseline 3327 to 3329 (no new test methods — extended existing assertions). |
| 2026-05-18 | docs | +~20 | parallel-branches kit INDEX.md gains Runtime status table — cross-references each Phase 1 + Phase 2 spec module to its clever-lama-mcp shipped state (loader/checker/dispatcher/render/_plan_cli/4 interwave handlers + 3 final handlers all shipped; 5 deferred items explicitly catalogued). Closes the docs-side of finishing parallel-branches — readers can now see status at a glance from the kit INDEX without diving into commit history. Also bumps the canonical runtime sha references in the table. |
| 2026-05-18 | docs | +~70 | docs/superpowers/templates/ gains umbrella README — explains the 3 entries (parallel-branches kit / observer kit / chunk-plan-template loose), why kits live there (long-lived; addressable-by-topic) vs session bundles (time-scoped; addressable-by-date), and the conventions to follow when adding a new kit (top-level INDEX or README; schemas/ + examples/ subdirs; runtime cross-reference; append a row here per shim-and-sweep iron-law). Plus boy-scout sweep on parent docs/superpowers/README.md tree diagram — now includes templates/README.md row. |
| 2026-05-18 | feat | +~170 | system-watcher MVP — kaizen-daemon systems-check + 2 hooks (SessionStart-boot + PostToolUse-periodic). systems-check pings 4 runtime systems (hooks/trace/dxm/observer) and appends heartbeat row to KAIZEN_KEEPALIVE_DIR/heartbeat.jsonl. Per-system probe collects {name kind exists path size last_mtime count}; jsonl row count excludes malformed lines. session-start-systems-check.sh fires at SessionStart (one-shot boot). posttool-keepalive.sh fires every KAIZEN_KEEPALIVE_INTERVAL tool calls (default 25; counter persisted in keepalive/counter.txt; resets after each fire). Both hooks honor KAIZEN_KEEPALIVE_DISABLE bypass; both exit 0 always. Live smoke detected trace=109481 events + dxm=1 + hooks ok; observer absent (pending session restart to load its capture hook). 7 paired tests. Test baseline +7. |
| 2026-05-18 | fix | -4 +4 | rogue manifest/profile writers — SSOT cleanup. _blobs.py:77-78 used KAIZEN_USER_DIR/manifest.{json,lock} instead of _paths.MANIFEST_JSON / MANIFEST_LOCK. models.py:466-467 hardcoded ~/.claude/.kaizen/profile.env instead of importing _paths SSOT. Result was 3 path-restructure Phase 3 conflicts (manifest.json / manifest.lock / profile.env had both src + dst because fresh writes kept going to old root after migration). Fix routes _blobs through _p.MANIFEST_JSON + _p.MANIFEST_LOCK; routes models through _p.DATA_DIR / profile.env with per-call env re-read (preserves drift-resilient-config-read iron-law). 43 tests green (models + path_migrate + binary_quantization). After this lands the migrator should re-classify the 3 from conflict to needs-migration once user removes the duplicates at root. |
| 2026-05-18 | feat | +~180 | system-watcher heartbeat — coverage 4 to 8 + staleness + rotation. Per audit in docs/superpowers/2026-05-18-kaizen-md-9f4c8972/audit-heartbeat-daemon-watcher.md _TRACKED_SYSTEMS grows 4 to 8 (added learning + keepalive-meta + daemon-watcher + handoff-db; gold deferred — per-project sink needs glob-walk probe shape). _check_one refactors from 2 ad-hoc special-cases to a 5-kind dispatch table (config/sink/pid/db/meta) — _probe_sink/_probe_pid/_probe_db/_probe_meta each return canonical state dict. _stat_into + _count_jsonl_rows + _resolve_path extracted as KISS helpers. Stale flag _apply_stale sets state[stale] when last_mtime older than KAIZEN_KEEPALIVE_STALE_SEC (default 7200s); absent sinks never stale. Heartbeat rotation _maybe_rotate_heartbeat moves heartbeat.jsonl to .1 when size >= KAIZEN_KEEPALIVE_ROTATE_BYTES (default 1MB; single rolling rotation); never raises on failure. Both env knobs drift-resilient (re-read per call). Live smoke proves all 8 probes accurate against real .kaizen state — dxm shows [stale] (prior session 2h ago) and observer now shows count=144 confirming the capture hook IS firing post-restart. 19 tests green (4 baseline + 4 staleness + 3 rotation + 2 pid + 2 db + 1 meta + 3 audit). Test baseline +14. |
| 2026-05-18 | feat | +~360 | chatlog MVP — trigger-rule-driven CC transcript slicer (kaizen-chatlog). New skill chatlog + _chatlog.py pure-function core (match_event / extract_slice / slice_transcript) + chatlog.py CLI (slice subcommand with --transcript / --rules / --out / --json) + bin/kaizen-chatlog wrapper + plugin.json 2 perms + skills/chatlog/SKILL.md body + skills/chatlog/domain/schemas/triggers.schema.json (oneOf 3 trigger kinds) + skills/chatlog/domain/examples.yaml + commands/chatlog.md slash. 3 trigger types — event_type / user_keyword (regex IGNORECASE) / assistant_tool_use (tool_name). 1 action — capture {before N, after N} returns matched event + N before + N after (clamped at transcript bounds). Output is one JSONL per rule under out_dir; each line is one slice (JSON array of events). 23 paired tests across 4 classes — match_event (4 subtests) + extract_slice (5) + slice_transcript (4) + chatlog CLI (3) + graceful-degradation edges. Live smoke against this session 9f4c8972 sliced 8582 events in <1s — gate_failures 0 / handoff_calls 0 / session_boundary_events 5095 (every hook fire matched). Sits below trace/dxm (live-event capture) and above raw Read of session jsonl. Sister to kaizen-trace-search but rule-based rather than semantic. Test baseline +23. |
| 2026-05-18 | feat | +~50 | handoff multi-handoff chain — parent_handoff field. New helper _most_recent_prior_handoff(session_dir, exclude=) scans the session dir for the lexicographically-newest .yaml (filenames carry YYYY-MM-DD_HH-MM prefix so sort-by-name is sort-by-time); exclude= skips the file being scaffolded so a fresh handoff cannot point at itself. _build_session_meta gains parent_handoff kwarg; scaffold wires it. session_meta YAML gains parent_handoff /path/ only when prior exists (omitted otherwise — no empty field). Live smoke against this session links 2026-05-18_20-18_*.yaml to its parent 2026-05-18_20-02_*.yaml (prior session handoff). Closes 1 of 6 remaining handoff upgrades. 5 paired tests — no-prior-returns-none / most-recent-returned / excludes-specific / session-meta-carries-parent / session-meta-omits-when-none. 53 handoff suite tests green (was 48). |
| 2026-05-18 | feat | +~210 | handoff append — mid-session list-section append. New subcommand handoff append surgically inserts ONE entry at the end of a body list-section (done_this_session / blockers / questions / decisions / findings / worked / failed / next). Line-based YAML manipulation handles 3 shapes — `section [empty]` is inflated to a proper list; populated section gets entry inserted before the next top-level key; missing section is created at EOF. dict entries (done_this_session) render as task with files block; string entries render as quoted-text line. Scalar/dict sections (goal / now / test / files / code_context / session_meta) raise ValueError to prevent corruption. Atomic write tempfile then rename. Closes 2 of 6 remaining handoff upgrades. 10 paired tests across 2 classes — _append_to_list_section unit (5 — populated / empty / string / missing / raises) + handoff append CLI integration (5 — task / files / string / missing-file / idempotent). Live smoke on a real 4.5KB handoff yaml appends correctly with files attached. Test baseline +10 (77 total handoff suite green; was 67). |
| 2026-05-18 | fix | -22 +30 | kaizen-implementer agent — pattern syntax fix. Bash(git *) Bash(python3 *) etc. patterns in agent tools frontmatter are silently ignored by Claude Code runtime (only plain tool names honored — verified via claude-code-docs sub-agents.md::available-tools). Result was agent got ZERO Bash, not pattern-restricted Bash. Fix changes tools to bare list — Read / Edit / Write / Glob / Grep / Bash / TaskCreate / TaskUpdate / TaskList / TaskGet. Removes disallowedTools (also pattern-syntax — also ignored). Safety net moves to session-level plugin hook pretooluse-bash-gate.sh + _bash_gate.py which fires on every Bash invocation regardless of dispatching agent — opt-in to active block via KAIZEN_GATE_STRICT=1 env or touch ~/.claude/.kaizen/strict sentinel. SKILL body rewritten to document new contract — broad Bash + session-level gate. Cache file at ~/.claude/plugins/cache/kaizen-md/kaizen/1.39.0/agents/kaizen-implementer.md synced same commit. Closes #36 first arc — capability discovery surfaced the docs gap; next iteration adds new specialist agents. |
| 2026-05-18 | chore | +0 | path-restructure v1.39.0 Phase 3 + Phase 5 — live migration complete. Force-overwrite resolved the 3 conflicts (manifest.json / manifest.lock / profile.env had both root + data/ copies after the SSOT fix landed mid-flight). After force-overwrite all 11 paths report already-migrated; 0 conflicts; backup tarball at ~/.claude/.kaizen/backups/path-restructure-20260518T202259Z.tar.gz with `kaizen-migrate path rollback` available. Phase 5 smoke — observe layers / brain status / trace stats all return well-formed envelopes against the new data/ + indexes/ paths. plugin.json version was already 1.39.0 (Phase 4 done in prior session 406175f). Closes Task #34. Resolution required explicit user OK per pre-deletion belief — confirmed via AskUserQuestion. |
| 2026-05-18 | docs | -3 +3 | plugin description refresh — 3 stale fields. plugin.json::description was stuck at 49 skills / 21 MCP / 6 sub-agents / 14 routines / 12 gates (real today is 91 skills / 31 MCP / 7 sub-agents / 17 routines / 16 gates plus 32 iron-laws + 121 bins + 56 slash + 40 hooks); also mentioned the long-completed `workflow + workflow-routing merge` (v1.33.0) and the retired `5 remember/Second-Brain` family (superseded by kaizen-brain consolidated CLI). marketplace.json::description undersold kaizen as `commit discipline + JSON-sourced backlog`; the per-plugin entry described kaizen as `Pre-commit gate + BACKLOG`. All 3 rewritten to the concise current shape — anchors real counts plus 5 headline features (routines / gatekeeper / handoff / observability / brain+chatlog+bundles) without listing every skill family. JSON validates clean. No code change; doc-only refresh. |
| 2026-05-18 | docs | -3 +3 | description rewrite — drop volatile data (counts + version). Per CLAUDE.md global preference "Never write volatile data (commit shas / LOC counts / point-in-time inventories) into CLAUDE.md — use git notes / changelog / self-checking commands". Same constraint applies to plugin.json + marketplace.json descriptions — they describe WHAT IT DOES, not HOW BIG IT IS TODAY. Live counts available via kaizen list / kaizen surface validate / gatekeeper sub-gate enumeration; version lives in plugin.json::version. New descriptions describe capabilities — pre-commit gatekeeper (iron-laws / Karpathy 4 / frontmatter / schema / coverage axes) / multi-stage routine engine (named routines listed) / lens-driven handoff (named stages) / observability layers (named) / PARA Second Brain / coding-skills library / worktree-isolated sub-agents (named) / MCP gateway / session-bundle convention. Three description fields updated — plugin.json + marketplace.json top + marketplace plugins[kaizen]. |
| 2026-05-18 | feat | +~130 | handoff tree subcommand — commit-task bidirectional map. 3 of 6 handoff upgrades shipped. For each done_this_session entry with files attached _build_commit_task_map walks _auto_tag_commits per entry and returns {by_task / by_commit / skipped_no_files}. Entries without files surface separately. Drift-resilient (no module-level state). Side gain — discovered + fixed a git log --since=YYYY-MM-DD parse-quirk that drops same-day commits; new _normalize_since helper appends 00:00:00 time component when input is bare date. 6 paired tests across 2 classes — _build_commit_task_map unit (3 — bidirectional / skipped / one-commit-multi-tasks) + tree CLI integration (3 — happy path / missing file / default-since-from-handoff-date). Live smoke against this sessions handoff yaml returns 47 commits attributed to 2 tasks (the 2 description-refresh commits). 83 handoff suite tests green (was 77). |
| 2026-05-18 | feat | +~250 | handoff cost subcommand — token-budget estimator + 21-item handoff-automation brainstorm. Per user "more automation with scripts for token savings and optimizations / context budget / mental load". Brainstorm at docs/superpowers/2026-05-18-kaizen-md-9f4c8972/brainstorm-handoff-automation.md ranks 21 ideas by impact x effort — top item handoff cost (score 25) shipped this commit. handoff cost --file <yaml> [--json] reports total size_bytes + approx_tokens (chars/4 kaizen convention) + per-section breakdown + over_threshold flag + recommendation. Budget env-overridable via KAIZEN_HANDOFF_COST_TOKEN_BUDGET (default 2000). New helpers _approx_tokens (ceiling division so small strings dont under-report) + _cost_threshold (drift-resilient re-read per call). Recommendation when over budget — scaffold fresh + link via parent_handoff so chain stays intact without bloat (pairs with prior ea2a4d5 parent_handoff field). 13 paired tests across 4 classes — _approx_tokens (3 — empty / 7-chars / 400-chars) + _compute_costs (3 — total / breakdown / pct-sum) + threshold (4 — default / env-override / under / over) + CLI (3 — json / human / missing-file). Live smoke against this sessions handoff yaml — 5538 bytes / ~1368 tokens / done_this_session 65.9% of total (which is the natural growth indicator). 96 handoff suite tests green (was 83). |
| 2026-05-18 | feat | +~70 | handoff smart since-cutoff — brainstorm #2 (handoff tree default). When --since omitted _smart_since walks priority — parent_handoff session_meta.handoff_generated_at (exact UTC ts) → current handoff date field (broader day-bucket) → None (caller default). Graceful on missing parent file / missing parent handoff_generated_at — falls back through priorities without raising. Plumbed into _cmd_tree as `args.since or _smart_since(text) or "1 week ago"`. 6 paired tests — uses-parent-ts / falls-back-to-date / both-absent-returns-none / missing-parent-file / parent-without-handoff-generated-at / tree CLI integration. Live smoke against this sessions handoff yaml — before smart-since handed back 47 commits since 2026-05-18 00:00:00 (day-bucket noise); after returns just the 3 commits since parent finalized at 2026-05-18T20:02:59Z. 102 handoff suite tests green (was 96). Closes brainstorm #2 — pairs with prior parent_handoff field (ea2a4d5) to close the chain-aware default-loop. |
| 2026-05-18 | feat | +~150 | handoff auto-bridge — brainstorm #4 (auto-finalize integrates bridge). Before agents had to remember to separately run bridge --apply after auto-finalize to capture decisions / findings / worked / failed into the brain — easily skipped mental-load step. Now folded into the finalize call via opt-in --auto-bridge flag OR KAIZEN_HANDOFF_AUTO_BRIDGE=1 env. Gating layers — (1) flag/env required to opt in (never default; bridging writes to brain) (2) outcome=SUCCEEDED only (high-confidence durables; PARTIAL_PLUS / PARTIAL_MINUS / FAILED skip with reason in response) (3) subprocess failures NEVER fail the parent finalize (errors counted in failed; finalize continues). New _maybe_auto_bridge(fp outcome force) returns None when not triggered / {skipped reason} when gated out / {applied failed total rows} on real run. Plumbed into _cmd_auto_finalize after indexing succeeds; result added under data.auto_bridge in the envelope. 8 paired tests across 2 classes — _maybe_auto_bridge gating (5 — no-flag-no-env / flag+SUCCEEDED / env+SUCCEEDED / flag+PARTIAL skipped / env+FAILED skipped) + auto-finalize CLI integration (3 — without-flag-no-key / with-flag-includes-result / flag+wrong-outcome=skipped). Live smoke against this sessions handoff yaml — outcome=SUCCEEDED + --auto-bridge runs cleanly (0 candidates because decisions/findings/worked/failed all empty — the system correctly reports no-op). 125 handoff suite tests green (was 102). |
| 2026-05-18 | feat | +~270 | handoff trio — structured next[] + diff + tasks. (1) structured next[] (original-6 #2) — append CLI gains --why / --blocker flags; when set on section=next promote plain --entry to {item / why / blocker?} dict. Reuses existing _format_entry_yaml dict-handling so the new shape renders correctly; back-compat — plain --entry stays string. --why ignored on other sections (KISS). (2) handoff diff (brainstorm #5) — section-by-section delta between 2 handoff yamls. _compute_diff is a pure function returning per_section dict with status in identical / added / removed / modified plus counts rollup. CLI emits human-readable change-list (skipping identical) or full JSON. Comparison uses normalized json.dumps so dict-key-order quirks dont trigger false-positives. (3) handoff tasks (brainstorm #6) — emit TaskCreate-ready JSON per next[] item. _extract_tasks_from_next normalizes both plain-string entries (becomes {subject}) and structured dicts (becomes {subject / why? / blocker?}). CLI surfaces ready-to-mint output so resume agents skip the parse-then-mint loop. 18 paired tests across 3 modules — structured next (5 — plain-stays-string / why / blocker / both / other-section-ignored) + diff (8 — identical / modified / added / removed / counts / json / human / missing-file) + tasks (5 — both-forms / empty / json / human / missing-file). 143 handoff suite tests green (was 125). |
| 2026-05-18 | feat | +~150 | handoff re-mine — closes original-6 items #1 + #5 (re-mine + skill-frame capture). Both ride on _session_jsonl.mine_session which already returns completed_tasks AND skills_used — KISS to combine. Subcommand reads handoff yaml → looks up session_meta.cc_session_jsonl → runs mine_session → diffs vs existing done_this_session → reports new_completed_tasks + skills_used (skill-frame capture). Dry-run by default; --apply uses the existing _append_to_list_section helper to write new entries with task + empty files. _compute_remine is pure (only side-effect — reading the jsonl). 6 paired tests across 2 classes — _compute_remine unit (3 — new-tasks-excludes-already-in-done / no-done-returns-all / already-complete-returns-empty) + remine CLI integration (3 — dry-run-reports-without-mutating / apply-appends / missing-jsonl-link-fails). Live smoke against this sessions handoff yaml — 24/24 tasks already captured (handoff up-to-date) + skills used surfaced — kaizen:handoff and kaizen:plugin-development. 149 handoff suite tests green (was 143). |
| 2026-05-18 | feat | +~220 | docs/superpowers state tracker — kaizen-bundle state + tasks subcommands. Per user 2026-05-18 sid 32bad1f7 — "work on the new docs system have it generate and maintain a state file + task list tracker with metadata for /docs/superpowers/". Extends existing kaizen-bundle (DRY rather than minting a sibling tool). New _classify_kind maps filename prefix to plan / spec / brainstorm / audit / notes / README / data / other. New _extract_status walks first 50 lines for markers (case-insensitive) — SUPERSEDED / DEFERRED / COMPLETE / SHIPPED / DONE / **State** shipped|in-progress|draft|wip — first match wins. _scan_state walks the superpowers root → per-bundle + per-file metadata + aggregate totals. state subcommand dry-runs by default; --apply writes .state.json under the root. tasks subcommand task-list view of non-shipped items; filterable by --bundle and --status; JSON or human output with status marks (✓ shipped / ☐ draft / ▷ in-progress / ⏸ deferred / ✗ superseded / ? unknown). 25 paired tests across 5 classes — _classify_kind (8) + _extract_status (7) + _scan_state (4) + state CLI (3) + tasks CLI (3). Live first-scan of real docs/superpowers/ — 7 bundles / 33 files / 19 unknowns / 3 shipped / 3 superseded / 6 deferred / 2 draft / 30 pending tasks. .state.json initial snapshot written 9814 bytes. Test baseline +25. |
| 2026-05-18 | feat | +~80 | docs/superpowers state — trace unknown bucket + 4 refinements. First scan flagged 19/33 files as unknown — too noisy. Traced 4 root causes — (1) READMEs (7) are folder-descriptions not work items but were flagged as work / (2) detector missed `**Status:**` (only matched `**State:**`) so plan-menu-consolidation `**Status:** drafting` was unknown despite being a real draft / (3) tasks default view didnt skip READMEs / (4) my 2 newly-shipped files (audit-heartbeat + brainstorm-handoff) lacked explicit Status markers. Fixes — new _extract_status_for_file wrapper auto-classifies README.md as reference status regardless of body; _STATUS_PATTERNS gains **Status:** alias (plus drafting variant + PARKED alias for deferred); tasks command skip_statuses set widens to {shipped reference}. Boy-scout — added **Status:** SHIPPED markers to my 2 files plus plan-path-restructure-v1.39.0.md (all 5 phases complete). 5 new paired tests (status-alias-for-state / alias-in-progress / readme-with-no-content-reference / other-files-use-content / tasks-default-excludes-references). Result after rescan — unknown 19 to 7 / shipped 3 to 6 / reference 0 to 7 / pending tasks 30 to 20. 30 paired tests green. |
| 2026-05-18 | refactor | +~30 -~30 | docs/superpowers migrate — folder relocated to .kaizen/superpowers + default updated. Per user 2026-05-18 sid 32bad1f7 — "how do we keep it in .kaizen" + "no one is using this plugin but me". Single-user means we can change the default freely. (1) shell mv of docs/superpowers/* (incl .git nested-repo + .state.json) into .kaizen/superpowers (existing specs/ subdir coexists alongside bundles). (2) _superpowers_dir() default changed from cwd/docs/superpowers to cwd/.kaizen/superpowers; per-project semantics preserved via KAIZEN_SUPERPOWERS_DIR env override unchanged. (3) Docstring/comment sweep across 16 files — all literal docs/superpowers references replaced with .kaizen/superpowers (no functional code changed; tests sandbox via env so they unaffected). docs/ now removed from disk (empty). Nested git at .kaizen/superpowers/.git remains independent + functional (master at 8b335a7 — verified). Live state scan against new path returns 8 bundles / 35 files (was 7/33 — picks up existing .kaizen/superpowers/specs/ subdir as the +1 bundle with 2 old design docs). 48 superpower-related tests still green (test_superpower_bundle + scan + state + observer_capture). Removes the convention drift where both docs/ AND .kaizen/ held bundle-adjacent content — now single canonical location. |
| 2026-05-18 | feat | +~400 | kaizen-test unified harness — collapses 5 historical test entry points into one CLI. Audit revealed — parallel runner used all 32 cores correctly (wall bounded by slowest single file test_learning_log = 34s of 36s wall); 5 pytest-style files needed a runtime fallback hack; legacy _tests.py + bash test_plugin_root.sh + unittest discover + pytest all separate entry points. New kaizen-test consolidates — auto style-detection via _test_runner.classify_style (pure — .sh / unittest.TestCase parent / bare TestX / module-level def test_X), per-style dispatch via _test_runner.dispatch_cmd (pure), parallel fan-out at nproc concurrency, bench subcommand surfaces per-file timing (sorted slowest-first; total sequential ceiling reported). Onion-DDD layered — pure domain in _test_runner.py; CLI adapter in test_runner.py; new skill body in skills/test-runner/SKILL.md. 15 paired tests across 4 classes — classify_style (6 — unittest / pytest-class / pytest-fn / bash / both-prefers-unittest / unknown-fallback) + dispatch_cmd (3 — unittest-module / pytest-path / bash-shell) + bench_report (3 — sort-desc / top-n-caps / totals-included) + CLI integration (3 — default-run / json-envelope / bench-sorts-by-time). Live smoke against real suite — 288 passed / 1 fail (intermittent test_dxm) / 36s wall / bench surfaces test_learning_log as the dominator. KISS — auto-style means no manual flags; SOLID — new runner styles plug in via 3 pure functions; DRY — reuses existing _affected_tests helper; TDD — RED then GREEN strictly. Bin wrapper kaizen-test created with consolidated-cli-parent shape; plugin.json gets 2 perms. Older run_tests_parallel.py stays in place (gate still calls it; Phase 2 migrates). |
| 2026-05-19 | fix | +~50 | handoff _auto_tag_commits — git --since bare YYYY-MM-DD bug fix. Surfaced when running the kaizen-tests harness at full concurrency — test_handoff_commit_autotag::test_returns_commits_chronological failed because `git log --since=1970-01-01` (and `--since=YYYY-MM-DD 00:00:00`) silently returns empty regardless of date. Only ISO 8601 with explicit Z timezone (YYYY-MM-DDTHH:MM:SSZ) parses reliably. _normalize_since updated — bare YYYY-MM-DD promoted to YYYY-MM-DDT00:00:00Z (was space-form). _auto_tag_commits gains _normalize_since call at the source so all callers benefit (DRY) — fixes scaffold commit auto-tagging too. Also restored run_tests_parallel _resolve_concurrency to os.cpu_count() autodetection — earlier cap-at-4 was misdiagnosis of this real bug. test_handoff_tree default-since assertion updated to new ISO form. 38 tests across affected modules green (handoff_commit_autotag / tree / smart_since / remine / tests_run). |
| 2026-05-19 | perf | +~30 -~15 | test_learning_log dominator fix — 45x speedup on that test / 3.6x speedup on full parallel suite. Earlier kaizen-tests bench surfaced test_learning_log as the parallel-suite dominator (34s of 36s wall). Root cause — test_append_cost_constant made 1001 subprocess calls to seed the log before measuring one final append for the append-only invariant. Each subprocess call = ~30ms python cold-start; 1001 calls = ~30s minimum. Fix preserves the invariant proof — seed 1000 fake-but-realistic JSONL lines via direct file open(w) + write (lookalike entry shape with id / ts / promotion_status / category / problem / solution / pattern); only the FINAL append (the one being measured) stays a CLI subprocess (thats the actual code path being asserted append-only). 11 tests still green — 31.7s to 0.7s = 45x speedup. Full suite via kaizen-tests — 36s to 10s wall (parallel ceiling now bounded by next-slowest test_ci_gate at 6.5s; was bounded by test_learning_log at 34s). Demonstrates the value of kaizen-tests bench subcommand — surfaced the dominator instantly + the fix was a 2-line code swap once located. Sequential ceiling drops 221s to 194s (-12.5%). |
| 2026-05-19 | perf | +~25 -~10 | next dominators — test_gold_mine_trace 5.2s to 0s + test_ci_gate 6.4s to 4.5s. Continued the dominator-hunt with the harness bench surface. (1) test_gold_mine_trace — root cause was unconditional Ollama call (urllib.urlopen to localhost:11434 = ~1s per test x 4 tests). The KAIZEN_GOLD_MINE_ENABLE env knob was documented but never wired in run_mine. Fix wired the env gate so the Ollama import + score_hint loop only runs when explicitly enabled; test setUp now pops the env (was setting to 1 decoratively). 9 tests still green; 5.25s to ~0s = -100%. (2) test_ci_gate — TestDefaultIsSyntaxOnly had 3 tests each calling _run with same args (~750ms each). DRY refactor — setUpClass calls _run once cls._rc cls._out cls._err cls._elapsed cached; tests share. 6.4s to 4.5s = -30%. New parallel suite wall ~10s; new dominator test_self_audit 6.25s (already optimized with setUpClass on slowest class — remaining cost is genuine self-audit work). test_dxm 5.3s next-slowest but sleep calls are functionally required for time-window assertions. |
| 2026-05-19 | fix | +~65 -~10 | test_loop_state + test_loop_hardening race fix. Both _init_loop and _init_ledger_loop helpers called setup-ralph-loop.sh — that script spawns bash plus 2x python3-heredoc per call. Under 32-process parallel load the heredoc-execv overhead became a contention point — rc=2 intermittent failures. Fix replaced subprocess invocation with direct file-write of the state.md shape — preserves exact frontmatter (active / iteration / session_id / last_turn_id / max_iterations / completion_promise / started_at) + JSON body (pending and completed). What was tested — the loop_state Python API (add_item / list_pending / complete / status / next_pending / fail_run). What was assumed — a well-formed state file. Direct write satisfies the assumption without invoking the shell harness — the shell harness has its own coverage via TestBinWrapper + integration tests. Result — both files now pass solo AND under load 3x in a row; test_loop_state 0 fails across 3 parallel runs; test_loop_hardening 0 fails across 3 parallel runs. Whole suite — 287-289 passed, 0-1 transient flake (test_handoff_commit_autotag intermittent on 2 of 6 runs but did not repro after the loop fixes — may have been collateral damage from the heavy loop subprocess pressure). Sequential ceiling 187s to about 180s. |
| 2026-05-19 | feat | +~25 | iron-law +1 — flake-audit-load-before-logic + brain Note codifies the discovery. Per session 2026-05-19 — the test_handoff_commit_autotag intermittent was load-induced (subprocess pressure from test_loop_state + test_loop_hardening setUps invoking setup-ralph-loop.sh = bash + 2x python3-heredoc x ~88 spawns/suite). Fixing the loop tests dropped the suite pressure; the unrelated handoff flake vanished — 15 of 15 clean runs. New iron-law (soft / manual) codifies the diagnostic ladder — (1) solo run (2) --pattern run (3) kaizen-tests bench for >5s outliers (4) audit setUps for subprocess loops where the script behavior is not what is being tested (5) only THEN debug the failing test logic. Yaml grew 32 to 33 laws (19 auto + 14 manual). _NEW_LAW_IDS set in test_iron_laws_session_additions extended. references regenerated via kaizen-iron-laws render. Companion brain Note at ~/.claude/.kaizen/brain/Notes/pref-test-flake-load-vs-logic.md with full evidence + diagnostic ladder + related cross-links (pref-kaizen-metrics-self-correction + pref-onion-architecture-strict). |
| 2026-05-19 | feat | +~270 | brain blocks + show + edit — zero-roundtrip memory edits via heading-path. Per user 2026-05-19 — "index token efficient approach no roundtrips." New _brain_blocks.py pure layer (parse_blocks / extract_block / replace_block / append_to_list_block) — Markdown headings auto-define addressable blocks via heading-path. New brain.py subcommands — (1) blocks --file X --json lists every block with path + level + start + end lines (2) show --file X --block PATH extracts ONE block (heading included); no whole-file Read (3) edit --file X --block PATH --replace BODY (or --append LINE) does atomic in-place block edit via tempfile+rename. KISS — no HTML comment anchors no JSON sidecar no auto-rebuilds. Heading-path uses existing markdown structure. SOLID + DIP — pure layer has zero I/O so it composes anywhere (CLI / MCP / future hooks). 16 paired tests across 5 classes — parse_blocks (4) + extract_block (3) + replace_block (2) + append_to_list_block (2) + CLI integration (5). Live smoke against real Persona.md — 5 addressable blocks listed (Mission / Directives / Top Beliefs / Evidence Log) + show Top Beliefs returns only those 14 lines (~600 bytes) vs whole file ~5KB. 8.7x token saving per read. Edit similarly avoids the Read-then-Edit roundtrip pattern. |
| 2026-05-19 | feat | +~210 | pretooluse-brain-redirect hook — automate the brain block routing (no agent-skill reliance). Per user 2026-05-19 — "automate it dont rely on skills". When the agent calls Read on a Markdown file under .kaizen/brain/ OR <project>/memory/ the PreToolUse hook intercepts the event JSON on stdin and emits hookSpecificOutput.additionalContext containing — (1) the addressable heading-paths of the file with line ranges (2) the precise kaizen-brain show / edit / append commands to use instead of Read+Edit. Never blocks (advisory). Bypass via KAIZEN_BRAIN_REDIRECT_DISABLE=1. Pure layer should_redirect / build_hint in _brain_redirect.py composes _brain_blocks parsing. Shell wrapper pretooluse-brain-redirect.sh mirrors the bash-gate / roundtrip-detect pattern (trace via _trace.sh exit 0 always). 13 paired tests across 4 classes — should_redirect (7 — brain markdown / brain note / project memory / non-markdown / non-brain / non-Read / missing-path) + build_hint (3 — lists-blocks-and-commands / no-headings-still-suggests / missing-file-no-crash) + disable env (1) + shell hook end-to-end (2 — brain-path emits / non-brain emits empty). Live smoke against real ~/.claude/.kaizen/brain/Persona.md returns the 5 addressable blocks + the 3 precise commands; non-brain path returns empty. Wires PreToolUse matcher Read in hooks.json; plugin.json gets 2 perms (sh + py). Plus boy-scout — brain SKILL.md description expanded with trigger phrases for skill-suggest routing; brain.md slash arg-hint lists the new blocks/show/edit verbs. |
| 2026-05-19 | feat | +~140 | daemon brain-audit job + _daemon_jobs.py extract (Phase 1/7 of daemon-memory-arc). New _daemon_jobs.py is the pure-function job registry — each run_<job>(state) returns (ok msg action). brain-audit job calls brain_audit.py --apply via subprocess; honors KAIZEN_DAEMON_BRAIN_AUDIT_DISABLE (drift-resilient env read). Wired into daemon.py::tick() after the existing index-refresh block; logged + counted in state[actions]. TDD red→green — 3 unit tests in test_daemon_jobs.py (disabled / enabled / nonzero-rc) + 1 integration test in test_daemon_brain_wire.py confirms the action lands in state[actions] when tick runs. Plan at .kaizen/superpowers/2026-05-19-kaizen-md-c35624c1/plan-daemon-memory-arc.md drives the 7-phase arc — brain-index drift-gated / brain-promote 24h-throttled / brain-evolve opt-in daily / gold-promote weekly / test-dominator chase / CLAUDE.md→brain SSOT migration. |
| 2026-05-19 | feat | +~75 | daemon brain-index job — drift-gated reindex (Phase 2/7). run_brain_index(state) hashes Notes/ filenames+sizes+mtimes (cheap stat-only sig), compares against state[brain_notes_hash] from prior tick, only shells out to brain_index.py index when drift detected. Same shape as the existing source-vs-cache hash check. Failed reindex keeps old hash so next tick retries. Honors KAIZEN_DAEMON_BRAIN_INDEX_DISABLE. 4 new unit tests (disabled / no-drift / drift-triggers-reindex / failed-reindex-keeps-old-hash) + integration test extended to verify both brain-audit AND brain-index land in state[actions]. 8 daemon tests / 0 fails. Wired after brain-audit in tick(). |
| 2026-05-19 | feat | +~90 | daemon brain-promote job — 24h-throttled inbox drain (Phase 3/7). New run_brain_promote(state) shells out to brain_promote.py --apply with throttle policy state[last_run_at][brain-promote] ≥86400s. Failed runs don't stamp — next tick retries. Helpers _throttled / _stamp_last_run added to _daemon_jobs.py for reuse by Phase 4 (evolve) + Phase 5 (gold). KAIZEN_DAEMON_BRAIN_PROMOTE_DISABLE knob. Drains the 1236-draft Inbox backlog organically over time (~30 ticks/day at default cron cadence = 1 promote attempt per day). 5 new unit tests (disabled / throttled / expired / first-invocation / failed-no-stamp) + integration test extended for brain-promote. 13 daemon tests / 0 fails. Wired in tick() after brain-index. |
| 2026-05-19 | feat | +~90 | daemon brain-promote job — 24h-throttled inbox drain (Phase 3/7). New run_brain_promote(state) shells out to brain_promote.py --apply with throttle policy state[last_run_at][brain-promote] ≥86400s. Failed runs don't stamp — next tick retries. Helpers _throttled / _stamp_last_run added to _daemon_jobs.py for reuse by Phase 4 (evolve) + Phase 5 (gold). KAIZEN_DAEMON_BRAIN_PROMOTE_DISABLE knob. Drains the 1236-draft Inbox backlog organically over time (~30 ticks/day at default cron cadence = 1 promote attempt per day). 5 new unit tests (disabled / throttled / expired / first-invocation / failed-no-stamp) + integration test extended for brain-promote. 13 daemon tests / 0 fails. Wired in tick() after brain-index. |
| 2026-05-19 | feat | +~75 | daemon brain-evolve job — opt-in daily LLM consolidation (Phase 4/7). run_brain_evolve(state) shells out to brain_evolve.py once per calendar day (state[last_evolve_date] = ISO date). Default OFF — set KAIZEN_DAEMON_BRAIN_EVOLVE_ENABLE=1 to opt in. Expensive (LLM round-trips for consolidation/reflection across entire Notes/ corpus) — opt-in inverse of the disable pattern. Failed run keeps old date so next tick retries. 4 new unit tests (default-off / enabled-runs / already-ran-today / failed-keeps-old-date). Integration test extended for brain-evolve. 17 daemon tests / 0 fails / 293 suite-wide. Wired after brain-promote in tick(). |
| 2026-05-19 | feat | +~70 | daemon gold-mine job — weekly trace-mining pass (Phase 5/7). run_gold_mine(state) shells out to gold.py mine --json with 7-day throttle policy. Renamed from planned gold-promote (gold.py promote is per-pattern manual; gold.py mine is the canonical automation surface — wraps gold_mine.run_mine which parses trace log, dedupes templates, emits proposals.jsonl and auto-captures patterns ≥ AUTO_CAPTURE_THRESHOLD). Two-level env knob — KAIZEN_DAEMON_GOLD_MINE_DISABLE turns off the daemon job entirely; KAIZEN_GOLD_MINE_ENABLE (read inside run_mine) gates LLM scoring (mining still runs without it, just emits unscored proposals). 3 new unit tests (disabled / throttled / expired). Integration test extended for gold-mine. 20 daemon tests / 0 fails / 293 suite-wide / 10.2s wall. tick() now wires all 5 brain/gold jobs after the original 5 jobs (cache-hash / remote-sha / hygiene / index / brain-audit-index-promote-evolve-gold). |
| 2026-05-19 | perf | +~25 -~5 | Phase 6/7 test-suite dominator chase — suite wall 10.2s to 9.1s (-11%). Two dominators hit: (1) test_token_bloat 4.05s to 2.98s solo — root cause was time.sleep(1.1) in test_two_scans_produce_two_archives ensuring second-precision archive filenames don't collide. Real fix is upstream: new _uniq_archive_path() helper in token_bloat.py appends -N suffix on collision so two sub-second scans don't overwrite each other (was a silent data-loss bug under cron-driven scans). Test drops the sleep entirely. (2) test_self_audit 5.65s to 4.64s solo (-17%) — TestEndToEndAudit ran run_audit twice (shared no_write=True + standalone no_write=False for write-path verification). Collapsed to a single no_write=False call in setUpClass; tearDownClass cleans up the report. test_full_audit_no_write renamed to test_full_audit_result_shape since both paths return identical dict shape. Suite ceiling now 5.6s self_audit (next-irreducible). 293 / 0 fails. |
| 2026-05-19 | refactor | -209 | Phase 7/7 — CLAUDE.md thinned to brain-loader pointer (project: 406 to 197 lines, -51%). Companion: user-global CLAUDE.md 330 to 107 lines (-68%), 13 new brain Notes capture what was previously inline (pref-gab-discipline / pref-memory-two-tier-model / pref-memory-hooks-in-git-flow / pref-git-failure-recovery / pref-claude-md-is-rulebook-not-state / pref-mermaid-lexer-gotchas / pref-investigate-before-answering / pref-default-to-action / pref-subagent-dispatch-safe-defaults / pref-cross-platform-no-gnu-only / pref-kaizen-consolidation-patterns / pref-coverage-axis-naming / pref-jsonl-indexed-deliverables), Persona Top Beliefs expanded 10 to 23 entries (auto-loaded via SessionStart), .kaizen/superpowers/architecture-snapshot.md absorbs the drift-prone cluster tables. Brain is now SSOT for durable rules; CLAUDE.md is the loader handle. Per user 2026-05-19 — retire reliance on CLAUDE.md we use the memory+brain system with the custom backend. Backup at .kaizen/superpowers/2026-05-19-kaizen-md-c35624c1/claude-md-backup-2026-05-19/. Suite 293 / 0 fails / 9.2s wall. |
| 2026-05-19 | fix | +~80 -~3 | brain_audit broken-YAML writer — _yaml_safe() helper + 109 retroactive repairs (Phase 8). Inbox drafts were corrupting at ~6% rate (77 of 1334 by audit, 109 actually repaired by the fixer pass). Root cause: f-string inlined raw text values into YAML frontmatter without escaping. A newline in c[text] broke the field; a colon parsed as nested mapping; an embedded quote silently truncated. Fix: new _yaml_safe(text, maxlen) collapses whitespace to single space, truncates, doubles single quotes (YAML 1.2 spec) — safe to embed inside 'single-quoted' scalars. Writer now emits name: 'X' and description: 'Y' uniformly. 4 new TDD tests (strip newlines / escape quotes / leading special chars / end-to-end round-trip through PyYAML). 14 brain_audit tests / 0 fails / 293 suite / 9.1s wall. The Inbox retroactive pass found that some old drafts had broken multi-line fields too — flagged for promote-pass review. |
| 2026-05-19 | feat | +~190 | kaizen-better-memory regen — auto-memory MEMORY.md index management (Phase 9). New bin/kaizen-better-memory + skills/workflow/scripts/better_memory.py. Subcommands — regen [--dir PATH] [--json] rebuilds MEMORY.md from project_*.md + feedback_*.md frontmatter; path [--dir PATH] prints the resolved auto-memory dir. Per Claude Code doc — auto-loads first 200 lines / 25KB at session start; orphan captured entries (not in MEMORY.md) only load on-demand. Kept the project's 42 captures in sync by regen; fixed 109 broken-YAML drafts in brain Inbox earlier (Phase 8). Resolves to git repo root (Claude Code's convention — all worktrees + subdirs share one auto-memory dir); falls back to cwd outside git. Frontmatter normalizer handles 3 broken cases — properly-quoted (json.loads), single-quoted with YAML doubling, unterminated double-quoted (the real-world bug from Claude Code's older auto-capture). 6 TDD tests pass (regen-empty / regen-entries / skip-self / normalize-broken-YAML / git-root-resolution / non-git-fallback). plugin.json permissions updated for the new script + bin. 294 / 0 fails / 9.1s wall. |
| 2026-05-19 | feat | +~95 | brain blocks full-coverage on default-memory + brain (Phase 10+11). (Phase 10) _resolve_memory_file() + _file_not_found_msg() in brain.py — --file Persona.md auto-resolves to ~/.claude/.kaizen/brain/Persona.md, --file MEMORY.md to the project's auto-memory dir, --file Notes/X.md to brain/Notes/X.md. Literal paths still win. Not-found error lists all attempted paths so the user can diagnose. 5 new TDD tests (persona / memory / notes-subpath / literal-wins / unknown-clear-error) sandboxed via KAIZEN_BRAIN_DIR + KAIZEN_BETTER_MEMORY_DIR. (Phase 11) Extended pretooluse-brain-redirect.py to cover CLAUDE.md + CLAUDE.local.md (basename match) — Read on either now nudges to kaizen-brain show. Test_read_on_other_path_skips updated (CLAUDE.md is the rulebook; nudging is the point). 4 new redirect tests for project/user-global/local CLAUDE.md + README.md still-skips. 21 block + 16 redirect tests / 0 fails / 294 suite / 9.2s wall. |
| 2026-05-19 | feat | +~210 | auto_load — daemon-built CLAUDE.md companion (Phase 12). Exploits Claude Code's native @path import mechanism: ~/.claude/CLAUDE.md now imports @~/.claude/.kaizen/auto-load.md, which the daemon rebuilds every tick from brain Persona. Pure layer: parse_persona(text) extracts Directives + Top Beliefs (regex over ## sections); build_auto_load(persona, top_n, byte_budget) renders markdown with <HARD-GATE> wrap for must-not rules (Never / no deletions / mandatory / etc.) + soft Directives + ranked Top Beliefs. Token-budget capped at 5KB by default (truncates to nearest newline). Adapter: run_auto_load(state) daemon job (KAIZEN_DAEMON_AUTO_LOAD_DISABLE env knob; KAIZEN_AUTO_LOAD_PATH override). New bin/kaizen-auto-load wraps the script for ad-hoc / debug invocation (--print to preview). Wired into daemon.py::tick() as 11th job after gold-mine. Live smoke against real Persona produces 1966B output covering 3 hard gates + 8 directives + 5 top beliefs. 10 TDD tests pass (parse-directives / parse-beliefs / parse-no-beliefs / hard-gate-wrap / top-n-cap / byte-budget-truncate / do-not-edit-warning / starts-with-heading / disabled / writes-when-enabled). plugin.json permissions for script + bin. 295 / 0 fails / 9.2s wall. |
| 2026-05-19 | refactor | +~25 -~20 | _daemon_jobs.run_brain_index migrated to _index_kit.daemon_drift_job composition (Phase 4.A follow-up). Replaces the inline disable-env + drift-check + subprocess + state-stamp pattern with a factory call — preserves state key brain_notes_hash for back-compat with existing daemon state files; lambdas late-bind _brain_notes_hash and _regen_brain_index_subprocess so test monkey-patches still apply. Extracts _regen_brain_index_subprocess as a named module-level helper that raises RuntimeError on non-zero rc (so the factory exception handler keeps the hash un-stamped on failure). Proves the Phase 4.A contract works on one indexer; template for the other 5 (memory-sync / trace / onboard / knowledge / claude-docs / scrape). All 4 brain-index unit tests + 312-test suite still green / 9.7s wall. iron-laws clean (staged scope). |
| 2026-05-19 | refactor | +~45 -~45 | brain_index.py → build_index.py rename — upstream attribution alignment. Per ATTRIBUTIONS.md the Second Brain feature was originally based on Gabi Fratica's remember-md/remember plugin (MIT, scripts/build-index.js). When we ported to Python we added a brain_ prefix; the rename drops it to match upstream. Touched: script file (git mv brain_index.py → build_index.py), test file (git mv test_brain_index.py → test_build_index.py), argparse prog kaizen-brain-index → kaizen-build-index, Python imports (brain_mcp.py / test_build_index.py), subprocess paths (_daemon_jobs.py / brain.py consolidated dispatcher), docstring CLI examples + iron-law _wrapper_name example + plugin-development SKILL.md mapping table + brain/SKILL.md + customization.md + coverage SKILL/commands + envelope-retrofit + audit-pipeline / feature-shape / iron-laws yaml exemplars, plugin.json + commands/brain.md permission paths, test_brain_hooks/test_brain_command script-list assertions. Preserved (semantic API surface): KAIZEN_BRAIN_INDEX_* env vars, daemon action-key "brain-index", MCP tool names brain_index_build/brain_index_stats, state key brain_notes_hash, Python function names run_brain_index/_regen_brain_index_subprocess (internal). User-facing CLI verb kaizen-brain index unchanged (breaking otherwise). 312 / 0 fails / 9.7s wall. iron-laws clean (staged scope). |
| 2026-05-19 | fix | +~12 -~3 | daemon brain-index --json bug fix — pre-existing argparse error since wire-up, surfaced during verify-handoff pass. _daemon_jobs._regen_brain_index_subprocess shelled out to build_index.py index --json, but the index subparser does NOT declare --json (the script always prints JSON). Every daemon tick → argparse rc=2 → RuntimeError raised → state hash never stamped → indexer-stale brain-drift gate triggers OK on first tick but subsequent ticks have no prior state to compare against. Tests passed because patch("subprocess.run") mocks the call entirely. Fix drops --json (single-line subprocess arg removal). Regression test added: test_drift_triggers_reindex_and_updates_hash now asserts "--json" NOT in subprocess call args. Audited the other 4 daemon subprocess calls (brain_audit / brain_promote / brain_evolve / gold.py mine) — all 4 correctly accept --json. Bug isolated to brain-index. 312 / 0 fails / 9.9s wall. |
| 2026-05-19 | refactor | +~17 -~22 | _daemon_jobs.run_memory_sync migrated to _index_kit.daemon_drift_job composition (Phase 2 extension of A). Same shape as brain-index — extracts _regen_memory_sync helper that raises on failure (factory leaves hash un-stamped → next tick retries), then replaces inline disable-env + drift-check + try/except + state-stamp with one factory call. Lambdas late-bind _memory_dir_hash + _regen_memory_sync so test monkey-patches still apply. Preserves state key memory_dir_hash + disable env KAIZEN_DAEMON_MEMORY_SYNC_DISABLE for back-compat. Loses the n-entry count from the success msg (factory uses generic "regen ok (hash X → Y)" — tests don't assert the count, only the "no drift" substring on no-drift path). Both drift-gated daemon jobs (brain-index + memory-sync) now compose the same _index_kit primitive — canonical pattern proven. 312 / 0 fails / 9.8s wall. iron-laws clean (staged scope). |
| 2026-05-19 | refactor | +~61 -~70 | build_index.py migrated to _indexer_cli.IndexerCLI subclass (CON-2b — alignment with the 6 other indexers). Discovered during the consolidation sweep that _indexer_cli.IndexerCLI already exists as a shared CLI scaffolding base and 6 of 7 indexers (onboard / knowledge / claude_docs / scrape / trace / loc) already use it; only build_index.py (the renamed brain indexer) had its own bespoke argparse + _cmd_* dispatch. Migration: drop 70 LOC of _cmd_* wrappers + argparse setup, replace with 60 LOC BuildIndexCLI(IndexerCLI) subclass. Preserved exact CLI surface — 6 subcommands (index/search/stats/get/path/clear), no reindex (intentional — no prior contract for it), --yes guard on clear, {db_path: ...} JSON shape on path, {error: ...} + rc=1 on missing get. Module-level do_* helpers unchanged (tests call them directly). 13 build_index tests + 312 suite all green / 9.8s wall. iron-laws clean. All 7 indexers now compose the same CLI framework — convention restored. |
| 2026-05-19 | refactor | +~10 -~10 | scripts/*.js (10 upstream Gabi Fratica files) relocated to scripts/upstream/ (BIG-REFACTOR step 1/3). Preserves attribution clearly + clears top-level scripts/ for the imminent Python migration (step 3). Files moved: append-evidence / build-context / build-index / config / evolution-log / extract / promote / schema / session_start / user_prompt. Path refs updated in 12 files: plugin.json (permissions), hooks/hooks.json, ATTRIBUTIONS.md, references/structure.md, skills/{evolve,process,remember,status}/SKILL.md + reference.md, skills/workflow/scripts/health.sh + surface.py. CHANGELOG left as-is (historical references). 312 / 0 fails / 9.9s wall. iron-laws clean. |
| 2026-05-19 | refactor | +~430 -~750 | scripts/upstream/*.js (10 Gabi Fratica Node.js sources) FULLY RETIRED — dedup against existing kaizen Python equivalents (BIG-REFACTOR step 1.5/3). Per ATTRIBUTIONS.md the JS files were SUPPOSED to be "no longer present" post v1.38.0 Python rewrite but had remained as vendored attribution. User-authorized dedup. Ported 4 active scripts/upstream/{session_start, user_prompt, build_context, evolution_log}.js → plugins/kaizen/scripts/*.py (the 4 with no kaizen native equivalent; 2 wired into SessionStart + UserPromptSubmit hooks). 22 TDD tests covering parse_sections / merge_sections / render_sections / truncate_evidence_log / session_start.main + user_prompt.main flows. Wired hooks.json: `node scripts/upstream/X.js` → `python3 scripts/X.py`. plugin.json permission allow-list: `Bash(node scripts/*.js:*)` → `Bash(python3 scripts/*.py:*)`. Bulk-replaced 30+ legacy SKILL.md doc refs from `node ${PLUGIN_ROOT}/scripts/upstream/X.js` → kaizen Python equivalents (build_index / brain_audit / memory_schema / brain_promote / brain.py / evolution_log). 6 of 10 JS files have kaizen equivalents already (build-index→build_index.py, extract→brain_audit.py, schema→memory_schema.py, promote→brain_promote.py, append-evidence→brain.py, config→config.py); the other 4 ported. node runtime dependency RETIRED. 313 / 0 fails / 9.9s wall. iron-laws clean. |
| 2026-05-19 | feat | +~140 | kaizen-md skill scaffold — single orientation entry-point for the plugin (KMD step 1/N). Subsumes the discovery role of /kaizen:help; references intent (automation) + plugin-development (dev workflows) as specialized siblings. SKILL.md body documents the 6-cluster dispatch rubric: brain/memory, workflow/TDD, quality/coverage, plugin-dev, automation/hooks, discovery/inventory. Each cluster has its canonical skill + slash + bin surface. The kaizen-md skill is the front-door router; specialized skills do the actual work. Iron Law: read body in full (cluster taxonomy + dispatch rubric only hold together end-to-end). Triggers on "where do I start with kaizen", "what should I use", "which kaizen skill", "kaizen umbrella", etc. Scaffold-only — full implementation (CLI / dispatch rubric YAML / TDD tests) deferred to follow-up KMD steps. 313 / 0 fails / 9.9s wall. |
| 2026-05-19 | feat | +~520 | 4 umbrella skill scaffolds — kaizen-agent / kaizen-schema / kaizen-hook / kaizen-mcp (KMD step 2/N). Each is a dispatch hub for one major plugin aspect. kaizen-agent: catalog + dispatch rubric for the 7 specialized subagents with canonical Agent() prompt templates. kaizen-schema: 3-tier taxonomy (workflow routines / per-skill domain configs / per-skill data JSON Schemas) — 49 schemas + 32 domain yamls + 14 routines. kaizen-hook: 52 hooks across 12 Claude Code events catalogued; bypass-knob convention + add-new-hook checklist. kaizen-mcp: gateway SubServers fleet (29 MCPs under one FastMCP endpoint via RegexSearchTransform); tool-name discipline + add-new-MCP checklist. All 4 follow Iron Law "read in full" + cluster taxonomy + dispatch contract + DON'T-load-when guidance + pairing-with-siblings + triggers. Scaffolds only — CLI bins / rubric yamls / tests deferred. 313 / 0 fails / 10.0s wall. iron-laws clean (staged scope). |
| 2026-05-19 | refactor | +~95 -~30 | DOMAIN-1 — indexer cluster migrated to scripts/indexers/ (BIG-REFACTOR domain-incremental approach). 8 files git-mv'd: build_index, onboard_index, knowledge_index, claude_docs_index, scrape_index, trace_index, trace_index_gpu, loc_index. Each gets a sys.path MIGRATION BRIDGE so it can still import legacy helpers (_sqlite, _embed, _search, _indexer_cli, etc) staying in skills/workflow/scripts/. 9 legacy consumers (brain_mcp, knowledge_mcp, onboard_mcp, claude_docs_mcp, loc_mcp, scrape_mcp, symbol_search_mcp, index_flow, search_flow) got SYMMETRIC bridges so import build_index etc. resolve forward to scripts/indexers/. 14 test fixtures patched. Path string refs sed-updated (plugin.json permissions, commands frontmatter, hooks.json, docs). bin/kaizen-loc + setup.sh + daemon.py + bootstrap.sh + test_envelope updated to point at new locations. Key debugging lesson: parents[2] from skills/workflow/scripts/X.py = plugins/kaizen/skills/, NOT plugins/kaizen/ — bridge needs parents[3]. claude_docs_index needed manual fix (non-standard SCRIPT_DIR var). 313/0 fails / 10.0s wall. iron-laws clean. |
| 2026-05-19 | refactor | +~90 -~40 | DOMAIN-2 — hook handlers migrated to scripts/handlers/ (BIG-REFACTOR continuing domain-incrementally). 11 files git-mv'd: context_notifier, posttooluse_bash_commit, posttooluse_trace, pretooluse_trace, statusline_dxm, statusline_intent, stop_backlog_reminder, stop_karpathy_check, stop_self_improving_review, subagentstop_trace, userprompt_inbox. Each got MIGRATION BRIDGE to find kaizen modules (trace, inbox, _session_jsonl) still at skills/workflow/scripts/. Key fix: depth math from scripts/handlers/X.py is `parents[2] / skills / workflow / scripts` (NOT parents[1]). 9 shell hook wrappers in hooks/claude/*.sh sed-updated to invoke new paths. stop_karpathy_check needed _PLUGIN_ROOT fix (was .parent.parent.parent assuming skills/workflow/scripts depth; now .parent.parent for scripts/handlers depth). Test fixtures patched to add scripts/handlers to sys.path. 313/0 fails / 10.0s wall. iron-laws clean. Pattern proven for incremental domain moves with bridges. |
| 2026-05-19 | refactor | +~140 -~60 | DOMAIN-3 — brain cluster migrated to scripts/brain/. 8 files git-mv'd: brain, brain_audit, brain_evolve, brain_migrate, brain_promote, brain_mcp, _brain, _brain_blocks. Each got MIGRATION BRIDGE to skills/workflow/scripts/ (legacy helpers like flow, _embed) AND to scripts/indexers/ (for brain_mcp which imports build_index). brain.py _starters_dir() depth fix: parents[3] → parents[2] (scripts/brain/ now one level shallower than skills/workflow/scripts/ was). _brain.py _PLUGIN_ROOT depth fix: .parent.parent.parent → .parent.parent. trace_mcp got bridge to scripts/indexers/ (was importing trace_index). gateway.py got bridges to scripts/{brain,indexers,handlers}/ so it can mount the relocated MCP servers. brain hooks/_brain_redirect.py got bridge to scripts/brain/. ~15 test fixtures patched. 313/0 fails / 10.2s wall. iron-laws clean. |
| 2026-05-19 | refactor | +~250 -~120 | DOMAIN-4 — MCP servers migrated to scripts/mcp/. 33 *_mcp.py files git-mv'd. Each got MIGRATION BRIDGE to skills/workflow/scripts/, scripts/brain/, scripts/indexers/, scripts/handlers/ so they can resolve any consumer pattern. gateway.py extended with scripts/mcp/ bridge to mount the relocated servers. Sibling-file refs (dxm_mcp loads dxm.py, audit_mcp uses audit.sh, intent_mcp uses intent.py, gatekeeper_mcp uses gatekeeper.py, state_mcp uses status.sh+health.sh, backlog_mcp uses backlog.py, workflow_mcp uses workflow.sh+workflow_runner.py) updated to bridge back to skills/workflow/scripts/. surface.py MCP discovery updated to scan scripts/mcp/ first then fallback to legacy. _metrics.smoke_mcp() updated to scan scripts/mcp/. quality_mcp got explicit legacy bridge for frontmatter/coverage/name_quality/schema_coverage/slash_collision imports. ~16 test fixtures patched. 313/0 fails / 11.5s wall. iron-laws clean. The MIGRATION BRIDGE pattern proven across 4 domains now (indexers, handlers, brain, mcp). |
| 2026-05-19 | refactor | +~200 -~80 | DOMAIN-5 — quality axes migrated to scripts/quality/. 34 files git-mv'd: bin_coverage, class_name_quality, command_allowed_tools_coverage, complexity, coverage, dead_code, density, heavy_imports, hook_cascade, hook_coverage, hook_trace_coverage, mcp_coverage, mcp_trace_coverage, md_dupes/heading_depth/link_rot/whitespace, name_quality, perm_coverage, prompt_event_diff/rhythm, sandbox_check, schema_coverage, schema_load_coverage, silent_fail, slash_collision, subprocess_rc, test_density/isolation/name_quality, tname_quality, todo_inventory, turn_density, unused_env, var_name_quality, frontmatter, menu_lint. Each got MIGRATION BRIDGE to skills/workflow/scripts/. parents[2] → parents[1] in body (plugin_root depth shifts -1). 28 bin/ symlinks repaired (broken after move) to point at ../scripts/quality/X.py. coverage._list_source_scripts + perm_coverage._argparse_main_scripts updated to walk BOTH legacy + new scripts/ recursively. quality_mcp.py bridge extended to scripts/quality/. coverage._default_root + slash_collision._plugin_commands_dir + schema_coverage._plugin_root depth fixes (was parents[3]/[2] for skills/workflow/scripts depth; now parents[2]/[1] for scripts/quality depth). ~28 test fixtures patched. 313/0 fails / 10.1s wall. |
| 2026-05-19 | refactor | +~50 -~15 | DOMAIN-6 — daemon cluster migrated to scripts/daemon/. 3 files: daemon, _daemon_jobs, _index_kit. Daemon adds MIGRATION BRIDGE to skills/workflow/scripts/ for _paths/etc. gatekeeper.py _check_indexer_stale gets bridge to scripts/daemon/ for _index_kit. test_daemon.TestScriptsDir updated to expect daemon.py at scripts/daemon/ (was assuming everything in one dir). Test path-string refs sed-updated. 313/0 fails. |
| 2026-05-19 | refactor | +~25 -~10 | DOMAIN-7 — lint cluster migrated to scripts/lint/. 3 files: lint_fix_dispatch, lint_fix_prefs, lint_fix_setup. Each got MIGRATION BRIDGE to skills/workflow/scripts/. 6 test fixtures patched. 313/0 fails first try (cleanest domain migration yet). |
| 2026-05-19 | refactor | +~820 -~40 | DOMAIN-8 — hook handler privates + rules cluster migrated. 6 _*.py handlers git-mv from hooks/claude/ to scripts/handlers/ (_bash_discipline_scan, _bash_gate, _brain_redirect, _observer_capture, _roundtrip_detect, _write_atomic). 3 rules-cluster files git-mv from skills/workflow/scripts/ to scripts/rules/ (rules, rubric, schema_cli). 5 hook .sh wrappers (posttool-observer-capture, posttool-roundtrip-detect, pretooluse-bash-gate, pretooluse-brain-redirect, pretooluse-write-atomic) replaced unbound $PLUGIN_ROOT with locally-computed $_HANDLERS_DIR — safe under set -u. 3 shim files placed at skills/workflow/scripts/{rules,rubric,schema_cli}.py — importlib.spec_from_file_location loads the canonical and aliases sys.modules so bare `import schema_cli` from any caller returns the same module object (no duplicate-module / isinstance pitfall). Boy-Scout: scripts/rules/{rules,rubric,schema_cli}.py legacy-bridge path was parents[1] (resolved to non-existent scripts/skills/workflow/scripts/) — corrected to parents[2]. bin/kaizen-rubric + bin/kaizen-rules retargeted at scripts/rules/. test_bash_gate HOOKS_DIR → scripts/handlers/. test_rubric_cli _RUBRIC_PY → scripts/rules/rubric.py (shim cannot trigger __main__ via spec.exec_module). NEW: scripts/debug.py (709 LOC universal error-locator) + bin/kaizen-debug symlink — 5 verbs (scan/parse/replay/lint/tail) detect unbound-var / cmd-not-found / module-not-found / import / file-not-found / syntax / pytest fail / assertion / typed-errors; kaizen-specific hints (e.g. PLUGIN_ROOT unbound → _HANDLERS_DIR pattern). Bypass: KAIZEN_DEBUG_DISABLE=1. 313/0 fails / 10.2s wall. |
| 2026-05-19 | refactor | +~580 -~30 | DOMAIN-9 — io cluster migrated to scripts/io/. 14 files git-mv from skills/workflow/scripts/: _atomic, _envelope, _jsonl, _kaizen_dispatcher, _paths, _plugin_root, _sqlite, _subproc, _time, _uv, io, kaizen_write, kaizen_yaml, sql. The high-blast-radius surface (_envelope = 71 consumers; _paths = 34; _atomic = 16) is fenced by 10 importlib-spec shims at the legacy paths (same pattern as DOMAIN-8 rules cluster — spec_from_file_location loads canonical + sys.modules alias = single module object, no duplicate-module pitfall). 5 bin wrappers (kaizen, kaizen-io, kaizen-sql, kaizen-write, kaizen-yaml) retargeted at scripts/io/. 7 test fixtures (test_envelope, test_io, test_io_multi_read, test_kaizen_cli, test_kaizen_write, test_kaizen_yaml, test_sql) updated — fixtures that spec_from_file_location the LEGACY path under a different name get only the shim namespace, not the canonical attrs; canonical-path is the correct fixture target. 313/0 fails / 10.4s wall. Pattern proven across DOMAINs 1-9. |
| 2026-05-19 | refactor | +~15 -~15 | DOMAIN-14 — tests cluster migrated to scripts/tests/. 5 files git-mv: tests_run, run_tests_parallel, _tests, _tests_run, _affected_tests. _affected_tests.py parents[3] → parents[2] (depth shifts -1). _tests.py adds MIGRATION BRIDGE for _paths still at skills/workflow/scripts/ (subprocess `import _paths` strings updated to use _LEGACY_SCRIPTS). 3 bin wrappers (kaizen-tests + kaizen-run-tests-parallel + symlink kaizen-tests-run) retargeted at scripts/tests/. 3 test fixtures (test_tests_run, test_run_tests_parallel, test_affected_tests) updated to canonical path. No shims at legacy needed — only consumers were the 3 fixtures + 3 bin wrappers, all retargeted directly. 313/0 fails. |
| 2026-05-19 | refactor | +~25 -~10 | DOMAIN-shells Wave A — git-hooks cluster migrated to scripts/git-hooks/. 4 files git-mv: pre-commit.sh, post-commit.sh, commit-msg.sh, lib.sh. lib.sh:75 + pre-commit.sh:555 (sibling sources of _plugin_root.sh / _paths.sh) updated to reach back to skills/workflow/scripts/ (deferred — those 2 helpers stay legacy for now since 15+ hook .sh consumers). post-commit.sh PLUGIN_ROOT depth ../../../ → ../../ (scripts/git-hooks/ is one level shallower than skills/workflow/scripts/). pre-commit.sh DETECT_PY updated to reach scripts/workflow/scripts/detect_stack.py (DOMAIN-24 deferred). 3 .kaizen/hooks/ symlinks repointed (pre-commit, post-commit, commit-msg). 3 legacy .sh consumers (uninstall.sh, health.sh, publish.sh) updated to source lib.sh via ../../../scripts/git-hooks/lib.sh. 2 test fixtures + 1 slash command body updated. 313/0 fails on isolated runs (test_loop_merge / test_loop_promise_robust flake intermittently — pre-existing, unrelated). Pattern: keep blast-radius helpers (_paths.sh, _plugin_root.sh) at legacy and reach back; only move when ready to update all 15+ consumers. |
| 2026-05-19 | refactor | +~150 -~30 | DOMAIN-shells Wave B — ops cluster migrated to scripts/ops/. 10 .sh files git-mv: audit, backup, ci-gate, health, review, status, statusline, test-pipeline, vibe_check, workflow. Pattern shift from python shim: shell shims at legacy paths re-exec the canonical via `exec bash "$_DIR/../../../scripts/ops/X.sh" "$@"` — every caller (docs, MCP servers, slash commands, hooks) keeps working without per-caller updates. Internal sibling-path fixes: 5 files (audit/backup/review/status/test-pipeline) source _paths.sh via `_SCRIPT_REAL_DIR/../../skills/workflow/scripts/_paths.sh` (deferred at legacy). health.sh sources lib.sh via `_LIB_DIR/../git-hooks/lib.sh` (one-up to sibling) + _paths.sh same legacy path. ci-gate.sh REPO_ROOT depth ../../../../.. → ../../../.. (scripts/ops/ is shallower). audit.sh + review.sh sibling config.py refs reach back to skills/workflow/scripts/. ci-gate.sh run_tests_parallel.py reaches across to scripts/tests/. backup.sh _blobs.py reaches legacy. workflow.sh 5 internal `dirname $0` refs to trace.py + workflow_runner.py reach to skills/workflow/scripts/ (DOMAIN-12/16 deferred). 2 test fixtures updated (test_ci_gate, test_workflow_trace) to canonical path. 313/0 fails on 2 consecutive runs. |
| 2026-05-19 | refactor | +~180 -~40 | DOMAIN-shells Wave C — install cluster migrated to scripts/install/. 9 .sh files git-mv: bootstrap, setup, uninstall, refresh-cache, disable-skill, enable_all, setup-local-llm, update, publish. kaizen-env.sh KEPT at legacy (user-sourced; the exec-shim trick does NOT propagate env back to the calling shell; needs a source-style shim deferred to a future commit). 9 shell shims at legacy paths exec-bridge to canonical, preserving callers. setup.sh required deeper rework: SKILL_DIR semantic was the historical "skills/workflow/" parent — re-pinned via _SCRIPT_DIR/../../skills/workflow/ so `$SKILL_DIR/scripts/config.py` etc still resolve to legacy (config.py + backlog.py + migrate_paths.sh + _prune_bin_symlinks.py deferred for their own DOMAINs). HOOK_TARGET now points at $PLUGIN_ROOT/scripts/git-hooks/ (DOMAIN-shells Wave A). bootstrap.sh PLUGIN_ROOT depth ../../../ → ../../ (scripts/install/ shallower). uninstall.sh + publish.sh lib.sh path /../../../scripts/git-hooks/lib.sh → /../../scripts/git-hooks/lib.sh. disable-skill.sh PLUGIN_SKILLS_DIR walks explicitly to ../../skills/ (was ../.. which now lands at plugins/kaizen). update.sh SCRIPT_DIR moved from $PLUGIN_SRC/skills/workflow/scripts → $PLUGIN_SRC/scripts/install (refresh-cache.sh moved with it). 2 test fixtures (test_kaizen_config_schema, test_setup_consolidation) updated to canonical path. 313/0 fails on 2 consecutive runs. |
| 2026-05-19 | refactor | +~700 -~30 | DOMAIN-11 — handoff cluster migrated to scripts/handoff/. 4 files git-mv: handoff, auto_handoff, _handoff, _session_jsonl. 4 Python importlib-spec shims at legacy paths (skills/workflow/scripts/{handoff,auto_handoff,_handoff,_session_jsonl}.py) so bare `import X` from any caller still resolves to the canonical module — _session_jsonl has 14 consumers across handlers/, legacy /, and tests; the shim covers all. Each moved file gets MIGRATION BRIDGE to legacy skills/workflow/scripts/ for cross-cluster deps (_envelope/_atomic/_dxm_emit/_sqlite/context/session_mode). Boy-Scout: handoff.py + auto_handoff.py + _handoff.py PLUGIN_ROOT depth was parent.parent.parent (4 ups from skills/workflow/scripts/) → fixed to parent.parent (3 ups from scripts/handoff/). handoff.py dxm.py subprocess invocation updated to reach skills/workflow/scripts/ (DOMAIN-16 deferred). 2 bin wrappers (kaizen-handoff, kaizen-auto-handoff) retargeted. 13 test fixtures bulk-updated (_SCRIPTS + sys.path.insert + _HANDOFF_PY); 4 fixtures (test_envelope, test_handler_emits, test_handoff_dxm_integration) targeted specifically because they invoke handoff.py as a script (shim cannot trigger __main__). 313/0 fails on 2 consecutive runs. |
| 2026-05-19 | refactor | +~1500 -~80 | DOMAIN-10 — index cluster migrated to scripts/index/. 23 files git-mv: _embed (14 cons), _search (11), _sparse, _colbert, _crawl, _quant, _chunk (7), _ast_chunk (6), _ts_chunk, _blobs, _summary, _indexer_cli (7), _ollama, _token_db (6), _token_extractor, _token_watch, search_flow, index_flow, plugin_docs, docs_flow, docs_gen, tokens, token_bloat. 23 Python importlib-spec shims at legacy paths (consistent with DOMAIN-9 + DOMAIN-11 pattern). 4 bin wrappers (kaizen-docs, kaizen-token-bloat, kaizen-plugin-docs, kaizen-tokens) retargeted. ~10 test fixtures bulk-updated to canonical scripts/index/ paths. Boy-Scout depth fixes: token_bloat._plugin_root() parents[2]→parents[1] (SCRIPT_DIR base) + parents[1]→parents[1]+/skills/ for split-rubric YAML path (added missing "skills" middle component); search_flow + index_flow indexers-bridge parents[3]→parents[2]; legacy MIGRATION BRIDGE depth distinction: Path(__file__).resolve().parents[2] for whole-file expr vs SCRIPT_DIR.parents[1] for already-parent expr (different N for same plugin_root target). backup.sh _blobs.py reach updated to scripts/index/. 8 canonical files added inline MIGRATION BRIDGE for cross-cluster sibs. 313/0 isolated; flaky /loop tests intermittent under parallel run. |
| 2026-05-19 | refactor | +~200 -~20 | DOMAIN-18 — intent/inbox cluster migrated to scripts/intent/. 3 files git-mv: intent, intent_userprompt, inbox. 3 Python importlib-spec shims at legacy paths. Sibling reaches: intent adds sys.path for scripts/io + scripts/observe (`_envelope` + `_dxm_emit`); intent_userprompt adds scripts/observe + scripts/io (`_dxm_emit`, `intent`); inbox adds scripts/io (`_paths`, `_time`). Path-arithmetic: intent._intents_path() plugin_root `parent.parent.parent → parent.parent`. 2 bin wrappers retargeted (kaizen-intent, kaizen-inbox). plugin.json permissions entries updated. 4 hooks updated (stop-backlog-reminder, sessionend-drain, intent-userprompt, posttooluse-drain-inbox). commands/inbox.md repointed. scripts/mcp/intent_mcp.py spec_from_file_location target updated to scripts/intent/intent.py (was the shim, which dropped attrs due to host-naming). 5 test fixtures updated (test_intent, test_intent_mcp inline, test_handler_emits, test_intent_autoload_coverage, test_intent_mode, test_intent_scan_hook). 311/0 isolated. |
| 2026-05-19 | refactor | +~120 -~20 | DOMAIN-17 — migrate cluster moved to scripts/migrate/. 5 files git-mv: migrate.py, _migrator.py, path_migrate.py, migrate.sh, migrate_paths.sh. 3 Python importlib-spec shims + 2 shell exec-bridge stubs at legacy paths. path_migrate.py adds sys.path for scripts/io (_envelope/_paths). 1 bin wrapper retargeted. 2 plugin.json permissions entries updated. 1 command (migrate.md) + 1 script (install/enable_all.sh) repointed. 2 test fixtures updated (test_migrate_dispatch, test_path_migrate) with sys.path now pointing at scripts/migrate + scripts/io. 312/0 isolated. |
| 2026-05-19 | refactor | +~450 -~30 | DOMAIN-16 — dxm/trace/metrics/observe cluster migrated to scripts/observe/. 7 files git-mv: dxm, _dxm_emit, trace, metrics, _metrics, observe, observer_events. 7 Python importlib-spec shims at legacy paths so bare `import trace` / `import dxm` / `import metrics` / `import _metrics` (consumers across handoff, intent, gold, gold_mine, intent_userprompt, skill_suggest, _envelope schemas) keep working. Internal sibling reaches: metrics + dxm + trace + observe + observer_events + _metrics insert sys.path for scripts/io (`_envelope`, `_paths`, `_time`); observe inserts scripts/workflow + scripts/index (`schemas`, `_blobs`); dxm + _dxm_emit insert scripts/handoff for `_session_jsonl`. Path-arithmetic: _metrics.plugin_root() `parent×3 → parent×2`. trace.py module-level sys.path set added so `from _time import iso` + `import _envelope` resolve at canonical-load time. 5 bin wrappers retargeted (kaizen-dxm, kaizen-trace, kaizen-metrics, kaizen-observe, kaizen-observer-events). 3 plugin.json permissions entries updated. 4 hooks updated (_trace, metrics-session-end, stop-ralph, sessionend-drain). 3 commands updated (observe, metrics, trace). scripts/ops/workflow.sh _wf_trace `dirname/../workflow/scripts/trace.py → ../../scripts/observe/trace.py`. dxm_mcp.py canonical-load path updated to scripts/observe/dxm.py (was the legacy shim — which dropped attributes since spec_from_file_location names it `kaizen_dxm_mcp_inner` not `dxm`). handoff.py dxm subprocess path repointed. 9 test fixtures updated to canonical paths (test_dxm, test_dxm_clean, test_dxm_discovery, test_dxm_mcp, test_dxm_replay, test_envelope inline 6 entries, test_handoff_dxm_integration, test_intent_scan_hook, test_workflow_trace). 312/0 isolated; 1 pre-existing /loop flake under parallel runner. |
| 2026-05-19 | refactor | +~50 -~10 | DOMAIN-15 — backlog cluster migrated to scripts/backlog/. 1 file git-mv: backlog.py (cluster was a single CLI; "4-file" handoff estimate counted consumers). 1 Python importlib-spec shim at legacy path. backlog.py has no internal cross-cluster deps (stdlib-only). 1 bin wrapper (kaizen-backlog) + plugin.json permission + agents/kaizen-backlog-curator.md prose updated to canonical scripts/backlog/. 311/0 isolated. |
| 2026-05-19 | refactor | +~600 -~30 | DOMAIN-13 — iron-laws / drift / self-audit / surface / shim cluster migrated to scripts/iron-laws/. 10 files git-mv: iron_laws, _iron_laws, drift_cli, _drift, gatekeeper, self_audit, self_audit_agent, _self_audit, surface, shim. 10 Python importlib-spec shims at legacy paths. Internal sibling reaches: drift_cli + iron_laws + surface + self_audit + self_audit_agent each add sys.path for scripts/io (`_envelope`); self_audit + self_audit_agent add scripts/workflow for `flow`; iron_laws + _iron_laws keep skills/iron-laws/application sys.path (skill-side `_loader` + `codegen`). Path-arithmetic fixes (scripts/iron-laws shallower than legacy): _iron_laws._SKILLS_DIR parents[2]→parents[2]+/skills; iron_laws._APP_DIR _SCRIPT_DIR.parent.parent/iron-laws→+/skills/iron-laws; _self_audit + gatekeeper + surface PLUGIN_ROOT parent×3→parent×2; surface._gateway_subservers + _curated_core gw _SCRIPT_DIR/gateway.py → _PLUGIN_ROOT/skills/workflow/scripts/gateway.py (gateway not yet migrated). 7 bin wrappers retargeted (kaizen-iron-laws, kaizen-gatekeeper, kaizen-surface, kaizen-drift, kaizen-self-audit, kaizen-agent-self-audit, kaizen-shim). 3 plugin.json permissions entries updated. 6 commands updated (iron-laws, gatekeeper, surface, self-audit, agent-self-audit, loop md prose). 7 test fixtures updated to canonical paths (test_iron_laws CLI, test_gatekeeper, test_claude_md_bloat, test_surface, test_self_audit, test_self_audit_agent, test_bash_gate long-form, test_cli_rename_aliases, test_envelope inline 5 entries). Boy-Scout: scripts/handlers/_bash_gate.py _LONG_FORM_SCRIPT regex broadened to match both legacy skills/workflow/scripts AND new scripts/<cluster>/ paths so the long-form nudge stays accurate post-migration. 311/0 isolated, 2 pre-existing /loop flakes under parallel. |
| 2026-05-19 | refactor | +~700 -~30 | DOMAIN-12 — workflow cluster migrated to scripts/workflow/. 11 files git-mv: workflow_config, workflow_runner, flow, flow_cli, manifests_cli, _workflow_prefill, _manifests, schemas, onboard_schema, memory_schema, session_mode. 11 Python importlib-spec shims at legacy paths so bare imports (`import flow` has 13 consumers; `import session_mode`; `import _manifests` (2); `import workflow_config`; `import schemas`; `import onboard_schema`) keep resolving to the canonical module. Internal sibling-cluster reaches preserved: manifests_cli adds sys.path for scripts/io (`_envelope`); workflow_config dry-run / start adds scripts/rules + legacy skills/workflow/scripts (`schema_cli`); flow.py adds scripts/index + legacy (`docs_gen`); workflow_runner sys.path still legacy for `_paths` (DOMAIN deferred). Path-arithmetic fixes: workflow_runner PLUGIN_ROOT depth `parents[3]→parents[2]` (scripts/workflow shallower than skills/workflow/scripts); workflow_config builtin-schemas dir `parents[4]→parents[3]`; memory_schema schema-path `_SCRIPT_DIR.parent.parent / brain →  + /skills/brain` (one less subtree level). 4 bin wrappers retargeted (kaizen-manifests, kaizen-flow, kaizen-workflow-config, kaizen-session-mode). 5 plugin.json permissions entries updated. 3 hooks (session-intake, userprompt-classify, userprompt-skills-reminder) + 4 commands (workflow, schema, flow, plugin-development) + scripts/git-hooks/post-commit.sh + scripts/ops/workflow.sh (5 internal refs) repointed at canonical. setup-ralph-loop.sh WC_PY depth `../../workflow/scripts → ../../../scripts/workflow` (shim cannot trigger __main__). 8 test fixtures bulk-updated to canonical paths (test_envelope manifests_cli inline, test_flow_dispatch, test_workflow_runner_state/_trace, test_workflow_config_run/_dryrun, test_workflow_prefill, test_session_mode, test_mode_command, test_plugin_development_workflow). 312/0 isolated, 1 pre-existing /loop flake under parallel run. |
| 2026-05-19 | refactor | +~50 -~50 | BIG-REFACTOR cleanup — 122/122 bin wrappers green. CLEANUP-1: 18 bin wrappers + scripts/index/docs_gen.py repointed at canonical scripts/<cluster>/ (DOMAINs 1/2/3/4/5/6 had moved files without updating the bin wrappers; bins silently rc=0 with python3 'No such file' for months). CLEANUP-2: 28 scripts/quality/*.py had `parents[1] / skills / workflow / scripts` arithmetic — was correct from skills/workflow/scripts/X.py (parents[1]=skills), wrong after DOMAIN-5 move to scripts/quality/X.py (parents[1]=scripts). Repointed at canonical scripts/io for _envelope. CLEANUP-3: kaizen-brainstorm symlink corrected to scripts/util/brainstorm.py (was pointing at the Python shim — bash exec failed with syntax error). bin/kaizen-sql parents[2] → parents[1] depth fix. scripts/index/docs_gen.py adds scripts/io to sys.path. scripts/ops/ci-gate.sh broadened: bash-syntax-check skips files with python3 shebang; ast.parse walks scripts/<cluster>/*.py too. Test baseline: 312/0 isolated. |
| 2026-05-19 | refactor | +18 -9 | Migration verification + final fix: plugin.json had 6 permissions entries still pointing at legacy `skills/workflow/scripts/X.py` for files moved in DOMAIN-9/14 (kaizen_yaml, tests_run, sql, kaizen_write, _kaizen_dispatcher, run_tests_parallel) — repointed to canonical scripts/io + scripts/tests. scripts/workflow/flow_cli.py dispatch map for docs/index/search flows pointed at sibling _SCRIPT_DIR (now scripts/workflow/) — repointed at scripts/index/ where the *_flow.py canonicals live post-DOMAIN-10. scripts/index/index_flow.py + search_flow.py add scripts/workflow + scripts/io to sys.path so bare `import flow` + `import _envelope` resolve. Tests 313/0; 122/122 bin wrappers smoke-clean. Verified: plugin.json (219/219), hooks.json (55/55), .mcp.json (1/1) all resolve to real files. Canonicals imported clean (176/181 — the 5 import-broken are heavy-dep optional like trace_index_gpu numpy + indirect-load shapes like _ts_chunk which work via consumer-driven sys.path). |
| 2026-05-19 | refactor | +~80 -~12 | Command surface cleanup — 5 quick-win retirements. /kaizen:env → folded into /kaizen:setup env (was a self-declared alias). /kaizen:agent-self-audit → folded as /kaizen:self-audit agent verb (the bash dispatch routes agent|agent\ * to self_audit_agent.py). /kaizen:iron-laws → folded under /kaizen:plugin-development rules (already had the verb). /kaizen:tests → renamed to /kaizen:test-suite (eliminates singular/plural footgun with /kaizen:test which is the TAP pipeline smoke). /kaizen:trace-search → folded as /kaizen:trace search verb (heavy-dep semantic-search routes via kaizen-trace-index uv venv). Net: 57 → 53 commands (5 retired + 1 renamed). All bins kept for direct-CLI access. |
| 2026-05-19 | feat | +~280 -~5 | 4 new MCP servers for agent self-orientation (Phase 12 of the gateway). coverage_mcp wraps scripts/quality/coverage.py (gaps/summary/report); rules_mcp wraps scripts/rules/rules.py (list/show/validate brain-sourced rules); schema_mcp wraps scripts/workflow/workflow_runner.py (list/show/stages/artifact/branches/validate over declarative workflow schemas); surface_mcp wraps scripts/iron-laws/surface.py (list/validate plugin inventory + drift detection across gateway/hooks.json/.mcp.json). Each ~70-90 LOC subprocess wrappers — KISS, follow the canonical FastMCP shape (state_mcp/iron_laws_mcp template). Registered in gateway.py SUBSERVERS Phase 12 block (32 → 36 sub-servers). 4 plugin.json permissions entries added. Gateway boots clean (FastMCP 3.3.1, all sub-servers mount). 313/0 tests. Net new agent surface: gaps query for test coverage during write, rule discovery during turn-time, workflow schema inspection, plugin inventory drift detection. |
| 2026-05-19 | refactor | +52 -52 | Command description tighten — 52 of 53 /kaizen:* .md frontmatter descriptions rewritten for KISS+karpathy ("write code that reads like its intent"). Median 331 → 153 chars. Worst-offender (plugin-development 719) → 227. Over-250-chars: 43 → 0. Trigger phrases retained; explanatory tails cut. ascii-format consistency (Subcommands "Verbs - X | Y | Z"). chatlog.md unchanged (no frontmatter; deferred to separate touch). |
| 2026-05-19 | feat | +90 -1 | New `kaizen-metrics noise` subverb — aggregates 3 dynamic-trace noise axes (hook_cascade + silent_fail + turn_density) into one verdict + counts envelope. KISS: subprocess fan-out (each axis already emits the canonical envelope). DRY: `_run_axis()` helper extracts the subprocess+JSON-parse on 2nd-repetition trigger. SoC: pattern detection lives in metrics (rollup/never-used family), separate from observe (layer-centric). Static wiring axes (hook_coverage / mcp_coverage / *_trace_coverage) excluded — coverage ≠ noise. 313/0 tests. |
| 2026-05-19 | feat | +~250 -~5 | Three new declarative workflow schemas + observer rename + persisted workflow defaults. New schemas under plugins/kaizen/schemas/ - review (5 stages, gate=verdict — diff-time review of touched files; complement /kaizen:review slash), boy-scout (4 stages, gate=apply — RED-GREEN per fix; in-flight cleanup bounded to staged scope), consolidate (5 stages, gate=fold-plan — collapse-redundancy with no-functionality-lost gate; pattern matches the 5-slash retirement done earlier this session). Each ≤5 stages, present-tense intent descriptions, validates clean via workflow_runner. Rename kaizen-observer-events → kaizen-observer (bin symlink, prog name, plugin.json permission, iron-laws.yaml rationale refs, regenerated iron-laws.md). .kaizen/workflow.json persisted with run_mode=schema + schema_name=onion-tdd-strict + all 15 disciplines. 313/0 tests. |
| 2026-05-20 | docs | +~10 -~210 | CLAUDE.md drift + YAML→memory relocation. CLAUDE.md: stripped stale "7 sub-gates aggregated" (actual 14) and "Current axes: code-to-test / schema / name-quality / frontmatter" (token-bloat missing) → pointers at live registries (kaizen-gatekeeper list / kaizen audit:axis). Per pref-anti-drift-rule-for-catalog-docs captured to brain this session. CLAUDE_YAML.md (created earlier as sibling) relocated to ~/.claude/projects/-home-cherry86-workspace-kaizen-md/memory/project_claude-yaml-orientation-mirror.md per user directive "YAML content -> CLAUDE MEMORY": parseable orientation lives in auto-memory tier, not in the repo. CLAUDE.md remains canonical. |
| 2026-05-20 | feat | +257 -0 | New kaizen-inventory bin: walk the plugin tree, dump file contents to a bundle. Stdlib-only Python (254 LOC). 3 subverbs — dump (markdown/jsonl/raw formats) / list (json or paths) / stats (per-type counts+bytes). Filters: --type (py/yaml/yml/json/jsonl/md/sh/schema.json) + --include / --exclude globs + --max-size byte cap (default 200KB skip-with-stub). Always-excluded: __pycache__ / .git / *.pyc / node_modules / .venv. Smoke-tested: stats reports 1170 files / 7.7MB across 6 types; markdown dump fences with lang per type; jsonl dump emits {path,type,bytes,lines,truncated,content} per file. Plugin.json permissions wired. Bin symlinked via setup.sh. 318/0 tests. |
| 2026-05-20 | feat | +~60 -~10 | kaizen-inventory expansion — added 21 more file types + --split flag for per-type output. New canonical types: js/cjs/mjs/ts/tsx/rs/go/rb (code), rst/html/css (markup), toml/ini/cfg/conf/env/xml/csv/tsv/dot (data/config), and 8 bare-name files (LICENSE/README/CHANGELOG/CONTRIBUTING/Dockerfile/Makefile/.gitignore/.mcp.json). Bumped ALWAYS_EXCLUDE with .ruff_cache/.pytest_cache/.mypy_cache/dist/build/*.egg-info. --split mode bucketizes by type and emits <stem>.{md,jsonl,raw} per type into --out DIR — useful when one big bundle is too large for downstream tools or when you want to grep one type at a time. New stats: 1178 total files (was 1170) across 14 type buckets. py.md alone is 4.9MB; smallest splits (mcp-json, dot, toml) are 354B-1.1KB. |
| 2026-05-20 | feat | +~140 -~5 | kaizen-inventory v2 — pattern recognition + drift verb + per-type split. PATH_PATTERNS adds 19 semantic-role mappings (skill / command / agent / mcp / hook-sh / hook-py / hook-registry / handler / indexer / git-hook / domain-yaml / domain-schema / application-py / generated-ref / test / plan / routine-schema / iron-law-registry / bin-wrapper / plugin-manifest) → fnmatch-first wins over extension. CANONICAL_TYPES grew from 8 to 35 (added js/cjs/mjs/ts/tsx/rs/go/rb/rst/html/css/toml/ini/cfg/conf/env/xml/csv/tsv/dot + 8 bare-name files like LICENSE/Dockerfile/.mcp.json/.gitignore). ALWAYS_EXCLUDE picks up .ruff_cache / .pytest_cache / .mypy_cache / dist / build / *.egg-info. New verb: `drift` reports pattern-matched vs extension-only vs orphan counts + drift % from established kaizen-plugin shape (current: 63.5% pattern / 36.4% ext / 0.2% orphan = 2 true orphans surfaced for backlog). New flag: `--split` on dump emits one file per type bucket into --out DIR. Stats now shows 14 type buckets (was 6); test=318, bin-wrapper=128, skill=98, domain-schema=49, hook-sh=45, mcp=38 etc. All four subverbs work with --json. 318/0 tests preserved. |
| 2026-05-20 | feat | +~30 -~5 | kaizen-inventory v3 — 17 new path patterns. claude-md (CLAUDE.md anywhere) / agents-md / memory-index / rubric-yaml (4 hits: dispatch-rubric / brainstorm-rubric / split-rubric / auto-handoff rubric) / checklist-yaml (2: intake / wiring) / iron-law-registry (hoisted above generic domain-yaml so SSOT registry classifies distinctly) / cross-skill-schema (15 assets/schemas/*) / starter-asset (27 files in assets/starters/) / template-asset / jsonl-deliverable (3 brainstorm + 1 plan-index + future inventory/audit jsonl) / brainstorm / audit-report / backlog-source (.kaizen/workflow/backlog.json) / backlog-md / workflow-state / architecture-log (.kaizen/workflow/progress.md) / deletion-log / workflow-config. SKILL.md pattern broadened from skills/<X>/SKILL.md only → SKILL.md anywhere. Re-ordering moved iron-law-registry + rubric/checklist BEFORE generic domain-yaml (first-match-wins). Drift % improved 36.5% → 32.9% (pattern coverage 63.5% → 67.1%, +48 files into semantic buckets). New stats shows 36 type buckets (was 14 in v2). Same 318/0 tests. |
| 2026-05-20 | feat | +~600 -~250 | kaizen-inventory v4 — all 20 powertool features TDD-implemented. tests/test_inventory.py (368 LOC, 21 tests across 5 batches A-E) authored RED-first, then GREEN-implemented in scripts/util/inventory.py (1030 LOC total). BATCH A (per-file enrichment): each jsonl record now carries tokens (chars/4 estimate) / sha256 / nonblank_lines / comment_lines (per-language hash-comment counters: py/sh/yaml) / symbols (ast.ClassDef + ast.FunctionDef for py; H1-H6 regex for md; top-level keys for yaml). BATCH B (subverbs): tree (directory layout + per-leaf metadata), outline (md headings + py docstrings), map (one-line-per-symbol grouped by file), grep-symbol (substring search over symbol index), --budget N (greedy file selection up to N tokens). BATCH C (filters): --not-type T, --mtime-since '7d'/'24h'/ISO/unix, --git-since REV (git diff REV...HEAD; falls back gracefully when git absent), --min-lines / --max-lines, --dedupe (collapse identical sha256). BATCH D (formats): --format html (self-contained <details> collapsibles), --for-rag (chunked ~1000-token windows with line-range metadata), --for-llm (structure-tree header), --sqlite DB (files+symbols+indexes), --validate (json/jsonl/yaml/py parse-check → validation_status). BATCH E: --with-graph adds imports + imported_by per py file via ast.Import/ast.ImportFrom 2-pass walk. DRY refactor: extracted ALL_TYPES = CANONICAL_TYPES + PATH_PATTERN labels constant (was duplicated in 3 spots). KISS: stdlib-only, no new deps. 319/0 tests (21 new + 318 preserved). bin smoke 119/0. |
