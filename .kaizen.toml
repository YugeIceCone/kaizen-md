#:schema https://raw.githubusercontent.com/YugeIceCone/kaizen-md/main/plugins/kaizen/assets/schemas/kaizen-config.schema.json
# kaizen config — refreshed 2026-05-17 (v1.39.0 — dropped
# user-specific brain_path / project_memory_path; defaults from
# _paths.py take over).
# Schema (Taplo / Even Better TOML auto-binds via the #:schema
# directive above): plugins/kaizen/assets/schemas/kaizen-config.schema.json

compile_check_cmd = ""
architecture_log  = ".kaizen/workflow/progress.md"
plan_dir          = "plans"
backlog_path      = ".kaizen/workflow/backlog.md"
verify_cmd        = ""

allow_deletion_env = "KAIZEN_ALLOW_DELETE"
skip_tdd_check_env = "KAIZEN_SKIP_TDD_CHECK"

# brain_path + project_memory_path INTENTIONALLY OMITTED — they're
# resolved via _paths.py defaults (~/.claude/.kaizen/brain and
# ~/.claude/projects/<cwd-slug>/memory). Add them back ONLY if you
# need a custom location (per-setup, not a project commitment).
